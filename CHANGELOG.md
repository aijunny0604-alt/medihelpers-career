# 업데이트 기록

## 2026-08-15 · 마이페이지 권한·세션 충돌 복구

- HttpOnly 쿠키와 탭 보조 토큰이 함께 있을 때 오래된 탭 토큰 때문에 정상 쿠키 로그인이 무시되던 문제를 수정했습니다. 쿠키를 기준으로 사용하고 유효하지 않을 때만 보조 토큰으로 복구합니다.
- 계정 API의 일시 오류를 비회원 판정으로 바꾸지 않고 재시도·자동 재연결하도록 했습니다. 이미 확인된 역할과 마이페이지 권한은 유지됩니다.
- 회원센터 연결 오류 화면에서 로그인 중인 회원에게 다시 로그인하라는 버튼을 제거하고, 실제 데이터가 도착하기 전 빈 대시보드 수치를 표시하지 않도록 했습니다.
- 일반회원·병원회원 마이페이지 메뉴와 관리자 전환을 로컬 브라우저에서 직접 검증했고, 자동 테스트 **145/145**와 운영 빌드가 통과했습니다.
- Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩 및 D1 스키마는 변경하지 않았습니다.

## 2026-08-15 · 전체 기능 재검증·의존성 보안 정리

- 공개 화면·상세 화면·가입·로그인·주소 검색과 의료인·병원·관리자 역할별 마이페이지를 운영 사이트에서 직접 점검했습니다. 주요 화면의 깨진 이미지, 가로 넘침, 잘못된 가입 안내, 인증 버튼 깜빡임과 내부 접수 코드 노출은 발견되지 않았습니다.
- 역할별 보호 API를 별도 세션으로 확인해 의료인 이력서, 병원 회원센터, 관리자 읽기 전용 콘솔의 허용 범위와 비회원·다른 역할의 `401`·`403` 차단이 정상임을 확인했습니다.
- 병원 가입의 카카오 주소 검색 UI, 채용공고와 맞춤 헤드헌팅 독립 상세 페이지, 일반 사용자의 내부 코드 비노출과 주요 화면 접근성 이름을 실제 브라우저로 확인했습니다.
- 의존성 잠금 버전을 안전한 최신 호환 버전으로 갱신해 `npm audit --omit=dev`의 알려진 취약점을 4건에서 **0건**으로 정리했습니다.
- Node 자동 테스트 **144/144**, 프로덕션 빌드, 생성 Worker 문법 검사와 diff 검사를 통과했습니다. 과거 D1 레거시 원문, Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩과 스키마는 변경하지 않았습니다.

## 2026-08-15 · 일반·병원·관리자 전환 동작 복구

- 상단 테스트 계정 버튼이 전환 성공 뒤 현재 페이지를 다시 불러오던 방식을 제거했습니다. 서버가 반환한 역할을 즉시 공용 인증 상태에 반영해 같은 화면에서 버튼과 권한 콘텐츠가 바뀝니다.
- 느린 이전 계정 응답이 새 역할을 덮는 경쟁 상태를 요청 순번 검사로 차단하고, 전체 계정 정보는 로딩 화면 없이 백그라운드에서 재확인합니다.
- 마이페이지의 독립 회원조회도 공용 역할 변경을 따르도록 연결했습니다. 역할이 바뀌면 이전 회원의 프로필·대시보드 데이터를 즉시 제거하고 새 역할의 회원센터 데이터를 다시 불러옵니다.
- 병원회원·관리자·일반회원 버튼을 실제로 순서대로 눌러 URL 유지, 선택 상태 반영, 잔여 로딩 요소 `0개`를 확인했습니다. 500ms 느린 계정 조회에서도 선택 역할이 되돌아가지 않았습니다.
- Node 자동 테스트 **144/144**, 프로덕션 빌드와 생성 Worker 문법 검사를 통과했습니다. D1 스키마와 Sites project_id, D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-15 · 운영 직접 검증 결과 문서화

- 공개 운영 사이트 version `279`와 commit `868ca62`를 기준으로 일반회원 주요 6개 경로를 브라우저에서 직접 재검증했습니다. 인증 완료 후 임시 자리 요소는 전부 제거됐고 역할이 맞는 콘텐츠가 표시됐습니다.
- 운영 서버에서 의료인·병원·관리자 테스트 계정 전환, D1 역할 조회, 이력서·회원센터·관리자 기록 보호 API를 실제 요청해 모두 HTTP `200`을 확인했습니다. 비회원 이력서 API의 HTTP `401`도 정상 권한 차단으로 확인했습니다.
- 2026-08-08 이전 테스트 활동 중 일부에 이미 저장된 `??` 원문이 남아 있는 사실을 상태·테스트 문서에 알려진 데이터 항목으로 기록했습니다. 이번 변경은 문서 동기화이며 D1 원본을 변경하지 않습니다.
- Node 자동 테스트 **142/142**, 프로덕션 빌드, 생성 Worker 문법 검사와 diff 검사를 다시 통과했습니다. Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-15 · 전 화면 로그인·역할 UI 안정화

- 공고 상세, 의료인 채용 상세, 이력서, 헤드헌팅 접수, 상담 자동입력, 인재 열람 결제, 로그인·가입 축하 화면의 중복 계정 조회를 제거하고 앱 공용 인증 상태 한 번으로 통일했습니다.
- 헤더 알림·계정·로그아웃과 병원 채용·광고 등록 행동은 권한 조회 전에 실제 문구나 잘못된 역할 버튼을 그리지 않으며, 동일 크기의 중립 자리로 레이아웃을 고정합니다.
- 관리자 콘솔은 실제 운영 DB 응답 전에 데모 기록과 조작 버튼을 잠깐 표시하지 않습니다.
- 로컬 개발 API에도 운영과 동일한 일반회원·관리자·병원회원 원클릭 전환과 느린 인증 재현 옵션을 추가해 역할별 첫 렌더를 반복 검증할 수 있게 했습니다.
- Node 자동 테스트 **142/142**, 프로덕션 빌드, 생성 Worker 문법 검사와 역할별 브라우저 점검을 통과했습니다. Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-15 · 회원 버튼 깜빡임 제거

- 앱 전체에서 한 번 확인한 로그인·회원 역할을 공고, 광고, 인재 게시판에 공통으로 전달하도록 바꿔 같은 화면의 `/api/account` 중복 조회를 제거했습니다.
- 계정 확인 전에는 비회원 가입·로그인 버튼을 먼저 그리지 않고 같은 크기의 중립 자리만 유지해 버튼이 나타났다 사라지거나 문구가 바뀌는 현상을 없앴습니다.
- 병원회원의 의료인 구인구직 화면에는 `의료인 회원으로 등록`·`구직글 등록` 버튼이 렌더링되지 않고 병원 인재 채용 상담 동선만 표시됩니다. 의료인회원은 이력서·구직글 등록과 헤드헌터 상담 동선을 유지합니다.
- 헤더, 모바일 메뉴·하단 바, 병원 채용 공고 등록 버튼과 광고 상품 버튼도 같은 로딩 기준을 사용합니다.
- Node 자동 테스트 **140/140**를 통과했으며 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-15 · 회원 역할 불일치 안내 정리

- 로그인한 병원회원이 의료인 전용 이력서 화면에 들어갔을 때 다시 로그인하거나 회원가입하도록 유도하던 버튼을 제거했습니다.
- 계정 역할이 맞지 않으면 보호된 이력서 폼을 뒤에서 렌더링하지 않고, 현재 로그인 역할과 이용 가능한 회원 유형만 간단한 독립 안내 페이지로 표시합니다.
- 안내 페이지에 `뒤로 가기` 버튼을 추가했습니다. 같은 사이트의 이전 화면이 있으면 돌아가고, 직접 주소를 연 경우에는 역할에 맞는 공개 목록으로 안전하게 이동합니다.
- 비회원은 기존 로그인 경로를 유지하고 의료인회원과 관리자는 기존처럼 이력서 화면을 이용합니다.
- Node 자동 테스트 **137/137**를 통과했으며 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-15 · 채용공고 등록 화면 확대

- 채용공고 등록 페이지의 섹션 제목, 설명, 필드 라벨, 입력 글자와 입력 높이를 데스크톱에서 한 단계 크게 조정했습니다.
- 병원 브랜드 이미지 업로드, 샘플 배너 선택, 안내 문구, 결제 요약과 최종 등록 버튼의 크기·간격도 함께 확대했습니다.
- 781~980px 구간에서는 브랜드 이미지 영역을 한 열로 전환하고, 모바일에서는 기존 한 열 구조와 화면 폭을 유지합니다.
- Node 자동 테스트 **135/135**와 프로덕션 빌드·생성 Worker 문법 검사가 통과했습니다. Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-15 · 병원 채용 페이지 중간 메뉴 간소화

- 병원 채용 페이지 중간의 `전체 채용`, `의료인 구인구직`, `맞춤 초빙`, `내 활동` 이동 메뉴를 제거했습니다.
- 같은 위치에는 병원 광고 등록으로 이어지는 `공고 등록` 버튼만 남기고, 비로그인 사용자는 병원회원 가입 후 이어서 진행하도록 유지했습니다.
- Node 자동 테스트 **134/134**와 프로덕션 빌드·생성 Worker 문법 검사가 통과했습니다. Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-15 · 채용공고 등록 조건 입력 간소화

- 채용공고 등록의 `채용조건`을 초빙 분야(필수), 급여·보수, 근무 일정, 추가 안내 네 항목으로 줄였습니다.
- 인센티브·당직·환자량·시술 범위·지원 인력·휴무·입사 시점·면접 절차처럼 길게 나뉘어 있던 선택 입력은 제거하고, 필요한 내용만 추가 안내에 자유롭게 적도록 정리했습니다.
- 병원회원의 기존 공고 수정 화면도 같은 네 항목으로 통일했습니다. 화면에서 감춘 기존 세부 데이터는 수정 저장 시 삭제하지 않고 보존합니다.
- Node 자동 테스트 **133/133**와 프로덕션 빌드·생성 Worker 문법 검사가 통과했습니다. Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-14 · 실제 메일 활성화 운영 문서 정리

- 가입자 축하 메일과 대표자 신규 가입 알림을 실제로 보내기 위한 Resend 도메인 인증, Sending access API 키 발급, OpenAI Sites 환경변수 등록, 재배포, 테스트 가입 순서를 운영 문서에 추가했습니다.
- 실제 회원 발송은 `onboarding@resend.dev`가 아니라 인증된 `@medihelpers.co.kr` 발신 주소를 사용하도록 명확히 했습니다.
- 현재 공개본을 Sites v272·소스 커밋 `9a3a871`, 자동 테스트를 132건으로 동기화했습니다.

## 2026-08-14 · 회원가입 축하 페이지·가입 알림 이메일

- 회원가입 완료 직후 `/signup/welcome` 전용 축하 페이지로 이동하며, 가입 이름·회원 유형·가입 이메일과 역할별 다음 작업을 안내합니다.
- 가입자에게는 가입 이메일로 축하 메일을 보내고, 대표자·관리자에게는 `ALERT_EMAIL_TO`에 등록된 주소로 신규 가입 안내 메일을 보냅니다.
- 관리자 메일에는 회원 유형, 가입자명, 가입 이메일, 병원명 또는 전문분야, 가입 시각만 포함하며 비밀번호·인증정보는 포함하지 않습니다.
- 두 메일은 가입 DB 저장과 세션 발급 후 백그라운드로 각각 발송합니다. 한쪽 메일이나 외부 메일 서비스가 실패해도 회원가입 자체는 취소되지 않습니다.
- `RESEND_API_KEY`와 `RESEND_FROM`이 없는 환경에서는 발송 성공을 가장하지 않고 축하 페이지에 가입 저장 완료와 메일 설정 대기 상태를 표시합니다.
- Node 자동 테스트 **132/132** 통과. 기존 Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩과 D1 스키마는 변경하지 않았습니다.

## 2026-08-14 · 이메일 아이디 찾기·비밀번호 재설정

- `/account/recovery`를 가입 이메일 안내 방식으로 교체했습니다. 아이디 찾기는 이름·휴대전화가 일치하면 가입 이메일을 안내하고, 비밀번호 찾기는 가입 이메일로 30분 일회용 재설정 링크를 보냅니다.
- 재설정 토큰은 원문이 아닌 SHA-256 해시만 D1 `account_password_resets`에 저장하며 만료·사용 여부와 발송 상태를 추적합니다. 재설정 URL에는 가입 이메일을 넣지 않습니다.
- 새 비밀번호는 기존과 같은 PBKDF2-SHA-256(100,000회) 규칙으로 저장하고, 변경 완료 시 해당 계정의 기존 로그인 세션과 남은 재설정 링크를 모두 무효화합니다.
- 공개 응답과 화면에서 계정 존재 여부·대상 이메일·내부 접수번호를 노출하지 않습니다. `RESEND_API_KEY`와 `RESEND_FROM`이 없을 때는 발송 성공을 가장하지 않고 설정 미완료 안내를 표시합니다.
- D1 스키마는 런타임 `ensureAccountSchema`와 `drizzle/0010_email_password_reset.sql`로 기존 바인딩을 유지한 채 자동 확장하고, 새 토큰 테이블을 R2 일일 백업 대상에 포함했습니다.

## 2026-08-11 · 작성자 본인 채용공고 수정

- 병원회원 마이페이지 `내 공고 관리`에 본인이 결제·등록한 채용공고의 수정 버튼과 독립 수정 페이지를 추가했습니다.
- 제목, 병원 정보, 채용 조건, 소개, 로고와 3:1 배너를 수정할 수 있으며 이미지는 선택·드래그앤드롭·클립보드 붙여넣기를 지원합니다.
- 서버가 로그인한 병원 계정과 결제 주문의 `account_id`를 함께 확인해 다른 병원의 공고 수정 요청을 차단합니다.
- 결제금액, 광고 상품 등급, 노출 시작·종료일은 기존 값을 보존하고 작성 내용만 갱신합니다. 의료인 구직글은 기존 이력서 수정 흐름을 유지합니다.
- Node 자동 테스트 **125/125**, 프로덕션 빌드와 생성 Worker 문법 검사가 통과했습니다. D1 스키마와 Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-11 · 상단 테스트 계정 전환 후 현재 화면 유지

- 상단의 일반회원·병원회원·관리자 테스트 전환 버튼에 남아 있던 마이페이지 강제 이동을 제거했습니다.
- 일반 화면에서 계정을 전환하면 같은 화면을 새 로그인 세션으로 다시 열고, 로그인·회원가입 화면에서 전환한 경우에만 메인으로 이동합니다.
- 이메일 로그인은 기존처럼 명시된 안전한 이전 화면으로 복귀하며, 이동 대상이 없으면 메인으로 이동합니다.
- Node 자동 테스트 **120/120** 통과. D1 스키마와 Sites 바인딩은 변경하지 않았습니다.

## 2026-08-10 · 관리자 계정 DB 기록 조회 전용 전환

- 관리자 콘솔을 회원·이력서·공고·상담·채용·결제·환불·백업 기록을 확인하는 읽기 전용 화면으로 정리했습니다.
- 관리자용 결제 상태 변경, 환불 승인·거부, 회원 상태 변경, 공고 등록·수정·삭제, 수동 백업 버튼과 공개 화면의 관리자 관리 버튼을 제거했습니다.
- `/api/admin-console`, 관리자 상담·채용 CRM·백업 API는 `GET` 조회만 허용하고 변경 요청은 서버에서 거부합니다.
- 병원회원의 결제와 결제 완료 즉시 공고 게시, 회원·지원·알림·결제 결과의 자동 DB 저장은 그대로 유지합니다.
- Node 자동 테스트 **119/119**, 프로덕션 빌드와 생성 Worker 문법 검사가 통과했습니다. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-10 · 로그인 후 이동 흐름 개선

- 일반·병원회원이 로그인할 때마다 마이페이지로 강제 이동하던 동작을 제거했습니다.
- 공고·상담 등 이용 중이던 화면에서 로그인하면 해당 화면으로 돌아가며, 직접 로그인 화면을 연 경우에는 메인으로 이동합니다.
- 관리자는 직접 로그인 시 기존처럼 운영 콘솔로 이동하고, 외부 주소를 이동 대상으로 사용할 수 없도록 내부 경로 검증을 유지합니다.
- Node 자동 테스트 **118/118** 통과. D1 스키마와 Sites 바인딩은 변경하지 않았습니다.

## 2026-08-10 · 의료인 구인구직 히어로 정리

- 화면 오른쪽의 `병원 회원·열람권` 안내 카드를 제거해 구직 페이지 첫 화면을 단순하게 정리했습니다.
- 핵심 타이틀을 넓은 단일 컬럼의 대형 2줄 제목으로 확장하고, 모바일에서는 화면 폭에 맞게 크기가 줄어들도록 조정했습니다.
- 히어로와 구직 게시판 사이의 과도한 세로 여백도 줄였습니다.
- Node 자동 테스트 **116/116** 통과. D1 스키마와 Sites 바인딩은 변경하지 않았습니다.

## 2026-08-10 · 구직글 등록 버튼 위치 개선

- 페이지 제목 옆에 떠 있던 구직글 작성 버튼을 게시판 탭·검색 도구 모음으로 이동했습니다.
- 넓은 화면에서는 검색창 옆에 정렬되고, 작은 화면에서는 검색창 위에 전체 폭으로 표시됩니다.
- 의료인·비회원·병원회원의 기존 작성 권한 분기는 그대로 유지합니다.
- Node 자동 테스트 **115/115** 통과. D1 스키마와 Sites 바인딩은 변경하지 않았습니다.

## 2026-08-10 · 병원회원 채용공고 역할 안내 정리

- 이미 로그인한 병원회원에게 의료인 로그인·회원가입을 다시 요구하던 채용공고 상세 안내를 수정했습니다.
- 병원회원은 버튼 없는 의료인 전용 안내를 보고, 의료인만 상세조건 열람과 직접 지원을 진행할 수 있습니다.
- 비회원의 의료인 로그인·가입 동선과 의료인회원의 정상 열람은 유지합니다.
- 역할 확인이 끝나기 전 잘못된 가입 버튼이 잠깐 보이지 않도록 로딩 상태를 분리했습니다.
- Node 자동 테스트 **114/114** 통과. D1 스키마와 Sites 바인딩은 변경하지 않았습니다.

## 2026-08-10 · 문의 대화의 깨진 과거 메시지 숨김

- 인코딩이 이미 유실된 `????`와 자리표시자만 남은 `○○○○` 메시지를 대화 내용처럼 표시하지 않도록 수정했습니다.
- API와 화면에서 같은 판별 규칙을 적용해 과거 알림을 합칠 때도 깨진 메시지가 다시 나타나지 않습니다.
- 정상 한글 메시지와 `김○○` 형태의 이름 마스킹은 그대로 표시됩니다.
- D1 데이터를 삭제하거나 스키마·바인딩을 변경하지 않았으며 Node 자동 테스트 **112/112**가 통과했습니다.

## 2026-08-09 · 광고 상품 3단계에서 2단계로 단순화

- `베이직 공고 / 추천 공고 / 집중 채용`을 `기본 공고 / 메인 추천 공고` 두 상품으로 정리했습니다.
- 메인·채용목록의 집중채용·플래티넘 표현을 추천공고로 통일하고, 광고센터의 2열 상품 비교를 더 넓고 읽기 쉽게 배치했습니다.
- 광고와 성격이 다른 후보 발굴·전담 컨설턴트·익명 인재풀 추천은 맞춤 헤드헌팅으로 분리했습니다.
- 기존 집중 채용 주문과 공고는 보존하되 추천 등급으로 표시하고, 폐지 상품의 신규 주문은 서버에서 차단합니다.
- Node 자동 테스트 **109/109** 통과. Sites project_id와 D1·R2 바인딩은 변경하지 않았습니다.

## 2026-08-09 · 공개 화면 사전 검수 표현 제거

- 메인 집중채용과 공고·헤드헌팅 상세의 확인·검증 배지를 제거했습니다.
- 광고센터의 담당자 확인 후 결제·게시 표현을 없애고 결제 완료 즉시 공개되는 흐름으로 통일했습니다.
- 등록 병원의 콘텐츠 책임과 게시 후 정책 위반 대응만 간결하게 안내합니다.

## 2026-08-09 · 회원 유형 자동 분류와 광고 결제 후 즉시 게시

- 회원가입 때 선택한 일반회원·병원회원 유형을 D1 역할로 고정하고, 로그인 때 서버가 역할을 자동 조회하도록 회귀 테스트를 추가했습니다.
- 병원 광고 주문의 관리자 사전 검수·승인·반려 단계를 제거했습니다. 결제 승인 성공과 동시에 공고가 공개됩니다.
- 이니시스 미설정 Sites 가상결제도 승인 API를 자동 호출해 성공 안내만 나오고 공고가 남지 않던 흐름을 바로잡았습니다.
- 과거 결제 완료 후 `draft`에 남은 병원 광고는 공개 운영 데이터나 병원 마이페이지 조회 시 자동 복구합니다.
- 관리자 공고 승인·반려 버튼, 공개 화면의 검수 문구, 홈페이지 하단 의사·병원·안내 링크 열을 제거했습니다.
- 회원 본인확인, 결제 금액·서명 검증, 신고·정책 위반 게시물 사후 숨김은 안전장치로 유지합니다.
- Node 자동 테스트 **106/106** 통과. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-08 · 전체 기능·로직 전수조사와 이력서 수정 복구

- 의료인 마이페이지 이력서 카드가 병원용 담당자 문의로 연결되던 오류를 수정해 저장된 이력서 수정 화면으로 이동합니다.
- `/resume` 수정 화면이 `GET /api/resumes`로 최신 D1 이력서와 상세·프로필 사진을 불러오도록 보완했습니다. `?new=1` 신규 작성은 빈 양식을 유지합니다.
- 비회원·의료인·병원·관리자 운영 API 권한 매트릭스, CSRF 차단, 병원 무료 공고 차단을 실제 Sites 요청으로 검증했습니다.
- 운영 R2에 PNG를 실제 업로드·조회하고 금지 SVG 차단을 확인했습니다.
- 인재 열람권 가상 주문·승인·권한 발급을 실제 청구 없이 검증했습니다.
- 관리자 콘솔·상담·CRM·백업 목록, 최근 일일 백업과 보존 정리 상태를 확인했습니다.
- Node 자동 테스트 **98/98**, 프로덕션 빌드, 생성 Worker 문법과 diff 검사가 통과했습니다.
- 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-08 · 이미지 입력 편의성과 회원 알림함
- 병원 로고·채용 배너·시설 사진과 의료인 프로필 사진을 파일 선택, 드래그앤드롭, 클립보드 `Ctrl+V` 붙여넣기로 등록할 수 있습니다.
- 지원 화면의 `새 이력서 작성`과 `내 이력서 관리`를 같은 위치의 선명한 버튼으로 재배치했습니다.
- 병원 유료광고에 지원하면 지원서, 병원 활동 기록, 읽지 않은 알림이 D1에 한 번에 저장되어 일부만 성공하는 상황을 막았습니다.
- 병원과 지원 의료인이 지원 상세 화면에서 메시지를 보내면 상대 회원의 마이페이지 알림함과 상단 종 아이콘에 표시됩니다.
- 헤드헌터가 상담 상태·답변을 변경한 경우에도 해당 회원에게 읽지 않은 알림이 생성됩니다.
- 알림 읽음·전체 읽음, 30초 자동 갱신, 마이페이지 강조 배너를 추가했습니다.
- 기존 Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.
- Node 자동 테스트 97건과 프로덕션 빌드가 통과했습니다.

## 2026-08-08 · 헤드헌터 문의와 병원 유료광고 지원 완전 분리
- 맞춤 헤드헌팅 상세 페이지의 문의 CTA를 해당 게시글 ID·제목·병원 정보와 함께 의사 헤드헌터 상담 경로로 연결했습니다.
- 병원 유료 채용광고의 지원서는 공고를 등록한 병원에만 전달하며 헤드헌터 상담함, Resend 이메일, Solapi 문자 알림에서 제외했습니다.
- 서버가 클라이언트 값을 신뢰하지 않고 `submissionChannel`을 `headhunt_board`, `paid_job_direct`, `general_headhunting`으로 판정해 D1에 기록합니다.
- 병원 마이페이지 문의·후보 화면에서 자신의 유료광고에 들어온 직접 지원과 선택 이력서를 확인할 수 있습니다.
- 유료광고 지원 화면에서는 헤드헌터 전화·이메일을 제거하고 병원 직접 전달임을 명시했습니다.
- Node 자동 테스트 93건과 프로덕션 빌드가 통과했습니다.

## 2026-08-08 · 맞춤 헤드헌팅 상세 독립 페이지 전환
- 초빙공고를 누르면 화면을 가리는 모달 대신 `/headhunting/posts/{공고ID}` 상세 페이지로 이동합니다.
- 목록 복귀, 새 탭 열기와 링크 공유가 가능한 고유 주소를 제공합니다.
- 초빙 조건, 상세 내용, 담당 컨설턴트, 전화 상담과 온라인 초빙 의뢰를 한 화면에 유지했습니다.
- 모바일에서는 상세 조건과 상담 버튼이 한 열로 정렬됩니다.

## 2026-08-08 · 마이페이지 활동 요약 위젯 연결
- 병원회원 위젯을 공고·문의·결제·추천 후보 상세 화면과 연결했습니다.
- 의료인 위젯을 이력서·상담·관심 공고·결제 상세 화면과 연결했습니다.
- 추천 후보 상세 목록과 키보드 포커스·모바일 터치 상태를 추가했습니다.

## 2026-08-08 · 채용공고 상세 독립 페이지 전환
- 홈과 채용정보 목록의 공고 카드를 누르면 모달 대신 `/jobs/{공고ID}` 상세 페이지로 이동합니다.
- 상세 공고마다 고유 URL이 생겨 뒤로가기, 새 탭 열기와 링크 공유가 자연스럽게 동작합니다.
- 기존 지원·문의, 관심공고 저장, 배너와 병원 상세정보 기능은 페이지에서도 그대로 유지됩니다.

## 2026-08-08 · 상세 모달 기관 아이콘 개선
- 가로형 채용 배너를 정사각형 로고 칸에 축소 표시하던 문제를 제거했습니다.
- 별도 병원 로고가 없으면 건물과 의료 심볼을 결합한 전용 기관 아이콘을 표시합니다.
- 배너와 다른 정식 병원 로고가 등록되어 있으면 기존 로고를 그대로 표시합니다.

## 2026-08-08 · 집중채용 공고 카드 가독성 개선
- 집중채용 6개 카드의 공고 제목, 지역·마감일, 진료과, 인증 정보와 상세보기 링크 글자를 확대했습니다.
- 확대된 글자에 맞춰 카드 최소 높이와 항목 간격을 조정하고 모바일에서는 과도하게 커지지 않도록 별도 크기를 적용했습니다.

## 2026-08-08 · 공고 상세 모달 배너 고도화
- 공고 상세 헤더를 정보 영역과 3:1 배너가 균형 있게 나뉘는 2열 구성으로 변경했습니다.
- 배너에 프리미엄 칩, 반투명 병원 정보 패널, 은은한 조명 효과와 깊이감 있는 그림자를 적용했습니다.
- 태블릿과 모바일에서는 배너가 정보 아래로 이동하며 화면 너비에 맞게 표시됩니다.

## 2026-08-08 · 선택형 샘플 채용 배너 6종
- 병원 이미지가 없어도 사용할 수 있도록 메디컬 블루, 웰니스 민트, 진단 네이비, 케어 라벤더, 리햅 코랄, 서지컬 티얼 배너를 추가했습니다.
- 모든 샘플은 1500×500px(3:1) JPEG이며, 병원명이나 가짜 로고를 이미지에 굽지 않고 화면에서 실제 공고 정보가 겹쳐 보이도록 구성했습니다.
- 병원 공고 결제 화면에서 직접 업로드 또는 샘플 선택 중 하나를 사용할 수 있고, 선택한 샘플 경로가 공고 데이터에 저장됩니다.
- 공고 상세 상단의 오른쪽 여백에 배너를 배치하고 작은 화면에서는 본문 너비로 내려오게 했습니다.
- OpenAI Sites Worker 패키지에 샘플 이미지 6종의 정적 응답 경로를 추가했습니다.
- Node 자동 테스트 85건과 프로덕션 빌드가 통과했습니다.

## 2026-08-08 · 집중채용 그리드 노출량 확대
- 집중채용 공고를 한 묶음에 데스크톱 6개, 태블릿 4개, 모바일 2개씩 노출하도록 확대했습니다.
- 기존 광고 등급·균형 정렬·자동 순환은 유지하며, 한 화면에서 보이는 공고 수를 두 배로 늘렸습니다.
- 공고가 한 묶음보다 많을 때만 다음 묶음으로 순환하므로 전체 페이지가 과도하게 길어지지 않습니다.

## 2026-08-08 · 병원 채용공고와 맞춤 헤드헌팅 게시판 분리
- 병원이 결제·등록한 공개 채용광고가 맞춤 헤드헌팅 게시판에도 함께 표시되던 데이터 혼합을 해소했습니다.
- 맞춤 헤드헌팅 관리 화면에서 작성한 콘텐츠에만 `payload.publicationChannel='headhunt_board'`를 저장합니다.
- `/headhunting`은 전용 채널 콘텐츠만 읽고, `/jobs` 및 의료인 채용 목록은 전용 채널 콘텐츠를 제외합니다.
- 기존 운영 공고 `124124 아무 의사나 초빙`은 D1 레코드를 변경하지 않고 병원 채용 영역에만 유지됩니다.
- 기존 D1 스키마 및 `.openai/hosting.json`의 D1·R2 바인딩은 변경하지 않았습니다.
- Node 자동 테스트 81건과 프로덕션 빌드가 통과했습니다.

## 2026-08-08 · 채용 공고 배너 실규격 적용 및 전수검사

- 채용 공고 카드용 배너 7종을 직접 제작해 `public/banners/`에 추가하고 모두 1500×500px(3:1), 8MB 이하 JPEG로 최적화했습니다.
- 프리미엄 공고 8개에 일반병원·척추/통증·건강검진·여성의원·내과/정밀진단·소아청소년과·웰니스 분야별 배너를 연결했습니다.
- 프리미엄 카드에서는 `cardBanner`/`banner`를 로고보다 우선하고, 이미지는 `object-fit: cover`로 3:1 영역 전체를 채우도록 변경했습니다.
- 병원명은 이미지에 굽지 않고 HTML 텍스트로 겹쳐 표시해 한글 가독성과 접근성을 유지했습니다.
- Sites Worker 패키지에 배너 7종의 정적 응답 경로와 장기 캐시 헤더를 추가했습니다.
- 자동 테스트에 모든 프리미엄 fixture의 배너 존재 여부, 1500×500 규격, 8MB 제한 검사를 추가했습니다.
- 데스크톱 1600px과 모바일 390px에서 3개 자동순환 묶음 전부를 검사해 3:1 비율, 이미지 로드, 텍스트 표시, 가로 넘침 없음 상태를 확인했습니다.
- Node 자동 테스트 80건과 프로덕션 빌드가 통과했습니다.
- 운영 D1 승인 공고 `124124 아무 의사나 초빙`에도 배너를 연결하고 OpenAI Sites v234로 배포했습니다.

## 2026-08-03 · 병원 광고 승인·프리미엄 노출 연동

- 병원 광고 결제가 `paid`여도 관리자 공고가 `draft`이면 공개되지 않는 2단계 운영 규칙을 운영 DB와 문서에 명확히 반영
- `집중 채용`을 `spotlight`, `추천 공고`를 `featured`로 공개 공고 payload에 자동 매핑해 구매 등급대로 프리미엄 영역에 배치
- 결제 metadata의 노출 시작일·종료일을 공고 payload에 동기화해 서버·클라이언트 만료 필터가 동일한 값을 사용하도록 수정
- 기존 결제 공고도 관리자 콘솔 조회 시 등급·노출기간을 보정하도록 호환 처리
- 운영 공고 `124124 아무 의사나 초빙`을 `published`로 게시하고 한글 제목·병원 형태·지역·진료과를 복원한 뒤 프리미엄 첫 묶음 노출 확인
- 병원회원의 `/api/member-center`, `/api/payment-orders`, `/api/saved-jobs` HTTP 200과 최신 집중 채용 주문 `paid` 확인
- Node 자동 테스트 79건과 프로덕션 빌드 통과, GitHub 커밋 `ae95268`, OpenAI Sites v233 배포
- `.openai/hosting.json`의 기존 프로젝트 ID와 D1 `DB`·R2 `BACKUPS` 바인딩 유지

## 2026-08-02 · 제출 성공·운영 데이터 전수조사

- 계정 찾기·비밀번호 재설정 화면이 서버 저장 없이 성공을 표시하던 문제를 수정하고 `POST /api/account-recovery`와 D1 `account_recovery_requests`를 추가
- 계정 도움 요청은 실제 D1 접수번호가 반환될 때만 완료 화면을 표시하며 관리자 통합 모니터링에서 확인 가능
- 선택한 이력서 문서가 실제 업로드되지 않고 파일명만 남던 UI를 제거해 사용자에게 파일 보관을 오인시키지 않도록 정리
- 운영 D1에서 결제 완료 광고 2건과 관리자 공고 2건의 연결, 결제 트랜잭션, 상담·CRM 참조 상태를 대조해 누락 0건 확인
- 의료인·병원·관리자 테스트 계정의 계정·마이페이지·이력서·관리자 권한 응답 확인
- Node 자동 테스트 78건과 프로덕션 빌드 통과, GitHub 커밋 `d5a61ec`, OpenAI Sites v230 배포
- `.openai/hosting.json`의 기존 프로젝트 ID와 D1 `DB`·R2 `BACKUPS` 바인딩 유지

## 2026-08-02 · 인증 전환·권한 로딩·검색 노출 안정화

- 테스트 역할 전환 시 기존 비밀번호 해시를 다시 계산하던 병목을 제거하고, 새 D1 세션을 헤더와 각 페이지가 즉시 공유하도록 전체 페이지 이동으로 통일
- 광고 신청·인재 열람 화면에서 회원 권한 확인 중 로그인 게이트가 잠깐 잘못 표시되던 현상을 전용 로딩 상태로 분리
- `TEST_ACCOUNT_SWITCH_ENABLED=false` 하나로 정식 출시 때 테스트 계정 UI와 API를 함께 비활성화하도록 운영 안전장치 추가
- 메인 헤더가 없는 관리자 전용 콘솔에도 세션을 실제 폐기하는 로그아웃 버튼 추가
- `robots.txt`, `sitemap.xml`, canonical URL, 웹 앱 매니페스트를 추가하고 개인정보·관리자·API 경로의 검색 색인을 차단
- Node 자동 테스트 74건과 프로덕션 빌드·생성 Worker 문법 검사 통과
- Sites v227 배포 후 새 브라우저 세션에서 병원·의료인·관리자 전환, 역할별 화면, 관리자 로그아웃을 확인하고 GitHub 작업 브랜치와 동기화

## 2026-08-02 · 이력서 열람권 가상 결제 복구

- 열람권 도입 전에 생성된 기존 D1의 결제 상품 유형 제약 때문에 `talent_search` 주문이 HTTP 500으로 실패하던 문제를 기존 DB 재생성 없이 호환 처리
- OpenAI Sites 결제 화면에 실제 청구 없는 가상 결제 안내와 전용 활성화 버튼 추가
- 가상 승인 API가 실패해도 성공 화면으로 넘어가던 오류 처리 보완
- 주문 생성 → 가상 승인 → D1 결제 기록 → 단건 열람권 발급 → 후보 상세 열람 흐름을 운영 배포본에서 검증하도록 회귀 테스트 추가

## 2026-07-30 · 공고 모달·프리미엄 순환·Sites 업로드 정상화

- 메인과 채용정보의 공고 카드가 경로를 이동하지 않고 상세 모달을 열도록 동작 통일
- 프리미엄 공고 자동 순환 컴포넌트를 실제 목록에 연결하고 깨진 한글·접근성 라벨 수정
- OpenAI Sites에서 전용 `UPLOADS`가 없을 때 기존 `BACKUPS` R2를 병원 이미지 저장소로 사용하는 폴백 추가
- 의료인 전용 상세 경로 직접 접근에 로그인 게이트 적용
- 모바일 메뉴 버튼 최소 터치 영역 44×44px 보장
- Node 테스트 70건, 프로덕션 빌드, Edge 화면 검증, 공개본 HTTP 응답 확인
- Sites v224와 GitHub `main`에 기능 커밋 `c73b28d` 반영


## 2026-07-29 · 다른 PC 회원가입·로그인 안정화

- **테스트 계정 전환 서버화**: 상단과 로그인 화면의 관리자·일반회원·병원회원 전환을 브라우저 내 고정 비밀번호 로그인/자동가입 방식에서 서버 전용 전환 API로 변경. 테스트 계정이 없거나 잠겼거나 이전 PC 쿠키가 남은 상태도 서버가 계정·권한·세션을 복구해 즉시 전환
- **다중 PC 회원 세션 검증**: 신규 일반회원·병원회원이 PC 1에서 가입한 뒤 PC 2에서 같은 계정으로 로그인되고, PC 2 로그아웃 후에도 PC 1 세션은 유지되는 흐름을 운영 시나리오로 점검

## 2026-07-26 · 관리자 콘솔 UX · 주소검색 · 인재 표시 · 로그인 안정화

- **다른 PC 테스트 계정 전환 안정화**: 브라우저에 이전 계정 쿠키가 남아 있거나 새 쿠키 반영이 지연돼도, 일반회원·병원회원 전환 직후 발급된 탭 세션을 우선 확인하도록 인증 순서를 수정
- **관리 버튼 정렬 개선**: 관리자 콘텐츠 통합관리 표의 관리 열을 확대하고 버튼 글자는 깨지지 않게 유지하면서, 검수 버튼이 많은 행은 3개씩 정돈된 두 줄로 배치. 공개 초빙 게시판의 관리자 수정·삭제 버튼은 제목 아래로 밀리지 않도록 우측 전용 ‘관리’ 열로 이동하고 모바일에서는 별도 액션 행으로 정렬
- **회원가입 안내 배너 정리·주소검색 모달 확대**: 병원·의료인 가입 및 로그인 화면의 반복적인 보안·서류·마케팅 안내 박스를 제거해 입력 흐름을 단순화. 주소검색 모달을 최대 720×720px로 확대하고 블러 오버레이, 정돈된 헤더·닫기 버튼, 모바일 전체 높이, ESC 닫기와 배경 스크롤 잠금을 적용
- **주소 검색 CSP 후속 수정**: 카카오 우편번호 운영 스크립트가 현재 생성하는 iframe 주소가 `postcode.map.kakao.com`인데 기존 CSP에는 과거 `postcode.map.daum.net`만 허용되어 인라인 모달 안에 '콘텐츠 차단' 화면이 표시됐다. 현재 도메인을 `frame-src`에 추가하고 두 주소를 회귀 테스트로 고정
- **주소 검색 '콘텐츠 차단' 수정**: 병원 가입 주소 검색이 카카오 팝업 창(`.open()`)으로 떠서 그 창(about:blank)이 CSP를 물려받아 '콘텐츠가 차단되었습니다'로 막혔다. `.embed()`(페이지 안 인라인 iframe 모달)로 변경해 `frame-src` 허용만으로 정상 동작
- **인재 프로필 '샘플/예시' 표시 제거**: 데모 인재 16명이 `sample:true`라 상세 모달이 '샘플 프로필(예시)·열람권 결제 대상 아님'으로 뜨고 구매 버튼이 없었다. `sample` 제거 → '이력서 열람권 구매' 버튼 노출(이름은 마스킹 유지, 가짜 실명 안 만듦)
- **관리자 대시보드 위젯 클릭 이동**: '전체 현황'의 위젯(전체회원·결제요청·누적결제·상담접수·진행채용·운영콘텐츠)이 정적 카드라 눌러도 반응 없던 것을 버튼으로 바꿔 해당 관리 섹션으로 바로 연결
- **관리자 콘솔 빈 페이지 방지**: `/api/admin-console`가 403(세션 만료·쿠키 타이밍)일 때 콘솔이 빈 채로 렌더되던 것을 4회 재시도 후에도 실패하면 '관리자 권한이 필요합니다 + 다시 시도·로그인' 안내 화면 표시
- **관리자 로그인 라우팅**: QA 프리뷰 폐지로 헤더가 로그인해도 '로그인'만 뜨던 것을 실제 세션 기준으로 수정(관리자→콘솔, 회원→마이페이지). 마이페이지 진입 관리자는 '운영 콘솔 열기' 안내
- **테스트 로그인 타이밍**: 다른(느린) PC에서 테스트 버튼 로그인 직후 쿠키가 붙기 전 마이페이지가 401→'회원 정보 불러오지 못했습니다'가 뜨던 것을, 로그인 후 세션 확인 + 마이페이지 재시도(각 4회)로 방어

## 2026-07-26 · 쿠키 차단 로그인 보완 · 로그아웃 버튼

- 다른 PC의 Edge에서 로그인 API는 성공하지만 사이트 쿠키가 저장되지 않아 마이페이지가 401이 되던 문제를 탭 단위 보조 세션으로 보완
- 보조 세션은 같은 메디헬퍼스 출처의 `/api/` 요청에만 전달하고 탭 종료·로그아웃 시 삭제
- 로그인한 회원에게 데스크톱 헤더, 모바일 메뉴, 마이페이지에 로그아웃 버튼 추가
- 로그아웃 시 D1 세션, 보안 쿠키, 탭 보조 세션을 함께 폐기

## 2026-07-26 · 상단 테스트 역할 전환 · 마이페이지 CTA 복원

- 헤더의 마이페이지/관리자 버튼을 이전처럼 크고 선명한 파란 CTA로 복원
- 테스트 기간 동안 일반회원·관리자·병원회원 계정을 상단에서 바로 전환하는 버튼 추가
- 데스크톱은 계정 CTA 옆의 compact switcher, 모바일은 펼침 메뉴 안의 큰 터치 버튼으로 제공
- Sites `ADMIN_EMAILS`에 테스트 관리자 계정을 포함해 관리자 전환 시 실제 운영 콘솔로 이동하도록 구성

## 2026-07-26 · 병원 광고 신청과 관리자 공고 검수 연동

- 병원 광고 신청이 `payment_orders`에만 저장되고 관리자 “공고·콘텐츠”에는 나타나지 않던 단절을 수정
- 신규 신청은 결제 원장과 검수용 `admin_content_records(status='draft')`를 같은 D1 배치로 생성
- 기존 결제 원장에만 남은 광고 신청은 관리자 콘솔 최초 조회 시 공고 목록으로 1회 복구
- 공개 공고 payload에는 채용조건만 복사하고 담당자 연락처·이메일·사업자번호 같은 주문 개인정보는 제외
- 광고 접수 완료 화면의 잘못된 `/qa-preview` 링크를 실제 `/mypage`로 수정

## 2026-07-25 · 회원가입 간소화 · 주소검색 · 테스트 계정 · QA 프리뷰 폐지

- **회원가입 버튼 명칭**: 메인·네비의 '병원 회원가입' → '회원가입'(역할 선택 페이지로)
- **병원 가입폼 정리**: 기관 유형·병원 대표 전화·우편번호 필드 제거, **사업자등록번호 필수화**(숫자 10자리 검증)
- **병원 주소 실검색**: 다음(카카오) 우편번호 서비스 연동('주소 검색' 버튼 → 팝업 → 실주소 입력). CSP에 `t1.daumcdn.net`·`ssl.daumcdn.net`(script)·`postcode.map.daum.net`(frame) 허용
- **가입 버튼 명칭 정리**: 헷갈리던 '계속해서 계정 인증' → '회원가입 신청/완료'. '계정 인증' 표현들도 로그인/회원가입 문구로 교체
- **화면 미리보기(QA 프리뷰) 폐지**: 페이지 이동을 막던 원인. `qaActive`를 항상 false로 고정, 헤더/모바일 링크·리본 제거, `/qa-preview`→홈 정규화. 모든 화면이 실제 세션 기준으로만 동작
- **테스트 계정 3개**: 로그인 화면에 원클릭 버튼(관리자·병원 회원·의료인 회원). 없으면 자동 가입 후 로그인. 관리자는 `admin@medihelpers.co.kr`(`ADMIN_EMAILS` 기반). `wrangler.toml`에 기본값 추가, devApiMock도 admin 이메일 인식. ⚠️ 정식 오픈 전 제거
- **3역할 기능 E2E 검증**(로컬 브라우저): 병원 광고결제·열람권 팩·의료인 이력서·관리자 공고게시까지 실제 흐름 확인, 콘솔 에러 0

## 2026-07-25 · DB 백업·보존·탈퇴 데이터 보호

- 기존 Sites D1 바인딩을 유지하면서 비공개 R2 `BACKUPS` 저장소 추가
- 한국 날짜 기준 하루 첫 요청에서 보존기간 정리 후 전체 D1 일일 백업 생성
- R2 백업 35일 순환 보관, SHA-256 체크섬, D1 실행 이력 추가
- 관리자 콘솔에 백업 생성·목록·다운로드·최근 보존 정리 현황 추가
- 결제 이력이 있는 탈퇴 회원의 이력서·관심항목·활동·동의·열람권이 남던 문제 수정
- 탈퇴 식별값 30일, 종료 상담·감사 로그 3년, 거래 기록 5년 자동 정리
- `docs/DATA_PROTECTION.md`에 RPO 24시간·RTO 4시간과 복구 승인 절차 문서화

## 2026-07-25 · 전수조사 버그 9건 (크래시·개인정보 유출·결제·게이트)

- **미등록 API의 거짓 성공 응답 수정**: 존재하지 않는 `/api/*` 요청이 SPA 홈페이지 HTML과 HTTP 200을 반환해 연동 실패를 성공처럼 보이게 하던 것을 JSON 404로 분리
- **병원 사진 클릭 크래시 수정**: `PhotoLightbox`의 미정의 `embedded` 참조 제거(사진 클릭 시 ReferenceError로 상세 화면이 깨지던 것)
- **비공개 이력서 유출 차단**: `talentDetailApi`가 visibility 검사 없이 조회해, 기본값(private)인 미공개 의사의 실명·연락처까지 열람권으로 열리던 것을 `public`/`proposal`만 열람 가능하게 제한. 팩 크레딧도 비공개 이력서엔 미소모
- **상담 역할검사 fail-closed**: `ACCOUNT_HASH_SECRET`이 약하면 역할검사를 건너뛰던 것을 503으로 차단(아무나 병원 상담 접수→CRM 케이스 생성 가능하던 우회로)
- **테스트모드 결제 멱등성**: testMode 승인 UPDATE에 `status<>'paid'` 가드 추가 + `talent_credit_pools.order_id` UNIQUE 인덱스(동시 승인 시 거래기록·팩 크레딧 이중 생성 방지)
- **광고 등록 게이트 수정**: `AdvertisePage`·`AdvertiseApplyPage`·`JobsPage`가 `qa.active`만 봐서 진짜 로그인 병원이 광고 결제까지 도달 못하던 것을 `useAuthGate`로 수정
- **후보 동의 전 노출 차단**: `recommendedCandidates`를 `consent_status='granted'`만 반환
- **관리자 회원수정 고아행 방지**: `member_update`가 계정 존재 확인 없이 upsert하던 것을 404 검증 추가
- **최상위 에러 핸들러**: 핸들러 밖 DB 예외가 D1 오류 담긴 HTML 500으로 새던 것을 try/catch로 감싸 API는 JSON·HTML은 일반 텍스트로 통일


## 2026-07-22 · 의사 멤버십 폐지(수익모델 단순화) · 배포본 글꼴 깨짐 수정 · 구직글 등록 버튼

- **의사 대상 유료 멤버십 전면 폐지**: 유료 상품을 **병원 채용광고 + 병원 인재 열람권** 두 가지로 단순화. 의사·의료인은 전부 무료로 이용한다
  - 제거 범위: `/membership` 라우트·`MembershipPage`·`MembershipCheckout`·`ValueCalculator`, `data.js`의 `membershipPlans`, 서버 상품 카탈로그의 `doctor-single`(커리어 체크 19,000원)·`doctor-pass`(커리어 컨시어지 39,000원), 마이페이지 멤버십 지표·탭 문구, 약관 문구, QA 프리셋
  - `/membership`은 `ROUTE_ALIASES`로 `/jobs` 정규화(기존 링크·북마크 보호)
  - 마이페이지 의사 지표의 '멤버십' 자리는 '관심 공고' 건수로 대체
  - QA 프리셋 '멤버십 이용 회원' → **'열람권 구매 병원'**으로 교체(이제 검증할 유료 상태가 이것뿐)
- **열람권을 병원 전용으로 강제**: `talent_search` 상품에 역할 검사가 없어 **의사도 결제할 수 있었다**. 열람 권한은 병원에만 부여되므로 돈만 내고 못 쓰는 상태였음(과금 위생 문제) → 병원 회원이 아니면 403
- **🔴 배포본 글꼴이 전부 깨져 있던 문제 수정**: 2026-07-20 보안 작업에서 넣은 CSP의 `style-src`에 구글폰트가 빠져 있어 Manrope·Noto Sans KR 스타일시트가 차단됐다. 사이트 전체가 대체 글꼴로 렌더되던 상태
  - 배포본 브라우저 점검에서 발견(로드된 웹폰트 **0개** 확인). `style-src`에 `https://fonts.googleapis.com`, `font-src`에 `https://fonts.gstatic.com` 허용 추가
  - ⚠️ CSP는 외부 리소스를 조용히 막는다 — 서버 로그·자동 테스트에 흔적이 남지 않아 **배포본을 브라우저로 직접 봐야만** 발견된다
- **구직 게시판에 '구직글 등록' 버튼 추가**(의료인 구인구직): 기존에는 페이지 상단 히어로에만 등록 경로가 있어 게시판까지 스크롤한 뒤 다시 올라가야 했다. 역할별 분기 — 의료인 회원=`/resume`, 병원 회원=숨김(정책상 구직글은 의료인 본인만), 비로그인=`/signup/doctor`
  - 작업 중 `qa.info.capabilities`로 역할을 판단해 병원 계정에서도 버튼이 보이던 것을 브라우저 검증에서 발견 → 실제 세션을 보는 `useAuthGate`로 교체
- **배포본 회원가입·로그인 실동작 점검 — 정상 확인**: 실제 브라우저로 가입폼 입력→필수값 검증→제출→"가입이 완료되었습니다"→로그아웃→재로그인→마이페이지 진입까지 전 구간 확인(테스트 계정은 삭제 완료)

## 2026-07-22 · 히어로 영상 노출 수정 · 하단 도크 반응 개선 · GitHub Pages 테스트 미러 공개

- **히어로 영상이 일부 PC에서 아예 안 보이던 문제 수정**: `styles.css`의 `@media(prefers-reduced-motion:reduce)`가 `.home-video-hero-bg{display:none}`으로 영상을 완전히 숨기고 그라디언트로 대체하고 있었음. OS '동작 줄이기(애니메이션 효과 끄기)'를 켠 PC·모바일은 영상이 렌더 자체가 안 됐다. 해당 숨김 규칙을 제거하고 그라디언트는 **영상 로드 전·실패 시 폴백 배경으로만** 유지
  - 코드베이스의 다른 reduce-motion 규칙은 모두 `html:not(.force-motion)` 예외를 두는데 **영상 규칙만 예외 없이 무조건 숨겨** 일관성도 어긋나 있었음
  - 검증: `reducedMotion=reduce` 강제 에뮬레이션 + `performance-lite` 적용 상태에서 `display:block`·`visibility:visible`·`paused:false`·`readyState:4`(1920×1080) 확인. 로컬·GitHub Pages 라이브 양쪽 통과
  - ⚠️ reduce-motion은 본래 어지럼·멀미 방지용 접근성 설정이다. 무음·배경 재생이라 위험은 낮지만 **의도적으로 접근성 기본동작을 덮은 결정**임을 기록해 둔다
- **하단 '병원 채용 바로가기' 도크(SmartAdDock) 등장 지연 단축**: 첫 등장 2200ms → 700ms, 스크롤 후 재등장 1450ms → 400ms. 스크롤 중 숨겼다가 멈추면 다시 뜨는 기존 동작은 유지
  - 체감이 특히 길었던 이유는 **스크롤 이벤트마다 `setVisible(false)` 후 타이머를 재시작**해, 스크롤을 멈춘 뒤에야 1.45초를 다시 기다렸기 때문
  - 실측: 첫 등장 1125ms(페이지 로드 포함), 스크롤 후 재등장 349ms
- **GitHub Pages 미러를 최신으로 배포**: `main`이 2026-07-16(`5e16882`)에 멈춰 있어 미러에 영상 자체가 없었음(mp4 404). 작업 브랜치를 fast-forward 병합해 **209커밋·76파일(+13,996/-1,312)** 반영
  - 되돌림 지점 태그: `backup-main-before-video-deploy`(= `5e16882`)
  - ⚠️ 이 병합으로 **이니시스 결제 연동 코드가 공개 미러에 함께 게시**됐다. 환경변수 미설정이라 실결제는 동작하지 않지만, 원래 계획은 "Sites 약관의 PCI 금지 → Cloudflare 이전 후 결제 연결" 순서였음
- **테스트 미러 검색엔진 색인 차단**: GitHub Pages는 정식 오픈 전 테스트용인데 `robots.txt`·`noindex`가 없어 색인이 열려 있었음. 미완성 화면 노출과 정식 도메인(medihelpers.co.kr) 대비 중복 콘텐츠로 검색 순위가 갈릴 위험
  - ⚠️ `public/robots.txt`에 넣으면 **정식 배포까지 noindex가 따라가 진짜 사이트가 검색에서 사라진다.** 그래서 소스가 아니라 `.github/workflows/pages.yml`의 Pages 빌드 단계에서만 주입하도록 했다
  - 공개 번들 개인정보 점검 동시 실시: 전화번호는 전부 샘플(`010-0000-0000` 등)과 업무용 대표번호(`010-2435-5463`)뿐, 주민번호 패턴 0건 — '후보자 개인정보 번들 유출 차단' 조치가 정상 동작 중임을 확인
- ⚠️ **OpenAI Sites 공개본(`medihelpers-career.junnyai.chatgpt.site`)은 이번 변경이 반영되지 않았다.** 해당 배포는 GitHub이 아닌 ChatGPT Sites 인프라라 별도 배포가 필요하다

## 2026-07-20 · 인증 보안 강화(H2·H3·H4·L1)
- **관리자 세션 단축(H3)**: 관리자 세션·쿠키 수명을 12시간으로 단축(일반 회원은 7일 유지). 관리자 계정이 탈취되면 전 회원 개인정보·결제 상태에 접근할 수 있어 노출 시간을 줄임. 관리자 판별은 `ADMIN_EMAILS` 기준 — ⚠️ 값이 비거나 오타면 오류 없이 7일로 되돌아가므로 운영 배포 전 확인 필요
- **역할 변경 시 세션 회전(H4)**: 계정 역할이 바뀌면 기존 세션을 전부 무효화(`DELETE FROM auth_sessions`). 의사 계정이 병원으로 전환될 때 예전 세션이 새 권한을 그대로 얻던 문제 차단
- **계정 열거 차단(H2)**: 잠긴 계정에만 429를 돌려주던 것을 제거 — 아무 비밀번호나 5번 넣어보고 상태 코드만으로 가입 여부를 알 수 있었음. 이제 잠금 여부와 무관하게 해시를 계산한 뒤 동일한 401·동일 문구 반환(응답 시간 차이도 축소). 잠금 중 실패 횟수를 올리지 않아 잠금 무한 연장도 방지
- **보안 헤더(L1)**: HTML 응답에 CSP, HSTS, `X-Content-Type-Options`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Permissions-Policy` 적용
  - ⚠️ CSP는 이니시스 결제창(`stdpay.inicis.com`, `stgstdpay.inicis.com`)을 `script-src`/`connect-src`/`frame-src`/`form-action`에 허용 중. **이 도메인을 빼면 결제창이 조용히 안 뜬다**(브라우저 콘솔에만 로그, 서버 로그·테스트엔 흔적 없음)

## 2026-07-20 · 보안 강화 · Cloudflare 이전 준비
- **결제 승인 API 치명적 취약점 4건 수정**(실결제 전 필수): 승인 검증 필수화(fail closed — 기존엔 주문번호만 알면 공짜 결제 가능), 멱등성(중복 승인·열람권 중복 생성 차단), authUrl SSRF 화이트리스트(`*.inicis.com` https만 — 기존엔 공격자 서버로 signKey 파생값 유출 가능), 금액 검증 항상 실행, 승인 MOID 일치 확인, 열람권 기록 실패 로깅
- **병원 무료 공고 API 차단**: SPA 경로만 막혀 있어 `job_create` API 직접 호출로 무료 공고 생성이 가능하던 우회로 제거(403)
- **구직자 실명 보호(H1)**: `canRevealTalentIdentity`가 '병원 회원이면 공개'였던 것을 **열람권 확인 필수**로 변경. 목록은 항상 마스킹, 실명은 서버가 검증하는 상세에서만 — 열람권 수익모델 보호
- **Cloudflare Workers 이전 준비**: `npm run build:cf` 추가. 정적 파일을 Static Assets로 분리해 **Worker 번들 12MB → 1.4MB**(무료 3MB 제한 통과), `wrangler.toml` 자동 생성, `docs/CLOUDFLARE_MIGRATION.md` 이전 가이드 작성
  - 배경: OpenAI Sites 약관이 결제·PCI 처리를 금지 → 이니시스 결제를 붙이려면 이전 필요
- **히어로 영상 압축**: 2.9MB → 960KB (1280×720, 24fps, 무음)

## 2026-07-20 · 결제 흐름 보강 · 전수조사 수정
- **PG 리턴 결과 페이지 처리**: 이니시스가 결제창 인증 후 브라우저를 `returnUrl`로 form POST 이동시키는데 서버가 JSON을 반환해 **사용자 화면에 JSON 원문이 노출될 문제**를 수정. form POST 리턴은 `/mypage?payment=paid|failed`로 303 리다이렉트(성공·실패·승인실패·금액불일치·통신실패 전 경로)
- **결제 결과 배너**: 마이페이지 상단에 성공(초록)/실패(빨강) 안내 배너 추가(`.payment-result-notice`), 주문번호·사유 표시
- **중복 결제 방지**: 이니시스 결제창이 열린 동안 제출 버튼 잠금 유지(`paymentWindowOpened` 플래그)
- **`.form-error` CSS 정의**(전수조사 발견): 결제·회원탈퇴 실패 메시지가 본문 글씨처럼 보여 실패를 인지하지 못하던 문제 수정
- **관리자 공고 안내 정정**: 의료인 공고가 실제로는 헤드헌팅 '초빙 정보란'에 노출되는데 `/medical-staff`로 안내하던 것 정정
- **이니시스 실무 가이드 신설**: `docs/INICIS_SETUP_GUIDE.md`(키 발급→도메인 등록→환경변수 3개→테스트→문제 대응표)
- 전수조사 결과: 깨진 이미지·죽은 링크·z-index 충돌·모바일 가로넘침 0건

## 2026-07-20 · 이니시스 결제창 연동 · 게시판 디자인 정리
- **이니시스 웹표준결제 클라이언트 연동 완료**: `src/inicisPay.js` 신설(stdpay 스크립트 1회 로드 + 서명 파라미터 숨김폼 → `INIStdPay.pay()`). 멤버십·광고신청·인재 열람권 3개 결제 지점 모두 연결. `inicis.configured=true`면 실제 결제창, false면 기존 테스트(가상) 승인 유지 → 키 미설정 환경 회귀 없음. 실결제 전환은 `INICIS_MID`/`INICIS_SIGN_KEY`/`SITE_ORIGIN` 환경변수 설정만으로 가능(코드 수정 불필요). ⚠️ 실결제 검증은 이니시스에 등록된 배포 도메인에서만 가능(localhost 불가)
- **의사 초빙 정보란**: 게시판(테이블) 형태 유지하되 폭 1400px·글자 확대, 상단에 pill 검색바 + 지역·진료과 칩 필터 + 결과 카운트 추가
- **톤앤매너 통일**: 진한 남색(#102441) 제거 → 사이트 토큰(--blue/--soft/--line) 기반 밝은 톤. 게시판 헤더·검색·모달 CTA 일괄 정리
- **광고 카드 색 절제**: 금색 과다(테두리+로고배경+버튼) → **상단 3px 골드 라인만** 프리미엄 표식, 카드 테두리는 중립, 버튼·로고영역은 사이트 블루/화이트
- 카드 메타 세로 줄바꿈 깨짐 수정(white-space/flex 보정)

## 2026-07-20 · 의료인 구인구직 개편 · 병원 유료화 강제 · 버그수정
- **구직 게시판을 /medical-staff로 이동**: 헤드헌팅 페이지에서 빼고(헤드헌팅=의사 초빙 정보란 전용), /medical-staff에 배치. 채용공고와 동일한 카드형(직군 배지+열람권 잠금)으로 재디자인. 진료과·지역 필터 드롭다운 + 검색 + 결과 카운트 추가
- **/medical-staff = 구직 전용**: 위쪽 "의료인 채용정보"(병원 채용공고 목록) 제거 → 구직 의료인 게시판만. 메뉴명 "의료인 채용" → "의료인 구인구직"(nav·footer·QA)
- **병원 무료 공고 완전 차단(정책 강제)**: /advertise "무료 채용공고 등록"(/advertise/post) 폐지 → ROUTE_ALIASES로 /advertise 정규화, 히어로 문구를 "광고 상품 결제로 게시"로 변경, 무료 등록 버튼 제거. 병원 채용공고는 광고 상품(베이직·추천·집중) 결제로만
- **버그 수정**: (P0) 열람권 결제 후 /medical-staff?open=코드 자동열림 유실 → JobSeekerBoard에 route+openCode useEffect 이식. (P1) 초빙 모달 전화 tel:0102435463(오타) → tel:01024355463
- 게시판/모달 폭 확대(게시판 1320px, 초빙모달 1040px, 인재모달 1160px)
- flow-check(서브에이전트 2팀 정적분석 + Playwright E2E) 통과, 콘솔 에러 0, 빌드·테스트 49/49

## 2026-07-20 · 초빙 정보란 · 구직 게시판 · 열람권 · 병원 유료화 정책
- **의사 초빙 정보란**(헤드헌팅 페이지): 예전 메디헬퍼스 게시판 형식(번호/제목/작성자/조회/등록일) 복원. 관리자가 /admin/post로 올린 공고 + 예시 초빙글 노출. 상세는 넓고 고급스러운 모달(1040px)로 아빠 양식([병원 소개][병원 형태][근무 지역][초빙 유형][초빙 시기][보수·대우][경력][담당 컨설턴트]) 표시. 연락처 010-2435-5463
- **구직 게시판**(헤드헌팅 페이지): 의사·의료인이 직접 등록한 구직 이력서를 게시판 형식으로 노출(이름 블라인드 김○○). 병원 회원은 **열람권 결제 후** 이름·연락처·상세 열람(기존 TalentDetailModal·talent-unlock 재사용). 필터 탭(전체/의사/간호·의료인)
- **정책: 병원 채용 = 유료 광고만**. 병원의 무료 공고 직접 게시 경로는 없음(공고 게시는 /advertise 광고 결제로만). 헤드헌터 채용 의뢰(/request/hiring 상담)는 유지. 홈 QuickAccess의 병원 카드를 '무료 등록' → '헤드헌터 상담·의사 초빙 의뢰'로 문구 정정
- 병원 초빙 의뢰서(/request/hiring)·의사 간편 이력서(/request/job-seeker) 서식은 예전 양식대로 이미 구비됨(전 필드 + 010-2435-5463 확인)
- Playwright 검증(초빙 게시판·모달·구직 게시판·열람권 흐름·서식 2종) 통과, 콘솔 에러 0건, 빌드·테스트 49/49 통과

## 2026-07-20 · 관리자 전용 공고 게시판(/admin/post)
- 헤드헌터(관리자·아빠)가 의사·의료인 채용공고를 직접 올리는 전용 페이지 신설. 관리자 콘솔의 복잡한 통합 관리 대신 "공고 올리기 + 내가 올린 공고 관리(수정·숨김·삭제)"만 담은 간편 화면
- 기존 `/api/admin-console`의 `content_create`/`content_update`/`content_delete`를 그대로 사용 → 게시 즉시 `/jobs`(의사)·`/medical-staff`(의료인) 목록에 노출. 공고를 본 병원·의사가 상담 신청 시 관리자 상담함·CRM과 알림(Resend 이메일·Solapi 문자, 환경변수 설정 시)으로 접수
- 관리자 대시보드 '빠른 관리'에 "공고 올리기" 강조 바로가기 추가
- 로컬 devApiMock에 admin-console content CRUD + `/api/site-operations`를 추가해 로컬에서도 올린 공고가 실제 목록·채용 페이지에 노출되도록 개선(배포 서버와 동일 동작)
- 헤드헌팅 히어로의 신뢰 지표 스트립(2017/1:1/0원/비공개) 제거(요청)
- Playwright 검증: 공고 작성→게시→목록 표시→/jobs 실제 노출까지 확인, 콘솔 에러 0건, 빌드·테스트 49/49 통과

## 2026-07-19 · 의료인 채용 통합 · 헤드헌팅 리디자인
- '일반 회원' 표기를 '의료인 회원'으로 전면 통일(내부 role 키 `doctor` 유지)
- 회원가입 휴대폰 본인인증 절차 제거 — 휴대폰은 연락용 일반 입력으로 전환
- 이력서를 초간편 모드(직군 자유입력·불필요 항목 제거·의료인 위주)로 재설계
- 매칭 리포트 페이지·라우트·링크·QA 프리셋 완전 삭제
- 인재정보(/talent)를 의료인 채용(/medical-staff)으로 통합: 채용공고 + '구직 중인 의료인' 인재(열람권 상세열람)를 한 페이지에서 제공, `/talent`→`/medical-staff` 리다이렉트, 상단 메뉴에서 인재정보 제거
- 맞춤 헤드헌팅(/headhunting) 고급 리디자인: 신뢰 지표 스트립, 4단계 진행 절차, 비공개 원칙·전문 헤드헌터·상담 전화 신뢰 밴드 추가
- **라우팅 버그 수정(flow-check)**: `/talent`·`/professions` 옛 경로 진입 시 빈 화면이 뜨던 문제 해결. 원인은 `Redirect` 컴포넌트의 effect+popstate 이동이 무거운 대상 페이지에서 일으킨 리렌더 경쟁. App 상단 `ROUTE_ALIASES` 테이블로 렌더 시점 동기 정규화(URL·path·route 일치, 쿼리스트링·`open=` 자동열림 보존)하도록 바꾸고 `Redirect` 컴포넌트와 도달 불가 분기를 제거
- 빌드 통과 및 테스트 49개 전부 통과 확인. Playwright 엔드투엔드 검증(리다이렉트·열람권 모달·이력서·회원가입) + Codex 대조 검증 통과

## 2026-07-19 · 메디헬퍼스 자체 로그인
- OpenAI/ChatGPT 로그인 링크를 제거하고 자체 이메일·비밀번호 가입·로그인·로그아웃을 추가
- D1에 Worker 호환 PBKDF2-SHA-256(100,000회) 비밀번호 해시와 해시된 세션 토큰을 저장하고, 7일 보안 쿠키와 로그인 실패 잠금을 적용
- 기존 계정·역할·프로필·동의 데이터와 `.openai/hosting.json`의 프로젝트/D1 바인딩 유지
- Sites 버전 191로 운영 배포하고 `/api/account` 200, 비로그인 `/api/resumes` 401, 존재하지 않는 계정 로그인 401을 확인

## 2026-07-17 · 인재 이름 권한별 표시
- 사용자 화면에서 내부 후보 코드 대신 사람 이름을 표시하고, 비회원·일반 의료인에게는 `김○○` 형식으로 블라인드
- 후보자 공개 동의가 확인된 경우에만 병원 회원과 관리자가 실명을 확인하도록 권한 분리
- 인재 목록과 상세 프로필에 동일한 이름 공개 규칙 적용

## 2026-07-17 · 채용공고 상세 페이지 반응형 확대
- 대형 화면의 콘텐츠 최대 폭을 1,680px로 확대하고 제목·모집개요·조건표·상담 카드의 글자와 여백을 함께 상향
- 1,180px 이하에서는 사이드 상담 카드를 본문 아래로 전환해 태블릿 화면의 가독성 확보

## 2026-07-17 · 멤버십 상세조건 문구 정리
- 사용자의 필요를 단정하는 표현을 제거하고, 기능 중심의 중립적인 제목과 설명으로 변경
- 공고 상세 화면의 제목을 `채용공고 상세조건`으로 통일

## 2026-07-17 · 의사 멤버십 결정 조건표

- 공고 상세에 보수·계약, 실제 근무표, 진료 강도, 입사 판단을 묶은 16개 항목 멤버십 조건표 추가
- 무료 회원은 항목 구성을 미리 보고, 멤버십 상태에서는 실제 값과 헤드헌터 확인 메모 열람
- 멤버십 안내 화면에 실제 공개 항목과 건별·월 패스 혜택을 구체적으로 표시
- 병원 공고 등록창에 급여 기준·당직·환자량·지원 인력·면접 절차 등 멤버십 상세조건 입력란 추가

## 2026-07-17 · 공고 브랜드 표시·광고센터 간소화

- 실제 로고·배너가 없는 공고에서 임의 두 글자 약칭을 제거하고 병원명 중심 기본 카드 적용
- 병원 로고·공고 배너·시설 사진과 이력서 프로필 사진·자격증 이미지의 선택 여부 및 권장 픽셀·비율·형식·용량 명시
- 광고센터의 중복 설명·미리보기 구간을 단일 안내 구간과 하나의 신청 버튼으로 통합
- 헤드헌팅 의료인·병원 접수 버튼의 크기와 클릭 영역 확대

## 2026-07-17 · 의사 이력서 권한·업로드 사용성 개선

- 실제 서버 계정의 의사 회원 역할과 QA 의사 미리보기 역할을 이력서 등록 접근 판정에 연결
- 이력서 편집기·단계 메뉴·입력칸·실시간 미리보기의 데스크톱 해상도와 글자 크기 확대
- 프로필 사진, 자격증, 기존 이력서 및 병원 로고·공고 배너·시설 사진에 드래그앤드롭과 파일 검증 추가

## 2026-07-17 · QA 회원 상담 권한 연동

- 의사·병원 QA 로그인 상태를 역할별 상담 신청 접근 판정에 반영
- QA 접수는 미리보기 전용 로컬 기록으로 분리하고 운영 서버 인증 흐름 유지

## 2026-07-17 · 인재정보 단계별 더보기

- 인재 검색 결과를 12명 단위로 추가 표시하는 더보기 방식 적용
- 현재 표시 수·전체 결과 수·남은 인원 안내와 필터 변경 시 목록 초기화 추가

## 2026-07-17 · 인재 카드 시각 분류 단순화

- 조건별 개별 박스를 하나의 정보 밴드와 구분선 구조로 통합
- 공개 상태·제한 안내의 배경을 덜어내고 상세보기 버튼만 주요 동작으로 강조

## 2026-07-17 · 채용공고 상단 제어 영역 고급화

- 검색 결과·관심공고·정렬 버튼을 프리미엄 툴바로 재구성
- 플래티넘 공고 안내와 병원 등록 CTA의 위치·크기·배경·반응형 비율 통일

## 2026-07-17 · 의료인 이력서 선택 사진 등록

- 이력서 기본정보에 프로필 사진 선택 등록, 형식·용량 검증, 미리보기·교체·삭제 기능 추가
- 사진 비공개 원칙과 동의 후 전달 안내 및 실시간 이력서 미리보기 반영

## 2026-07-17 · 인재정보 공개 범위 구분

- 무료 익명 기본정보와 상담·후보 동의 후 공개정보를 상단 비교 안내로 구분
- 카드와 상세 화면에서 병원 멤버십 자동 열람이 아니라는 운영 원칙 명시

## 2026-07-17 · 인재 카드 공간 효율 개선

- 핵심 조건 3개를 카드 전체 폭을 사용하는 가로 요약 칸으로 개편
- 비공개 안내와 상세보기 동작을 한 줄로 묶어 카드 높이와 빈 공간 축소

## 2026-07-17 · 인재정보 탐색 영역 비율 개선

- 인재정보 메뉴와 검색 필터를 공통 1440px 중심축에 맞춰 확대·정렬
- 검색 필드, 익명 안내, 결과 제어 영역의 글자와 여백 및 반응형 비율 보완

## 2026-07-17 · 홈 하단 배너 해상도·비율 통일

- 진행 안내 및 상담 전환 배너 최대 폭 1440px로 확대
- 내부 여백, 제목·아이콘·버튼 크기와 반응형 비율 조정

## 2026-07-17 · 상담 신청 로그인 필수 적용

- 비로그인 상담 진입 시 로그인·회원가입 안내창 표시
- 로그인 후 원래 상담 경로 복귀 및 상담 API 서버 인증 검사 추가

## 2026-07-17 · 공고 상세 병원 정보 레이아웃 개선

- 병원 사진·기관 정보 영역 중앙 정렬 및 카드형 배경 적용
- 갤러리 비율, 정보표 여백·행 높이, 근무지 위치 카드 폭 통일

## 2026-07-17 · 상담 접수 운영 시스템 구축

- 상담 신청 D1 영구 저장 API와 상담 요청 마이그레이션 추가
- 관리자 전용 상담 목록·상세·처리 상태·메모 화면 추가
- Resend 이메일 및 Solapi 문자 알림 연동 코드와 발송 상태 기록 추가
- 상담 원본 우선 저장, 관리자 이메일 허용 목록 검사 적용

## 2026-07-17 · 간편 접수 상단 배너 고급화

- 의사·병원 접수 화면의 히어로 높이와 제목·설명·배지 확대
- 메디헬퍼스 블루·민트 그라데이션 및 반응형 장식 효과 적용

## 2026-07-17 · 접수 절차 안내 카드 확대

- 간편 이력서·초빙 의뢰서의 3단계 안내 영역 확대 및 프리미엄 카드화
- 제목·단계·설명·아이콘 크기와 화면별 반응형 배치 보완

## 2026-07-17 · 상담·초빙 접수 양식 디자인 개편

- 상담·간편 이력서·초빙 의뢰서의 두꺼운 검정 테두리 제거
- 폼 해상도, 제목·라벨·입력 폰트, 입력 높이와 제출 버튼 확대
- 2열 카드형 필드와 태블릿·모바일 1열 반응형 레이아웃 적용

## 2026-07-17 · 헤드헌팅 상담 비율·정렬 수정

- 상담 설명과 단계형 폼을 중앙 정렬된 균형 그리드로 재배치
- 폼의 불필요한 세로 늘어남과 제목의 과도한 줄바꿈 제거
- 중간 화면 한 열 전환 시점을 앞당겨 좁은 2열 문제 방지

## 2026-07-17 · 삼천포제일병원 배너 경로 수정

- 삼천포제일병원 공고 카드의 가로형 배너 파일을 Sites 배포 서버 정적 응답에 추가
- 이미지 404로 대체 텍스트만 노출되던 문제 해결

## 2026-07-17 · 의사 초빙 의뢰서·간편 이력서

- 기존 병원 초빙 의뢰 양식과 의사 간편 이력서의 핵심 항목을 홈페이지 상담 접수 화면에 반영
- 최소 필수정보와 선택정보를 분리하고 연령·성별 조건은 `무관` 또는 선택을 기본으로 설계
- 메디헬퍼스 상담 전화·이메일, 개인정보 수집 목적·파기 안내와 필수 동의 추가
- 종이 서식형 검은 테두리를 제거하고 메디헬퍼스 폼 시스템으로 재설계

## 2026-07-16 · 의사 전문 헤드헌팅 집중

- 모든 의료직군 플랫폼 메시지를 의사 비공개 이직·병원 의사 초빙 중심으로 전환
- 홈 검색을 초빙유형·진료과·지역 기반 의사 전용 검색으로 개편
- 의사 초빙정보에 초빙유형과 근무조건 빠른 필터 추가
- 회원가입·멤버십·인재정보·매칭리포트·광고센터·SEO 문구를 의사 중심으로 통일

## 2026-07-16 · 프리미엄 배너 풀 스테이지

- 등록 배너를 작은 내부 썸네일에서 카드 전체 폭 브랜드 스테이지로 확대
- 병원명과 광고 등급을 유리 질감 오버레이로 분리해 배너 가독성 개선
- 집중채용·추천공고·모바일별 배너 높이와 여백 최적화

## 2026-07-16 · 메인 주목 채용 상단 배치

- `지금 주목할 채용`을 메인 히어로 바로 아래로 이동
- 상담 빠른 메뉴와 멤버십 안내보다 채용공고를 먼저 노출
- 기존 광고 등급별 정렬과 메인 공고 상세 모달 동작 유지

## 2026-07-16 · 메인 공고 모달과 프리미엄 병원 배너

- 메인페이지의 추천 공고 클릭 시 채용정보 페이지로 이동하지 않고 현재 화면에서 상세 모달을 열도록 수정
- 프리미엄 공고에 3:1 병원 배너가 있으면 배너를 우선 노출하고, 없으면 병원 성격 기반 자동 워드마크 배너를 적용
- 소아·여성·검진·척추·한방·요양 등 병원 성격을 제한된 고급 팔레트로 분류하고 동일 병원에는 안정적으로 같은 무드를 배정
- 광고 신청에 1500×500px 권장, 3:1 비율, 최대 8MB 배너 업로드와 규격 검증·미리보기 추가

## 2026-07-16 · 권한별 QA 미리보기 모드

- `/qa-preview`에서 비로그인 방문자·관리자·병원 회원·의료인 일반 회원·의료인 멤버십 회원의 5개 테스트 상태를 즉시 전환하도록 구성
- 선택한 테스트 상태를 브라우저에 유지해 채용정보·상세 모달·매칭 리포트·광고센터·멤버십 페이지를 같은 상태로 점검 가능
- 관리자·병원·의료인·멤버십 상태에 맞춰 헤더 계정 표시, 주요 진입 버튼, 프리미엄 정보 잠금, 구독 상태 안내를 차등 표시
- 모든 테스트 화면에 `QA 미리보기 · 실제 권한 아님` 표식을 고정하고 실제 관리자 권한·개인정보·PG 결제와 분리
- PC·태블릿·모바일 반응형과 모션 감소 환경을 포함한 QA 전용 디자인 적용
- QA 상태 규칙 자동 테스트를 추가해 전체 32개 테스트 통과

## 2026-07-16 · 로고형 광고 병원명 크기 통일

- 로고가 등록된 광고의 병원명을 보조 캡션 크기로 덮어쓰던 스타일 충돌 수정
- 집중채용 로고형 병원명을 워드마크형과 같은 26~32px 제목 위계로 통일
- 추천공고 로고형 병원명을 등급에 맞는 21~25px 위계로 통일
- 가로 배너형·정사각형 로고형 모두 동일 기준 적용, 긴 병원명은 단어 단위 반응형 줄바꿈 처리

## 2026-07-16 · 광고 등급별 카드 위계 강화

- 집중채용 카드를 더 넓은 여백·대형 브랜드 스테이지·큰 공고 제목의 쇼케이스형으로 확대
- 집중채용에 최상단 노출 배지와 더 강한 테두리·그림자·호버 깊이 적용
- 추천공고를 정보 탐색이 빠른 압축형 큐레이션 카드로 재정렬
- 추천공고에 맞춤 추천 배지와 보라색 브랜드 톤, 절제된 호버 반응 적용
- 781~1100px 중간 해상도와 780px 이하 모바일에서 크기 차이를 유지하면서 한 열로 안전하게 전환
- 시스템 모션 감소 환경에서는 등급별 호버 이동을 제거

## 2026-07-16 · 메디 펫 이미지와 광고 진행 바 수정

- Sites 배포 서버가 메디 펫 PNG를 직접 제공하도록 정적 이미지 응답 경로 추가
- 브라우저 캐시 영향을 피하도록 펫 이미지 URL을 새 버전 경로로 교체
- 광고 자동 순환과 수동 이동 기능은 유지하고 카드 하단의 색상 진행 바 애니메이션 제거
- 배포 산출물에서 펫 이미지 200 응답·PNG 형식·원본 바이트 크기와 진행 바 코드 제거를 확인

## 2026-07-16 · 채용공고 상세 모달 모션 개선

- 광고·일반 공고 상세창을 배경 딤·블러와 깊이감 있는 열림·닫힘 애니메이션으로 통일
- 병원 브랜드→공고 제목→핵심 조건→상담 영역이 자연스럽게 이어지는 순차 등장 적용
- 광고 등급 색상을 모달 상단 포인트 라인에 연결하고 상세 조건 카드에 절제된 호버 반응 추가
- 모바일에서는 화면 아래에서 올라오는 바텀시트 형태로 전환
- Escape·배경 클릭·닫기 버튼 모두 닫힘 애니메이션 후 기존 카드로 포커스 복귀
- 시스템의 모션 감소 설정에서는 모든 비필수 전환을 즉시 제거

## 2026-07-16 · 메디헬퍼스 수호천사 도우미

- 기존 캐릭터를 복제하지 않은 메디헬퍼스 전용 ‘커리어 수호천사’ 캐릭터 메디 제작
- 고해상도 투명 PNG 에셋으로 데스크톱·고배율·모바일 화면에서 선명하게 표시
- 채용공고 찾기·매칭 리포트·1:1 헤드헌터 상담·전화 상담을 한 번에 여는 플로팅 안내 패널 추가
- 바깥 클릭·Escape 닫기, 키보드 포커스, 44px 이상 터치 영역과 한글 줄바꿈 대응
- 데스크톱의 미세 부유·반짝임과 모바일 축소 배치, 시스템 모션 감소 설정 대응
- 현재는 빠른 길찾기 도우미이며 실제 AI 대화는 상담 데이터·개인정보 정책 확정 후 서버 연결 예정

## 2026-07-16 · 프론트엔드 회원가입 준비 흐름 완성

- `SIGNUP_ENABLED=false`일 때 차단 게이트(`PreparationGate`) 대신 완성된 다단계 가입 준비 흐름(`PreparationFlow`)을 표시
- 의료인·병원 각각 4단계: 가입 유형·혜택 → 가입 방식 선택(이메일·카카오·네이버·Google) → 필수 약관·만 14세·개인정보 동의 → `가입 준비 프로필 완료`
- 가입 방식은 "정식 오픈 시 연결"만 표기하고 실제 OAuth·비밀번호 입력·가짜 인증은 제공하지 않음
- `localStorage`에는 role·selectedProvider·동의 UI 상태 등 비민감 정보만 저장(PII 미수집), 새로고침 이어하기와 초기화 지원
- 저장값 화이트리스트 정제와 단계 클램프로 조작된 완료 화면 건너뛰기 차단
- `src/signupPreparation.js` 순수 로직과 `src/signupPreparation.test.js` 테스트 12건 추가
- 진행 표시, `aria-live` 상태, 44px 터치 타깃, 한글 줄바꿈, 390/360 반응형 적용
- `SIGNUP_ENABLED=true`의 실제 플랫폼 인증·API 가입 흐름은 그대로 유지 (실제 인증·정식 계정 활성화는 법무 확정 후)

## 2026-07-16 · v64 회원가입 진입 안정화

- `src/basePath.js` 공유 base 경로 도우미를 추가해 앱 전반의 내부 링크 경로 기준을 통일
- AccountPage의 GitHub Pages 하위 경로 내부 링크 404 수정
- GitHub Pages처럼 서버 API가 없는 정적 미러에서는 오류 배너 대신 안전한 준비 게이트 표시
- 회원가입 화면의 로딩·오류 접근성 상태 보강
- 법무 게이트와 `draft` 약관 버전을 유지해 실제 공개 가입은 아직 비활성
- 자동 테스트 6건과 Sites·GitHub Pages 빌드 성공 확인
- 다음 Sites 공개 버전을 v64로 기록

## 2026-07-16 · 최소정보 회원가입 시스템

- `/signup` 최소정보 가입·계정 상태·삭제 화면 추가
- 의료인 회원 `/signup/doctor`와 병원 회원 `/signup/hospital`의 가입 화면·안내·서비스 진입 분리
- Sites 계정 인증과 D1 기반 `accounts`, `consent_records` API·마이그레이션 추가
- 이메일 원문 미저장(HMAC-SHA-256 식별키), 서버 역할 검증, CSRF 방어와 가입 크기 제한 적용
- 이용약관·만 14세 이상·개인정보 안내 확인을 버전별 기록으로 분리
- 법무 승인과 기능 플래그가 모두 충족되기 전 가입을 차단하는 안전장치 추가
- 회원가입 입력 검증 자동 테스트 3건 추가

## 2026-07-16 · 회원가입 법무·보안 설계

- 회원가입, 소셜 로그인, 개인정보 처리와 동의 문구 초안 문서 추가
- 마케팅·민감정보·병원 제3자 제공·국외 이전의 분리 동의 원칙 정의
- 자격 확인, 계정 연결, 권한, 감사 로그, 파일 보안과 출시 차단 체크리스트 추가
- 실제 사업자·수탁사·보유기간·직업안정법 검토와 법무 승인 전 공개 가입 금지

## 2026-07-15 · v53

- GitHub 공개 저장소 `aijunny0604-alt/medihelpers-career`와 로컬 `main` 브랜치 동기화
- GitHub Pages 전용 하위 경로와 직접 URL 접근 대응
- GitHub Actions 기반 Pages 자동 배포 구성 및 실제 배포 성공 확인
- 메인과 `/professions` 직접 접근에서 정상 응답 확인
- 동일 커밋 `15e3ff9`를 OpenAI Sites v53과 GitHub에 함께 반영
- README·CHANGELOG·STATUS·DEPLOY 문서의 배포 상태와 자동화 범위 최신화

## 2026-07-15 · GitHub 동기화

- GitHub 공개 저장소와 main 브랜치 연결
- GitHub Pages 자동 배포 워크플로 추가
- Sites 배포와 GitHub 소스·README·업데이트 기록 동시 갱신 원칙 추가

## 2026-07-15 · v52

- 직군별 반복 카드 그리드를 직군 선택 목록과 단일 상세 패널로 개편
- 모든 직군 상담을 전담 헤드헌터 창구로 통합
- 광고센터 미리보기를 비대칭 프리미엄 브랜드 쇼케이스로 교체
- 병원 브랜드 영역과 채용 포지션 정보를 분리해 광고 상품 차별화
- 360px, 390px, 768px, 1920px 반응형 검증

## 2026-07-15 · v51

- 전체 페이지에서 중복 적용되던 섹션 간격 정리
- 채용정보 하단 매칭 리포트와 상담 배너 사이의 과도한 빈 여백 축소
- 데스크톱·태블릿·모바일 섹션 리듬 통일

## 이전 주요 작업

- 메디헬퍼스 매칭 리포트와 헤드헌터 상담 연결
- 병원 로고·배너 중심 프리미엄 공고 카드
- 광고 등급별 우선 노출과 애니메이션 차등
- 부드러운 스크롤, 페이지 전환, 카드 호버 모션
- 카카오톡 공유용 소셜 미리보기와 파비콘
# 2026-07-16

- 홈페이지의 브라우저 저장 로직을 `browserStorage` 모듈로 통합했습니다.
- 손상된 JSON, 저장소 차단, 용량 초과 상황에서 메모리 저장으로 안전하게 대체하도록 개선했습니다.
- 내부 링크가 수정키·가운데 클릭을 가로채지 않도록 수정하고 모바일 메뉴 접근성 속성을 추가했습니다.
- 저장 로직 회귀 테스트를 추가했습니다.
- 첫 화면 검색 드롭다운의 쌓임 순서와 열림 방향을 보정해 인접 카드와 겹쳐 가려지지 않도록 수정했습니다.
- 모달을 문서 최상위 레이어로 이동하고 카드 내부 스크롤을 분리해 헤더·페이지 스크롤과의 겹침을 제거했습니다.
# 2026-07-17 문서·서버 경계 최신화

- README와 상태·아키텍처·API·권한·결제·테스트 문서를 현재 Sites+D1 구현 기준으로 다시 정리했습니다.
- 실제 구현된 주문 원장과 관리자 수동 상태관리, 아직 연결되지 않은 PG 승인·웹훅·환불 실행을 구분했습니다.
- 관리자 D1 콘텐츠와 정적 시연 데이터가 함께 노출되는 현재 구조와 건수 차이를 알려진 운영 과제로 기록했습니다.
- 상담 신청의 가입 역할 검사를 추가하고 인증 계정 이메일로 상담 내역을 연결하도록 보완했습니다.
- 손상된 JSON 데이터 한 건이 마이페이지·관리자 콘솔 전체를 중단하지 않도록 안전 파싱을 적용했습니다.
- 공개 카테고리 API의 60초 캐시 헤더가 응답에 반영되도록 JSON 응답 헬퍼를 수정했습니다.
