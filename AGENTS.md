# AGENTS.md

## Purpose

AI 개발 도구가 메디헬퍼스의 사업 방향과 안전 기준을 유지하며 작업하기 위한 지침입니다.

## Current State

현재는 정식 출시 전 통합 MVP입니다. 자체 로그인·D1 데이터 저장·관리자 운영·가상 결제는 구현됐으며, 이니시스 실결제와 외부 알림·자격 확인은 운영 계약과 키 설정 후 전환합니다.

## Current Rules

- 모든 작업 전 `docs/INDEX.md`와 관련 주제 문서를 먼저 읽습니다.
- **단일 소스 원칙:** GitHub `agent/medihelpers-ui-stability`를 현재 개발·배포 기준 브랜치로 사용합니다. Sites 소스 `main`은 배포용 미러일 뿐 독립적으로 개발하지 않습니다.
- **작업 시작 전 동기화:** 원격을 fetch한 뒤 기준 브랜치, Sites `main`, 현재 작업본의 공통 조상과 앞뒤 커밋 수를 확인합니다. 기준 브랜치에만 존재하는 커밋이 하나라도 있으면 그 상태로 구현·배포하지 않습니다.
- **클로드 작업 보존:** 클로드 작업 폴더의 미커밋 파일을 덮어쓰거나 초기화하지 않습니다. 별도의 깨끗한 작업본에서 통합하고, 미커밋 변경은 파일 목록과 diff를 확인해 누락 여부를 기록합니다.
- **배포 후보 단일화:** 기능 수정은 먼저 기준 브랜치에 통합합니다. 검증된 동일 커밋 SHA만 GitHub 기준 브랜치와 Sites `main`에 반영하며, 서로 다른 소스 커밋으로 배포하지 않습니다.
- **배포 차단 조건:** 미커밋 파일, 병합 충돌 표시, 기준 브랜치의 누락 커밋, 실패한 테스트·빌드, 운영 바인딩 변경이 하나라도 있으면 배포를 중단합니다.
- **다른 PC 인증 검증:** 로그인·권한 관련 변경은 쿠키가 없는 새 세션에서 의료인·병원·관리자 로그인을 각각 수행하고 역할별 보호 API가 `200`, 반대 역할은 `401/403`인지 확인합니다.
- **배포 후 검증:** 실제 운영 URL에서 배포 커밋에 대응하는 최신 문서가 제공되는지, HTML이 `no-store`인지, `/api/account`와 역할별 보호 API가 정상인지 확인한 뒤에만 완료로 보고합니다.
- **완료 보고 근거:** 기준 브랜치 포함 여부, 전체 테스트 수, 빌드 결과, 배포 버전, 새 PC 역할별 검증 결과를 `CHANGELOG.md`, `docs/STATUS.md`, `docs/TEST.md`에 함께 기록합니다.
- 메디헬퍼스의 핵심은 단순 공고 게시판이 아닌 1:1 의료 커리어 컨시어지입니다.
- 타 서비스의 상표, 문구, 화면을 복제하지 않습니다.
- 실제 의사 개인정보와 병원 내부정보를 샘플 데이터에 넣지 않습니다.
- 결제는 승인된 PG와 서버 검증 없이 성공 처리하지 않습니다.
- 개인정보 수집 폼에는 목적, 보유기간, 동의 체크를 포함합니다.
- 변경 후 `npm run build`를 실행합니다.
- 배포 시 `.openai/hosting.json`의 기존 `project_id`를 유지합니다.

위 배포 무결성 규칙은 Claude Code, Codex 및 수동 작업에 동일하게 적용하며, 작업자나 도구가 바뀌어도 생략할 수 없습니다.

## Related Docs

- `docs/DESIGN.md`
- `docs/ARCHITECTURE.md`
- `docs/TEST.md`
- `docs/DEPLOY_GUARDRAILS.md`

## Brand Adaptation Rule

- 사용자가 경쟁 서비스나 참고 화면을 제공해도 색상, 테두리, 간격, 카드 모양을 그대로 복제하지 않습니다.
- 레퍼런스에서는 정보 구조, 사용자 흐름, 필요한 입력 항목과 상호작용만 추출합니다.
- 최종 화면은 메디헬퍼스의 Navy `#10213F`, Blue `#1263E8`, Mint `#20B894`, 밝은 Surface 색상과 충분한 여백을 사용해 독자적으로 재설계합니다.
- 두껍고 검은 외곽선, 과도한 장식, 작은 글자처럼 메디헬퍼스 톤과 맞지 않는 요소는 참고 화면에 있더라도 사용하지 않습니다.
- 주요 버튼은 충분히 크고 명확하게 배치하며, 폼과 정보량이 많아질수록 콘텐츠 폭·글자 크기·입력 높이를 함께 확장합니다.
- 구현 전 항상 “참고한 구조”와 “메디헬퍼스 방식으로 재해석할 시각 표현”을 분리해서 판단합니다.
