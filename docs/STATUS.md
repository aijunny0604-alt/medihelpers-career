# 현재 구현 상태

## 2026-07-17 QA 회원 상담 권한 연동

- QA 미리보기의 의사·병원·관리자 회원 상태를 구직·구인 상담 신청 화면의 접근 권한과 연결했습니다.
- 미리보기 회원은 로그인 안내 대신 해당 역할의 상담 양식을 바로 확인하고 테스트 접수를 완료할 수 있습니다.
- QA 접수는 브라우저 미리보기 데이터로만 기록하며 실제 운영 회원의 서버 인증·서버 저장 경로와 분리했습니다.

## 2026-07-17 인재정보 단계별 더보기

- 검색 결과는 처음 12명만 렌더링하고 `인재 더보기`를 누를 때마다 12명씩 추가하도록 변경했습니다.
- 결과 상단에 현재 표시 인원과 전체 검색 결과 수를 함께 보여주고 더보기 버튼에는 남은 인원 수를 표시합니다.
- 진료과·지역·입사 시점·키워드·정렬 조건이 바뀌면 표시 범위를 자동으로 첫 12명으로 초기화합니다.

## 2026-07-17 인재 카드 시각 분류 단순화

- 인재 카드 안의 세 개 조건 박스를 하나의 가로 정보 밴드로 통합하고 구분선만 남겼습니다.
- 기본 공개 상태와 상담 후 공개 안내의 채움 배경을 제거해 시각적 단계와 색상 수를 줄였습니다.
- 카드에서는 상세보기 버튼만 주요 동작으로 강조하고 모바일에서는 간결한 세로 정보줄로 전환합니다.

## 2026-07-17 채용공고 상단 제어 영역 고급화

- 검색 결과 수·관심공고·추천순·최신순을 카드 그리드와 같은 폭의 프리미엄 툴바로 묶었습니다.
- 플래티넘 공고 제목·설명·병원 등록 버튼을 하나의 안내 배너에 정렬하고 여백·비율·강조 체계를 통일했습니다.
- 좁은 화면에서는 정렬 버튼을 균등 배치하고 등록 배너를 세로형으로 전환해 잘림 없이 유지합니다.

## 2026-07-17 의료인 이력서 선택 사진 등록

- 의사회원 이력서 기본정보 단계에 JPG·PNG·WEBP, 최대 5MB의 선택 프로필 사진 등록·미리보기·교체·삭제 기능을 추가했습니다.
- 등록 사진은 이력서 실시간 미리보기에도 표시되며 파일명은 이력서 접수 데이터와 함께 기록됩니다.
- 사진은 익명 인재목록에는 공개하지 않고 실명·연락처와 마찬가지로 본인 동의 후에만 전달된다는 안내를 명시했습니다.

## 2026-07-17 인재정보 공개 범위 구분

- 병원 인재정보는 유료 멤버십 자동 열람이 아니라 무료 익명 기본정보와 헤드헌터 상담·후보 동의 후 공개정보로 구분했습니다.
- 상단 안내에서 무료 공개 항목과 제한 항목을 두 칸으로 비교하고 모든 인재 카드에 `기본 공개` 상태를 표시합니다.
- 근무기관 이력·세부 술기·이직 사유·컨설턴트 메모는 병원 조건 확인과 후보자 동의 후 전달된다는 원칙을 카드와 상세 화면에 함께 명시했습니다.

## 2026-07-17 인재 카드 정보 밀도 개선

- 넓은 2열 카드에서 정보가 왼쪽에만 몰리던 세로 행 구조를 카드 전체 폭을 사용하는 3칸 조건 요약으로 변경했습니다.
- 비공개 상세정보 안내와 프로필 상세보기 버튼을 한 줄에 배치해 반복 카드의 높이와 불필요한 여백을 줄였습니다.
- 모바일에서는 요약 정보를 다시 한 열로 전환해 작은 화면에서도 라벨과 값을 빠르게 읽을 수 있게 유지했습니다.

## 2026-07-17 인재정보 탐색 영역 비율 개선

- 인재정보 메뉴, 검색 필터, 익명 안내, 결과 제어 영역을 상단 주요 배너와 같은 1440px 중심축으로 정렬했습니다.
- 메뉴·검색 제목·입력 필드·안내 문구의 글자 크기와 클릭 영역을 키우되 목록 노출을 해치지 않도록 전체 높이를 조정했습니다.
- 태블릿과 모바일에서는 확대된 데스크톱 비율이 잘리거나 과도하게 커지지 않도록 단계별 반응형 규칙을 추가했습니다.

## 2026-07-17 홈 하단 배너 비율 통일

- 홈 하단의 진행 안내 카드와 상담 전환 배너를 상단 주요 섹션과 같은 1440px 최대 폭과 29px 모서리 기준으로 통일했습니다.
- 진행 안내의 제목·설명·아이콘·단계 문구를 확대하고 내부 높이와 좌우 여백을 늘려 큰 화면에서 균형 있게 보이도록 조정했습니다.
- 파란 상담 배너도 동일한 폭과 비율로 맞추고 제목·버튼 크기와 그림자를 상단 배너 수준으로 보완했습니다.

## 2026-07-17 상담 신청 로그인 게이트

- 의사 구직상담과 병원 채용상담 진입 시 로그인 여부를 먼저 확인하고 비로그인 사용자에게 로그인 안내창을 표시합니다.
- 안내창에는 로그인 후 원래 상담 화면으로 돌아오는 버튼과 의사·병원 유형별 회원가입 안내 버튼을 제공합니다.
- 상담 저장 API에서도 인증 사용자 여부를 다시 검사해 주소 직접 접근이나 외부 요청으로 우회할 수 없도록 했습니다.
- 현재 정식 회원가입 저장 기능은 약관 승인 전 단계이므로 우선 로그인 여부를 기준으로 제한하며, 정식 오픈 시 계정 유형 검사로 강화할 수 있습니다.

## 2026-07-17 공고 상세 병원 프로필 중앙 정렬

- 공고 상세의 병원 사진과 기관 정보 영역을 중앙 정렬된 프리미엄 카드로 재구성했습니다.
- 사진 갤러리와 정보표 사이 여백을 늘리고 대표 사진·썸네일·정보 행의 높이와 글자 크기를 조정했습니다.
- 근무지 위치 카드도 같은 본문 폭과 톤으로 맞추고 태블릿·모바일에서는 자연스럽게 세로 배치됩니다.

## 2026-07-17 상담 접수 서버 저장·관리자 상담함

- 의사 구직희망과 병원 구인희망 제출 내용을 브라우저가 아닌 D1 데이터베이스에 영구 저장하도록 전환했습니다.
- `/admin/consultations`에 관리자 전용 상담함을 추가해 신규·연락 완료·진행 중·종료 상태, 상세 입력값, 관리 메모와 알림 발송 결과를 확인할 수 있습니다.
- 관리자 화면과 조회·수정 API는 `ADMIN_EMAILS`에 등록된 인증 사용자만 접근할 수 있도록 서버에서 권한을 검사합니다.
- 새 접수 저장 후 Resend 이메일과 Solapi 문자 알림을 각각 시도하며, 서비스가 설정되지 않았거나 발송이 실패해도 상담 원본은 먼저 안전하게 보관됩니다.
- 운영 알림에는 `RESEND_API_KEY`, `RESEND_FROM`, `SOLAPI_API_KEY`, `SOLAPI_API_SECRET`, `SOLAPI_SENDER` 설정이 필요합니다.

## 2026-07-17 간편 접수 상단 소개 배너 확대

- 의사 간편 이력서와 병원 초빙 의뢰서의 상단 소개 배너를 340px 높이의 프리미엄 히어로 영역으로 확대했습니다.
- 제목, 설명, 서비스 안내 배지의 글자 크기와 여백을 키우고 블루·민트 계열의 깊이 있는 그라데이션과 장식 효과를 적용했습니다.
- 모바일에서는 장식을 줄이고 제목 크기와 패딩을 조정해 핵심 문구가 먼저 보이도록 구성했습니다.

## 2026-07-17 접수 절차 안내 영역 고급화

- 간편 이력서와 초빙 의뢰서 상단의 3단계 안내를 넓은 프리미엄 카드로 재구성했습니다.
- 안내 제목, 단계명, 설명과 아이콘을 확대하고 각 단계를 독립 카드로 구분해 한눈에 읽히도록 개선했습니다.
- 태블릿에서는 2열, 모바일에서는 1열로 전환해 작은 화면에서도 여백과 글자 크기를 유지합니다.

## 2026-07-17 상담·초빙 접수 양식 시각 개편

- 기존 접수 양식에 남아 있던 두꺼운 검정 외곽선을 제거하고 메디헬퍼스의 블루·민트 톤에 맞춘 밝은 카드형 화면으로 교체했습니다.
- 양식 폭을 1320px까지 확장하고 제목, 라벨, 입력값, 안내 문구와 버튼을 전반적으로 키워 큰 화면에서도 읽고 작성하기 편하도록 조정했습니다.
- 입력 항목을 독립 카드로 구분하고 태블릿에서는 1열, 모바일에서는 더 간결한 1열 배치로 전환되도록 반응형 규칙을 보완했습니다.

## 2026-07-17 헤드헌팅 상담 섹션 비율 수정

- 헤드헌팅 상담 섹션이 왼쪽으로 치우치고 폼이 설명 영역 높이만큼 늘어나던 그리드 문제를 수정했습니다.
- 섹션을 화면 중앙에 배치하고 데스크톱 설명·폼 비율을 재조정했으며, 폼은 실제 콘텐츠 높이만 사용합니다.
- 제목 줄바꿈을 의도한 두 줄 구조로 고정하고 1240px 이하에서는 한 열로 전환합니다.

## 2026-07-17 삼천포제일병원 배너 배포 경로 수정

- 공고 카드가 요청하는 `samcheonpo-jeil-horizontal-logo-v2.png`가 Sites 서버 응답 목록에서 누락되어 대체 텍스트만 표시되던 문제를 수정했습니다.
- 가로형 병원 배너 원본을 배포 산출물에 포함하고 장기 캐시되는 PNG 정적 응답으로 제공합니다.

## 2026-07-17 의사 초빙 의뢰서·간편 이력서 개편

- 병원용 `/request/hiring`을 `의사 초빙 의뢰서`, 의사용 `/request/job-seeker`를 `의사 선생님 간편 이력서`로 재구성했습니다.
- 병원명·담당자와 직함·담당자 전화번호, 의사 이름·전화번호·전문 분류·전문과목만 최소 필수항목으로 두고 주소·이메일·희망 연령·성별·Fellow·경력·근무조건은 선택 또는 `무관` 기본값으로 보완했습니다.
- 연락처 `010-2435-5463`, `hr@medihelpers.co.kr`, 필수항목 안내와 수집 목적·파기 시점·동의 체크를 폼 안에서 명확히 표시합니다.
- 현재 접수 내용과 첨부파일명은 시장 검증용 브라우저 저장 흐름이며 실제 운영 DB·이메일 알림은 Phase 2 범위입니다.

## 2026-07-16 의사 전문 헤드헌팅으로 제품 집중

- 모든 의료직군 확장형 메시지를 제거하고 서비스 정의를 의사 비공개 이직과 병원 의사 초빙을 연결하는 전문 헤드헌팅으로 전환했습니다.
- 홈 검색을 `초빙유형 / 진료과 / 지역` 구조로 바꾸고 의사 전용 메시지와 비공개 상담 동선을 강화했습니다.
- 의사 초빙정보에 초빙유형 필터와 `주 4일 / 당직 협의 / 숙소 지원 / 검진 중심` 상세조건 필터를 추가했습니다.
- 회원가입·멤버십·인재정보·매칭리포트·광고센터·메타데이터의 사용자 명칭을 `의료인`에서 `의사`로 통일했습니다.
- 참고 서비스의 상표·문구·화면은 복제하지 않고 의사 채용 검색에 필요한 정보 구조만 반영했습니다.

## 2026-07-16 프리미엄 배너 풀 스테이지 개편

- 등록된 병원 배너를 카드 안의 작은 썸네일 대신 브랜드 영역 전체를 채우는 풀 배너로 확대했습니다.
- 병원명·광고 등급·추천 표시는 배너 위의 반투명 오버레이로 분리해 밝거나 복잡한 이미지에서도 읽히도록 했습니다.
- 로고형 광고와 배너형 광고의 표현을 분리하고 데스크톱·모바일에서 서로 다른 높이와 여백을 적용했습니다.

## 2026-07-16 메인 주목 채용 우선 노출

- 메인 페이지의 `지금 주목할 채용`을 히어로 바로 아래로 이동해 상담·멤버십 안내보다 먼저 보이도록 했습니다.
- 기존 집중채용 → 추천 광고 → 일반 공고 우선순위와 메인 공고 상세 모달 동작은 그대로 유지합니다.

## 2026-07-16 채용정보 균형 노출 시스템

- 순수 모듈 `src/jobExposure.js`(+`src/jobExposure.test.js`, 노드 테스트 10건)를 추가했습니다. `balancedOrder`는 진료과(dept) 라운드로빈에 지역(region) 보조 분산을 더한 안정적 인터리빙으로 특정 진료과·지역이 몰려도 앞쪽 슬롯에 여러 진료과·지역이 나오게 하고, 가장 작은 버킷 내부는 원본 등록 순서(최신 등록순 대용)를 유지합니다. 명시적 seed로 첫 진료과만 회전하며 같은 입력·seed면 결과가 고정됩니다. `orderPremium`은 광고 등급(spotlight→featured→basic) 우선순위를 지키며 등급 내부에서만 균형을 맞춥니다. `countBy`/`countByDept`로 카테고리 개수를 집계하고, 빈 입력·dept/region 누락에도 손실/중복 없이 안전합니다.
- `JobsPage`(`src/main.jsx`)를 리팩터링했습니다. 기존 진료과·지역·키워드 필터를 유지한 채, 세션당 1회 계산하는 UTC 일 단위 seed(`Math.floor(Date.now()/86400000)`)로 하루 동안 순서를 고정합니다. 프리미엄은 등급 내 균형 + 초기 6건 노출 후 `프리미엄 공고 더보기`, 일반은 진료과·지역 균형 + 초기 9건 후 전체 폭 `공고 더보기`(9건씩, 남은 개수 표시)로 확장하며, 필터 변경 시 노출 수를 초기화합니다. 유료 광고가 많아도 최상단 프리미엄 약속을 지키면서 일반 목록이 끝없이 밀리지 않습니다.
- 목록 상단에 지역+키워드 기준 진료과 빠른 필터 스트립(`전체` + 실제 개수 배지, 모바일 가로 스크롤, `aria-pressed`·44px 터치·`focus-visible`)과 `균형 노출` 설명 패널을 추가했습니다. 일반 헤딩 라벨을 `최신 등록순` → `진료과·지역 균형순`으로 바꾸고 노출/전체 개수를 표기합니다. 카드 열람·저장 트래킹과 매칭 리포트 섹션은 그대로 동작하며, 렌더 반복을 줄이는 `renderCard` 헬퍼를 도입했습니다. 데스크톱 3열/태블릿 2열/모바일 1열과 1920~360 무가로스크롤, 모션 감소 존중을 유지합니다. `npm test`(27건)·Sites/GitHub Pages 빌드 성공을 확인했습니다.

## 2026-07-16 홈 히어로 검색 패널 주요 액션 확대

- 홈 히어로 검색 패널(`hero-search-card`)을 주요 행동 패널로 키웠습니다. 데스크톱 ≥1400에서 최대 약 1,080px 폭·30px 패딩·31px 라운드·파란빛 테두리와 다층 그림자, 58px 검색 아이콘·21px 제목·15px 보조문구, 97px 높이 필드·18px 값·258px CTA(호버/`focus-visible` 깊이)를 적용하고, `hero-search-title`에 `1분 검색` 배지를 추가했습니다.
- 1101~1399 컴팩트 데스크톱은 폭·CTA·필드 높이를 살짝 줄여 컨시어지 카드와 균형을 유지하고 1366/1536/1920에서 가로 넘침이 없습니다. `!important` 없이 늦은 위치·미디어쿼리로 기존 오버라이드를 이깁니다. ≤780 반응형은 기존 세로 적층·1/2열 로직과 최소 52px 컨트롤, 맞춤 셀렉트 위치·키보드 포커스·모션 감소 동작을 그대로 유지합니다.

## 2026-07-16 프론트엔드 전용 가입 신청 초안 폼 전환

- 정식 오픈 전 회원가입 화면을 단계형 준비 흐름 대신 실제 입력 항목을 갖춘 프론트엔드 전용 **가입 신청 초안 폼**으로 교체했습니다(`/signup/doctor`, `/signup/hospital`). `/signup` 역할 선택 화면은 유지합니다.
- 공통 입력(이름·휴대폰·이메일·비밀번호·비밀번호 확인)과 병원 추가 입력(병원·기관명·담당자 역할)을 라벨·autocomplete·inputMode·`aria-invalid`/`aria-describedby`·한글 오류 메시지와 함께 제공합니다. 비밀번호는 영문·숫자 포함 8자 이상, 휴대폰은 한국 휴대폰 형식으로 검증합니다.
- 전체 동의 + 필수(이용약관·개인정보 수집·이용·만 14세) 동의와 펼침형 약관·개인정보 초안 요약을 제공하며 마케팅 수신 동의는 받지 않습니다.
- **개인정보를 저장하지 않습니다.** 입력값은 컴포넌트 상태로만 존재하고 `localStorage`/`sessionStorage`에 쓰지 않으며, 프론트엔드 전용 완료 또는 초기화 시 모든 필드와 동의를 즉시 비웁니다. 완료 화면은 `가입 양식 확인 완료`만 표시하고 실제 계정은 만들지 않으며 요약에 선택한 회원 유형과 `저장된 개인정보 없음`만 남깁니다.
- 순수 로직을 `src/signupFields.js`(+`src/signupFields.test.js`)로 분리하고, 프리미엄·반응형 가입 폼 스타일(넓은 셸 ~1340px, 히어로 46~64px, 큰 입력 필드 58px, 2열 데스크톱/1열 모바일, 360~1920 무가로스크롤)을 적용했습니다. 완료·초기화는 `clearDraftFields`로 회원 유형만 남기고 모든 PII·동의를 즉시 비웁니다. 기존 `SIGNUP_ENABLED`/플랫폼 인증/`api/account` 흐름은 그대로 유지하며 비밀번호 등 draft 값은 서버로 전송하지 않습니다.
- 네비게이션에서 `/professions`(의사 이직상담)를 제거하고 `/headhunting`으로 통합했습니다. 채용정보 페이지 CTA를 `헤드헌팅 상담`(`/headhunting`)으로 바꾸고, 예전 `/professions` 접근은 base 경로를 보존하는 SPA 리다이렉트로 `/headhunting`에 연결합니다.

## 2026-07-16 프론트엔드 회원가입 준비 흐름 (대체됨)

> **참고:** 이 단계형 준비(provider preparation) 흐름은 위의 `2026-07-16 프론트엔드 전용 가입 신청 초안 폼 전환`으로 **대체되었습니다.** 관련 순수 로직 파일 `src/signupPreparation.js`·`src/signupPreparation.test.js`는 삭제되었고, 현재는 `src/signupFields.js`·`src/signupFields.test.js`가 대신합니다. 아래 내용은 이력 기록용이며 현재 활성 흐름이 아닙니다.

- (이전) 정식 오픈 전 홈페이지에서 회원가입 전체 흐름을 끝까지 눌러볼 수 있도록, `SIGNUP_ENABLED=false`일 때 차단 게이트 대신 프론트엔드 가입 준비 흐름을 표시했습니다.
- (이전) 의료인(`/signup/doctor`)·병원(`/signup/hospital`) 각각 4단계: 가입 유형·혜택 확인 → 가입 방식 선택(이메일·카카오·네이버·Google) → 필수 약관·만 14세·개인정보 동의 → `가입 준비 프로필 완료`.
- (이전) 가입 방식 버튼은 "정식 오픈 시 연결"만 표기하고 실제 OAuth·비밀번호 입력·가짜 인증을 제공하지 않았습니다.
- (이전) `localStorage`에는 회원 유형·선택한 가입 방식·동의 UI 상태 같은 비민감 정보만 저장하며, 이름·전화·이메일·비밀번호 등 개인정보는 수집·저장하지 않았습니다.
- **구분**: 실제 플랫폼 인증과 정식 계정 활성화는 법무·개인정보 검토가 확정된 뒤에 진행합니다(`SIGNUP_ENABLED=true` 경로는 그대로 유지).

## 2026-07-16 회원가입 진입 안정화 (v64)

- 공유 base 경로 도우미를 추가해 앱 전반의 내부 링크 경로 기준을 통일하고 GitHub Pages 하위 경로에서의 계정 화면 내부 링크 404를 수정했습니다.
- GitHub Pages처럼 서버 API가 없는 정적 미러에서는 오류 배너 대신 개인정보를 수집하지 않는 안전한 준비 게이트를 표시하도록 보강했습니다.
- 회원가입 화면의 로딩·오류 접근성 상태를 보강했습니다.
- 법무 게이트와 `draft` 약관 버전을 유지해 실제 공개 가입은 아직 비활성이며, 자동 테스트 6건과 Sites·GitHub Pages 빌드 성공을 확인했습니다.

## 2026-07-16 최소정보 회원가입 시스템

- `/signup`에 플랫폼 계정 인증, 의료인·병원 역할 선택, 필수 약관·연령·개인정보 안내 확인, 가입 완료·로그아웃·계정 삭제 흐름을 구현했습니다.
- 의료인 회원은 `/signup/doctor`, 병원 회원은 `/signup/hospital`로 가입 목적·확인 절차·가입 후 이동을 분리했습니다.
- 가입 단계에서는 비밀번호, 이름, 전화번호, 면허번호, 주민등록번호와 마케팅 동의를 받지 않습니다.
- D1 `accounts`, `consent_records` 스키마와 서버 API를 추가했으며 이메일 원문 대신 서버 비밀키를 사용한 HMAC-SHA-256 값을 계정 키로 저장합니다.
- 가입 API는 약관·개인정보 안내의 `draft` 버전 제거, 기능 플래그·법무 승인 상태·32자 이상 계정 해시 비밀키가 모두 충족되기 전까지 닫혀 있고 화면은 개인정보를 수집하지 않는 준비 상태를 표시합니다.
- Google·Kakao·Naver 로그인은 Supabase와 각 사업자 앱 키·심사 정보가 없어 아직 연결하지 않았으며 가짜 로그인은 제공하지 않습니다.

## 2026-07-16 회원가입 법무·보안 설계

- 대한민국 개인정보 보호법과 개인정보보호위원회 2026 처리방침 작성지침을 기준으로 회원가입·소셜 로그인 법무/보안 기준을 문서화했습니다.
- 필수 약관, 개인정보 처리 근거, 선택 마케팅, 의료인 자격 확인, 소개 건별 병원 제공, 국외 이전을 서로 분리했습니다.
- Google·Kakao·Naver 로그인 데이터 최소화, 계정 연결, RLS·MFA·감사 로그·파일 보안과 동의 버전 기록 기준을 정의했습니다.
- 실제 회원가입 기능은 아직 구현하지 않았으며 사업자 정보, 수탁사·리전, 보유기간, 직업안정법상 신고·등록과 법무 최종 검토가 완료될 때까지 공개 가입을 차단합니다.

## 2026-07-16 홈페이지 안정성 점검

- 브라우저 저장 데이터가 손상되거나 저장소 접근이 차단되어도 화면이 중단되지 않도록 공통 저장 계층을 적용했습니다.
- 상담, 관심 공고·인재, 광고·멤버십 요청, 전환 이벤트의 저장 방식을 하나로 통합했습니다.
- 내부 링크의 새 탭·가운데 클릭 동작을 보존하고 모바일 메뉴의 접근성 상태를 보강했습니다.
- 저장 계층 자동 테스트 3건과 Sites 배포 빌드 검증을 통과했습니다.
- 첫 화면 검색 드롭다운이 아래 빠른 메뉴 뒤로 가려지는 겹침을 수정하고, 화면 여유 공간에 따라 위·아래로 열리도록 보정했습니다.
- 공고 상세 등 모달을 본문 전환 레이어 밖으로 분리하고 내부 전용 스크롤을 적용해 고정 헤더와 스크롤바가 내용을 가리지 않도록 수정했습니다.

## 마지막 갱신

- 날짜: 2026-07-16
- Sites 공개본: https://medihelpers-career.junnyai.chatgpt.site
- GitHub Pages 공개본: https://aijunny0604-alt.github.io/medihelpers-career/
- GitHub 저장소: https://github.com/aijunny0604-alt/medihelpers-career
- 최신 Sites 버전: v64

## 공개 화면에 구현됨

- 반응형 홈과 직군 선택형 채용 검색
- 채용정보 검색·필터, 관심 공고, 공고 상세 모달
- 8개 의료직군을 반복 카드 대신 선택 목록과 단일 상세 패널로 탐색하는 통합 허브
  - 의사·치과의사·한의사
  - 간호·간호조무
  - 약사·한약사
  - 방사선사
  - 임상병리사
  - 물리·작업·언어치료
  - 치과위생·치과기공
  - 응급·보건·기타
- 의사 채용정보 운영과 타 직군 파트너 모집·오픈 알림 상담 연결
- 의료인·병원 3단계 상담 흐름과 상담번호 발급
- 의료인·병원 프론트엔드 전용 가입 신청 초안 폼(이름·휴대폰·이메일·비밀번호, 병원은 기관명·담당 역할 추가): 개인정보 무저장, 완료·초기화 시 즉시 삭제, 완료 시 `가입 양식 확인 완료`만 표시(실제 계정 미생성, 정식 인증은 오픈 시 연결)
- 광고 상품, 멤버십, 건별 열람권 안내 및 요청 접수 화면
- 헤드헌팅과 서비스 소개 페이지
- 홈의 의료인·의료기관 선택 카드에서 한글이 글자 단위로 갈라지지 않는 반응형 줄바꿈
- 추천·집중채용 광고의 병원 로고 강조 영역과 로고 미등록 시 대체 마크
- 광고 등급을 공고 배지와 분리하고, 로고 등록 시 대형 브랜드 스테이지·미등록 시 병원명 워드마크·1회성 빛 효과 적용
- 집중채용 → 추천 광고 → 일반 공고 순 우선 노출과 등급별 차등 진입·호버 애니메이션
- 광고센터의 가상 블루케어 집중채용 배너 예시와 실제 공고 아님 고지, 광고 신청 연결
- 광고센터의 병원 브랜드와 채용 포지션을 분리한 비대칭 프리미엄 쇼케이스
- PC 프리미엄 광고 카드의 포인터 추적형 미세 광원·깊이감·느린 감속 호버
- 메인 화면의 설명형 남색 섹션을 제거하고 핵심 안내 다음에 우선순위 채용공고를 바로 노출
- 푸터 소개 문구를 추상적인 슬로건 대신 직접 듣고 연결한다는 사람 중심의 말투로 개선
- 메인 검색 카드와 직군·진료과·지역 선택 영역을 확대하고 펼침 목록의 글자·행간 여유 개선
- Windows 화면 배율별 카드 흐림을 막기 위해 스크롤 블러와 상시 GPU·3D 확대 렌더링 제거
- 집중채용·추천 광고 카드에 고해상도 생성형 병원 광고 마크 2종 적용 및 공식 로고 아님 고지
- 블러 없는 카드 스크롤 진입 모션과 프리미엄 카드 호버 이동·광원 효과 복원
- 링크 이동 시 본문 퇴장·진입을 분리한 선명한 페이지 전환 애니메이션 복원
- 광고 클릭 시 병원 프로필·근무지·지도 검색·채용조건을 구분해 보여주는 상세 화면
- 관성형 스크롤 등장, 약한 히어로 시차, 스크롤 진행 표시와 모션 감소 접근성 대응
- 광고 신청서의 병원 로고 미리보기·기관 유형·위치·소개 수집과 상세창 키보드 초점 관리
- PC 휠·트랙패드용 관성형 페이지 스크롤과 모바일·모달·모션 감소 환경의 기본 스크롤 유지
- 채용공고 카드 전체 클릭 상세보기와 독립 관심공고 버튼·키보드 초점 지원
- GitHub Pages 하위 경로와 직접 URL 접근 대응
- GitHub `main` 푸시 후 Pages 자동 배포 워크플로 운영
- 광고센터 라이브 예시도 실제 추천·집중채용 목록과 동일한 간결한 카드 구조로 통일
- `/professions`(의사 이직상담) 진입점을 제거하고 헤드헌팅(`/headhunting`)으로 통합, 예전 경로는 base 경로 보존 SPA 리다이렉트로 연결

## 아직 서버 연결이 필요한 기능

- 회원가입·로그인과 의료 면허·자격 검증
- 공고·상담·대기자 데이터의 서버 저장 및 운영자 백오피스
- 이력서·병원 서류 업로드와 접근권한 관리
- 실제 카드 결제, 웹훅 검증, 환불, 자동 권한 발급
- 후보자 동의 기반 병원 소개와 연락처 공개
- 직군별 인증 커뮤니티와 신고·운영 정책
- 알림, 분석, 광고 성과 리포트

## 현재 출시 원칙

- 기본 검색과 공개 요약은 무료로 제공합니다.
- 의사결정에 필요한 상세 급여, 비공개 정보, 검증 인재 연결은 유료 권한으로 설계합니다.
- 의료인의 개인정보는 상품처럼 판매하지 않으며 명시적 동의 후 필요한 범위만 전달합니다.
- 실제 공급이 없는 직군은 가짜 공고 대신 파트너 모집과 오픈 알림으로 표시합니다.

## 다음 구현 우선순위

1. 상담·공고·오픈 알림을 저장하고 처리할 운영 백오피스
2. 의료인·병원 계정과 자격·기관 인증
3. PG 결제와 서버 기반 열람 권한
4. 간호 직군의 실제 공고·파트너 확보와 전용관 개방
5. 방사선·임상병리 직군 전용관과 인증 커뮤니티

## 문서 갱신 규칙

기능 작업이 완료될 때마다 Sites 공개 배포와 GitHub 푸시를 함께 진행하고, 이 문서와 CHANGELOG.md의 구현 내용·다음 우선순위를 동시에 갱신합니다. GitHub Pages는 `main` 푸시 후 자동 배포되며, Sites는 검증된 같은 커밋을 별도로 저장·배포합니다. 로컬 변경을 실시간 자동 푸시하지 않고 작업 완료와 검증 후 의도적으로 커밋합니다. 사업 정책이나 데이터 구조가 바뀌면 `QUICK_REF.md`, `ROADMAP.md`, `DB.md`, 관련 상세 설계 문서도 함께 수정합니다.

## 최근 완료된 디자인·품질 개선

- 광고센터 상단의 기능 3칸을 노출 순위 미리보기·추천 광고 신청 CTA로 교체
- 의료인·병원 양방향 `메디헬퍼스 매칭 리포트`: 찜 목록 2~3개 선택, 중요 조건 강조, 조건별 비교, 협의 필요 항목, 상담 내용 자동 첨부
- 매칭 리포트·멤버십 페이지를 1,500px 데스크톱 폭과 모바일 한 열 구조에 맞춰 확대하고 본문·카드·입력 폰트 통일
- 광고 카드 브랜드 영역을 정사각형 로고형과 가로 배너형으로 자동 구분하고 비율 유지·배경 확장·모바일 크롭 적용
- 홈 검색의 Windows 기본 선택창을 넉넉한 맞춤 목록으로 교체하고 터치 지원 PC 마우스 감지·IntersectionObserver 대체·모션 강제 활성화 안내 추가
- 프리미엄 광고 세션별 균형 순환 및 자동 전환 UI 완료
- 데스크톱 2개·모바일 1개 반응형 노출, 수동 탐색·일시정지·모션 축소 대응 완료
- 홈 히어로 검색 컨트롤 폭·높이 재조정 및 내부 코드 대신 사용자용 직군 라벨 표시
- 회원가입 미리보기에 주민번호 미수집·휴대폰 본인확인 연결 UI와 중복가입 방지 방향 반영
- 홈 검색 CTA가 선택 필드 그리드 밖으로 돌출되던 데스크톱 최소 폭 충돌 수정
- 채용정보 메인에 베이직·추천·집중채용 3개 상품의 가격·노출 차이와 즉시 등록·결제 진입을 배치하고, 선택 상품 신청을 페이지 이동 없이 연결
- 채용정보의 진료과·지역 기본 선택창을 메인 검색과 동일한 맞춤 리스트박스·선택 체크·회전 화살표·위아래 자동 배치·모션 감소 대응으로 통일
- 공통 맞춤 선택 컴포넌트를 홈·채용정보·인재·광고 신청·매칭 상담에 공유하고 방향키·Home·End·Escape·포커스 복귀, 버튼 포커스·비활성 상태와 모바일 동작을 동일하게 정리
- 채용정보를 집중채용→추천공고→베이직 순으로 분리하고 각 등급 제목 옆 즉시 광고 등록 버튼 배치, 상품 비교 컨테이너 최하단 이동, DEMO 가상 광고 6건 추가와 등급별 2페이지 자동 순환 검증
- 메디헬퍼스 전용 수호천사 캐릭터 ‘메디’를 제작하고 채용공고·매칭 리포트·1:1 헤드헌터·전화 상담으로 연결되는 반응형 안내 도우미 적용
- 채용공고 상세 모달에 카드에서 이어지는 깊이감 있는 열림·닫힘, 정보 순차 등장, 등급 색상 포인트, 모바일 바텀시트와 모션 감소 대응 적용
- Sites 배포 서버에 메디 펫 PNG 응답 경로를 명시적으로 추가해 깨진 이미지 수정, 광고 자동 순환은 유지하면서 하단 진행 바 애니메이션 제거
- 집중채용은 대형 브랜드 쇼케이스·최상단 노출 배지·강한 깊이감, 추천공고는 압축된 큐레이션 카드·맞춤 추천 배지·절제된 호버로 등급별 크기와 디자인 차등 적용
- 로고 등록 카드의 병원명이 워드마크 카드보다 작아지는 선택자 충돌을 수정하고 등급별 동일 타이포 위계·긴 병원명 반응형 줄바꿈 적용
- 권한별 QA 미리보기 `/qa-preview` 추가: 비로그인·관리자·병원·의료인·멤버십 5개 상태 전환, 페이지 간 상태 유지, 공고 정보 잠금·구독·관리 진입 차등 표시, 테스트 전용 고정 표식 적용
- 메인 공고 클릭을 페이지 이동 없는 상세 모달로 변경하고, 프리미엄 공고에 3:1 병원 배너 우선 노출·배너 미등록 시 병원 성격 기반 자동 무드 워드마크·광고 신청 규격 검증을 적용
