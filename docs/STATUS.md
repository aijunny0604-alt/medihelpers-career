# 현재 구현 상태

## 2026-08-15 전체 기능·권한·배포 안전성 재검증

- 공개 화면 19개, SEO 정적 응답 3개, 샘플 채용 배너 6개를 운영 주소에서 실제 요청했습니다. 모두 의도한 HTTP `200`과 콘텐츠 형식을 반환했고, 주요 화면에서 깨진 이미지·가로 넘침·`???`/대체문자·인증 로딩 잔여 요소가 발견되지 않았습니다.
- 비회원, 의료인, 병원, 관리자 세션을 각각 분리해 `/api/account`, `/api/resumes`, `/api/member-center`, `/api/admin-console`, 미등록 API의 성공·`401`·`403`·`404` 권한 경계를 확인했습니다. 테스트 계정 전환 뒤 D1 역할과 관리자 권한도 정확히 유지됐습니다.
- 브라우저에서 의료인 주요 10개 경로, 병원 주요 6개 경로, 관리자 읽기 전용 콘솔, 채용공고·맞춤 헤드헌팅 독립 상세 페이지, 로그인·계정 찾기, 병원 가입 주소 검색 모달을 직접 확인했습니다. 병원회원의 의료인 전용 화면에는 재로그인·재가입 유도가 없고, 역할 전환 후 이전 대시보드가 남지 않았습니다.
- 주요 공개 화면 9개에서 이름 없는 버튼·링크·입력 필드·대체텍스트 없는 이미지가 모두 `0개`, 문서당 `h1`은 1개, 가로 넘침은 없음을 확인했습니다. 카카오 주소 검색도 차단 화면 없이 실제 검색 UI가 표시됩니다.
- 배포 의존성 점검에서 발견된 알려진 취약점 4건을 호환되는 최신 잠금 버전(`vinext 1.0.0-beta.6`, `vite 8.2.1`, `postcss 8.5.26`, `nanoid 3.3.18`)으로 갱신했습니다. `npm audit --omit=dev` 결과는 **0건**입니다.
- Node 자동 테스트 **144/144**, 프로덕션 빌드, 생성 Worker 문법 검사, diff 검사를 통과했습니다. 빌드의 단일 JavaScript 청크 크기 경고는 기능 장애는 아니지만 후속 성능 최적화 항목으로 남겨 둡니다.
- 과거 D1 테스트 원문 일부의 `??` 데이터는 복구 가능한 소스 버그가 아니라 이미 저장된 레거시 데이터라 보존했습니다. Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩과 D1 스키마는 변경하지 않았습니다.

## 2026-08-15 테스트 계정 전환 복구

- 상단의 일반회원·관리자·병원회원 전환은 더 이상 현재 페이지를 통째로 다시 불러오지 않습니다. 전환 API가 반환한 역할을 공용 인증 상태에 즉시 반영하고, 전체 계정·프로필은 화면을 비우지 않은 채 백그라운드에서 재확인합니다.
- 인증 재확인 요청마다 순번을 부여해 오래 걸린 이전 `/api/account` 응답이 새로 선택한 역할을 덮어쓰지 못하도록 했습니다.
- 마이페이지도 공용 역할 상태를 전달받아 전환 즉시 이전 역할의 프로필·대시보드 데이터를 비우고 새 역할의 `/api/member-center` 응답을 다시 읽습니다. 병원 대시보드에서 일반회원으로 전환했을 때 의료인 대시보드로 바뀌고 병원 화면이 남지 않는지 확인했습니다.
- 실제 버튼 클릭으로 병원회원 → 관리자 → 일반회원 순서 전환을 확인했고, 세 경우 모두 같은 URL을 유지하면서 선택 버튼 `aria-pressed=true`, 잔여 인증 자리 `0개`를 확인했습니다.
- `?slow-auth=1`의 500ms 지연 상태에서도 병원회원 역할이 클릭 직후와 재확인 완료 후 모두 유지됐습니다.
- Node 자동 테스트 **144/144**, 프로덕션 빌드와 생성 Worker 문법 검사를 통과했습니다. 기존 D1 계정·세션 구조와 Sites 바인딩은 변경하지 않았습니다.

## 2026-08-15 운영 사이트 직접 검증 및 문서 동기화

- 검증 대상은 공개 운영 주소 `https://medihelpers-career.junnyai.chatgpt.site`, Sites version `279`, 애플리케이션 커밋 `868ca62`입니다.
- 실제 일반회원 세션으로 `/resume`, `/medical-staff`, `/jobs`, `/advertise`, `/mypage`, `/request/hiring`을 차례로 열어 화면 제목·핵심 콘텐츠와 권한 안내를 확인했습니다. 모든 경로에서 인증 완료 후 `.auth-action-pending`은 `0개`였고 잘못된 역할 안내는 나타나지 않았습니다.
- 운영 `/api/auth/test-switch`와 별도 쿠키 세션으로 의료인·병원·관리자 전환을 각각 실행했고 모두 HTTP `200`을 받았습니다. 이어서 `/api/account`가 D1 계정의 `doctor`, `hospital`, 관리자 권한을 각각 정확히 반환하는지 확인했습니다.
- 보호 API는 의료인 `/api/resumes`, 병원 `/api/member-center`, 관리자 `/api/admin-console`에서 각각 HTTP `200`을 확인했습니다. 비회원 `/api/account`는 HTTP `200` JSON, `/api/resumes`는 의도된 HTTP `401` JSON을 반환합니다.
- 운영 응답 시간은 계정 전환 약 `1.25~1.33초`, 계정 조회 약 `0.96~0.98초`, 의료인 이력서 약 `0.58초`, 병원 회원센터 약 `0.93초`였습니다. 관리자 전체 기록 조회는 데이터량이 커 약 `3.74초`였습니다.
- 알려진 운영 데이터 항목: 2026-08-08에 생성된 일부 과거 테스트 지원·알림 원문에는 이미 저장된 `??` 문자가 남아 있습니다. 현재 요청·응답 인코딩과 역할 판정 오류는 아니며, 원본 보존을 위해 이번 문서 동기화에서 D1 기록을 수정하거나 삭제하지 않았습니다.
- Node 자동 테스트 **142/142**, 프로덕션 빌드와 생성 Worker 문법 검사를 다시 통과했습니다. Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-15 전 화면 인증 UI 깜빡임 전수조사

- 앱의 공용 `/api/account` 조회 결과에 계정·프로필·이메일·메일 기능 상태까지 포함하고, 공고 상세·의료인 채용 상세·이력서·헤드헌팅 접수·상담·결제·로그인·가입 축하 화면이 이 상태를 그대로 재사용합니다.
- 헤더의 알림·마이페이지·로그아웃 묶음, 병원 채용의 추천/기본 공고 등록, 광고 상품, 하단 등록 도크는 권한 확인 전 실제 버튼 대신 같은 크기의 중립 자리만 표시합니다.
- 관리자 콘솔은 운영 DB 응답 전 데모 수치와 버튼을 노출하지 않고 전용 로딩 화면을 표시합니다.
- 로컬 QA에서 `?slow-auth=1`로 500ms 지연을 재현했으며 응답 전후 헤더와 공고 버튼 폭 변화는 각각 `0px`, 역할별 화면의 잔여 로딩 자리 요소는 `0개`로 확인했습니다.
- 비회원·의료인·병원·관리자 주요 경로 브라우저 점검과 Node 자동 테스트 **142/142**, 프로덕션 빌드, 생성 Worker 문법 검사를 통과했습니다. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-15 회원 역할 버튼 안정화

- 로그인 여부와 회원 역할은 앱의 공용 계정 조회 한 번으로 확정하며, 하위 페이지에서 `/api/account`를 다시 조회하지 않습니다.
- 역할 확인이 끝나기 전에는 비회원용 가입·로그인 CTA를 노출하지 않아 버튼이 잠시 나타났다 사라지는 현상이 없습니다.
- 병원회원의 `/medical-staff`에는 의료인 등록 버튼을 노출하지 않고 병원 인재 채용 상담만 표시합니다. 의료인회원은 이력서·구직글 등록과 이직 상담을 이용합니다.
- 헤더, 모바일 계정 메뉴·하단 바로가기, 병원 채용·광고 등록 버튼도 같은 역할 확인 완료 시점을 사용합니다.
- Node 자동 테스트 **140/140**를 통과했습니다. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-15 회원 역할 불일치 화면 정리

- 병원회원이 `/resume`처럼 의료인 전용 화면에 접근해도 로그인·회원가입 버튼을 다시 보여주지 않습니다.
- 역할이 맞지 않는 로그인 계정에는 보호된 폼이나 API 의존 콘텐츠를 렌더링하지 않고 전용 안내 페이지만 표시합니다.
- 안내 페이지의 `뒤로 가기`는 같은 사이트의 이전 페이지로 돌아가며, 방문 기록이 없으면 의료인 전용 화면은 병원 채용 목록으로 이동합니다.
- 실제 비회원은 로그인 버튼과 보호 화면 미리보기를 유지하며, 의료인회원·관리자는 기존 권한대로 화면을 이용합니다.
- Node 자동 테스트 **137/137**를 통과했습니다. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-15 채용공고 등록 화면 가독성 확대

- 병원 채용공고 등록 화면의 본문·라벨·입력값·섹션 제목을 크게 하고 입력창을 최소 60px 높이로 확장했습니다.
- 브랜드 이미지 업로드와 샘플 배너 선택 카드, 결제 요약과 등록 버튼까지 같은 시각 크기로 통일했습니다.
- 데스크톱은 넓은 여백과 큰 컨트롤을 사용하고 태블릿·모바일은 한 열 반응형으로 접혀 가로 스크롤이 생기지 않습니다.
- Node 자동 테스트 **135/135**와 프로덕션 빌드·생성 Worker 문법 검사가 통과했습니다. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-15 병원 채용 페이지 공고 등록 동선 간소화

- `/jobs` 히어로 아래의 중복 이동 메뉴를 제거하고 `채용정보` 제목과 `공고 등록` 행동만 남겼습니다.
- 공고 등록 버튼은 병원회원에게 기존 광고 등록 흐름을 열고, 비로그인 사용자는 병원회원 가입을 거쳐 같은 흐름으로 복귀합니다.
- Node 자동 테스트 **134/134**와 프로덕션 빌드·생성 Worker 문법 검사가 통과했습니다. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-15 채용공고 조건 입력 간소화

- 병원 채용공고 등록의 채용조건은 `초빙 분야(필수)`, `급여·보수`, `근무 일정`, `추가 안내` 네 항목만 사용합니다.
- 상세조건을 열고 열 개가 넘는 항목을 각각 입력하던 UI를 제거해 한 화면에서 빠르게 작성할 수 있습니다.
- 당직·휴무·인센티브 등 꼭 필요한 추가 조건은 자유 입력란 하나에 함께 적습니다.
- 병원회원의 본인 공고 수정 화면도 동일한 구조이며, 과거 공고에 저장된 숨은 세부 값은 서버 전송 시 보존됩니다.
- Node 자동 테스트 **133/133**와 프로덕션 빌드·생성 Worker 문법 검사가 통과했습니다. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-14 회원가입 축하·이메일 알림

- 가입 성공 후 `/signup/welcome` 전용 페이지에서 가입자 이름, 회원 유형, 로그인 이메일과 역할별 다음 작업을 보여줍니다.
- 가입자 축하 메일은 회원가입 때 입력한 이메일로, 신규 가입 관리자 알림은 `ALERT_EMAIL_TO`에 등록한 대표자·관리자 이메일로 발송합니다.
- 이메일 발송은 계정과 세션 저장 이후 백그라운드에서 각각 실행하므로 Resend 장애나 한쪽 수신자 오류가 가입 성공을 되돌리지 않습니다.
- 비밀번호와 인증정보는 어떤 가입 알림 메일에도 넣지 않습니다.
- 2026-08-14 운영 Sites에는 `RESEND_API_KEY`, `RESEND_FROM`이 아직 없어 실제 발송은 두 시크릿을 등록한 뒤 활성화됩니다. 현재 화면은 메일 발송 성공 대신 가입 저장 완료와 설정 대기 상태를 정확히 표시합니다.
- Node 자동 테스트 **132/132** 통과. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-14 이메일 계정 찾기·비밀번호 재설정

- 아이디 찾기는 회원가입 때 저장한 이름·휴대전화와 일치하는 계정이 있으면 해당 가입 이메일로 로그인 이메일을 안내합니다.
- 비밀번호 찾기는 등록 이메일로 30분 동안 한 번 사용할 수 있는 재설정 링크를 발송합니다. 링크 원문은 DB에 저장하지 않고 SHA-256 해시·만료·사용·발송 상태만 저장합니다.
- 새 비밀번호 저장은 PBKDF2-SHA-256(100,000회)과 새 salt를 사용하며, 완료 즉시 기존 로그인 세션과 남은 링크를 무효화합니다.
- 존재하지 않는 계정도 공개 응답과 화면이 동일하게 보여 계정 유무·접수번호·발송 대상을 노출하지 않습니다.
- 서버 이메일 발송은 Resend를 사용합니다. 2026-08-14 확인 기준 운영 Sites에는 `RESEND_API_KEY`, `RESEND_FROM`이 아직 없어 코드 배포 후에도 실제 발송은 두 값을 등록해야 활성화됩니다. 미설정 상태에서는 화면이 요청 접수만 완료됐다고 정확히 안내합니다.

## 2026-08-11 병원 작성 공고 직접 수정

- 병원회원은 마이페이지 `내 공고`에서 본인이 결제해 등록한 공고를 선택해 독립 페이지에서 수정할 수 있습니다.
- 수정 가능 항목은 제목, 병원명·주소·홈페이지, 진료과·급여·근무표·지원인력·휴무·면접 절차·소개, 로고와 배너입니다.
- 이미지 교체는 파일 선택, 드래그앤드롭, 클립보드 붙여넣기를 모두 지원하며 기존 R2 업로드 경로를 사용합니다.
- `/api/member-center`의 `owned_ad_update`는 병원 역할, 주문 `account_id`, 광고 주문 상태, 연결된 공고 유형을 서버에서 검증합니다.
- 결제금액·상품등급·노출기간은 수정 입력을 받지 않고 기존 D1 payload 값을 보존합니다. 다른 계정의 공고 ID를 직접 호출해도 수정할 수 없습니다.
- 의료인 구직글은 기존처럼 본인 소유 이력서를 불러와 수정합니다.
- Node 자동 테스트 **125/125**, 프로덕션 빌드와 생성 Worker 문법 검사가 통과했습니다. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩 및 스키마는 변경하지 않았습니다.

## 2026-08-11 로그인·테스트 계정 전환 이동 통일

- 상단 테스트 계정 전환은 더 이상 일반회원과 병원회원을 마이페이지로 강제 이동시키지 않습니다.
- 메인·공고·인재·헤드헌팅 화면에서 전환하면 현재 화면과 검색 조건을 그대로 유지하고 새 세션만 반영합니다.
- 로그인·회원가입 화면에서 바로 전환했을 때는 인증 화면에 머물지 않고 메인으로 이동합니다.
- Node 자동 테스트 **120/120** 통과. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-10 관리자 DB 기록 조회 전용

- 관리자는 홈페이지 콘텐츠와 회원·결제를 직접 처리하지 않고 자동 저장된 D1·R2 운영 기록만 조회합니다.
- 관리자 콘솔에서 회원 상태, 공고, 설정, 기능 공개, 결제 상태, 환불 요청을 승인·수정·삭제하는 UI를 제거했습니다.
- 관리자 전용 상담·채용 CRM·백업 경로도 동일한 읽기 전용 콘솔로 연결하며, 관련 서버 API는 `GET` 이외 요청을 `405`로 차단합니다.
- 병원 광고는 병원회원이 직접 결제하고 결제 성공 즉시 공개됩니다. 관리자 계정은 광고·인재 열람권 구매 흐름에 들어가지 않습니다.
- 회원가입·이력서·지원·알림·결제·환불 요청·자동 백업 기록은 기존 D1/R2에 계속 저장됩니다.
- Node 자동 테스트 **119/119**, 프로덕션 빌드와 생성 Worker 문법 검사가 통과했습니다. 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-10 로그인 후 원래 화면 복귀

- 일반·병원회원 로그인 성공 시 마이페이지로 강제 이동하지 않고 로그인 전 이용하던 화면으로 돌아갑니다.
- 이전 화면이 없는 직접 로그인은 메인으로, 관리자 직접 로그인은 운영 콘솔로 이동합니다.
- 헤더 로그인 버튼이 현재 경로와 검색 조건을 안전한 `next` 값으로 전달하며 외부 주소 리디렉션은 차단합니다.
- Node 자동 테스트 **118/118** 통과. 기존 Sites project_id와 D1·R2 바인딩은 변경하지 않았습니다.

## 2026-08-10 의료인 구인구직 히어로 간소화

- 의료인 구인구직 첫 화면 오른쪽의 열람권 안내 카드를 제거하고 제목 중심의 단일 컬럼 히어로로 변경했습니다.
- 제목을 데스크톱에서 최대 104px까지 확장하고 모바일용 크기·줄 간격을 별도로 지정했습니다.
- 히어로 아래 구직 게시판이 더 자연스럽게 이어지도록 세로 간격을 줄였습니다.
- Node 자동 테스트 **116/116** 통과. 기존 Sites project_id와 D1·R2 바인딩은 변경하지 않았습니다.

## 2026-08-10 구직글 등록 버튼 재배치

- 구직 게시판 제목 오른쪽의 어정쩡한 위치에 있던 `구직글 등록` 버튼을 탭·검색 도구 모음 오른쪽으로 옮겼습니다.
- 데스크톱에서는 검색창과 한 줄로 정렬하고, 태블릿에서는 도구 모음 폭을 사용하며, 모바일에서는 검색창 위의 전체 폭 버튼으로 배치합니다.
- 의료인회원의 이력서 등록 경로와 비회원의 의료인 가입 경로는 유지하고 병원회원에게는 버튼을 노출하지 않습니다.
- Node 자동 테스트 **115/115** 통과. 기존 Sites project_id와 D1·R2 바인딩은 변경하지 않았습니다.

## 2026-08-10 채용공고 의료인 전용 안내

- 로그인한 병원회원이 채용공고 상세조건을 열었을 때 재로그인·재가입을 유도하던 버튼을 제거했습니다.
- 병원회원에게는 `상세조건은 의료인 회원만 볼 수 있습니다` 안내만 표시하며, 직접 지원·문의 버튼도 `의료인 회원만 지원 가능` 상태로 바뀝니다.
- 비회원에게는 기존 의료인 회원 로그인·가입 경로를 유지하고, 의료인회원과 관리자는 상세조건을 그대로 열람합니다.
- 계정 확인 중에는 가입 버튼을 먼저 노출하지 않아 역할 판별 전 화면 깜빡임을 방지합니다.
- Node 자동 테스트 **114/114** 통과. 기존 Sites project_id와 D1·R2 바인딩은 변경하지 않았습니다.

## 2026-08-10 문의 메시지 깨짐 방어

- 과거 저장 과정에서 `????`, `○○○○`, 유니코드 대체문자처럼 내용이 유실된 문의 메시지가 정상 대화 말풍선으로 노출되던 문제를 수정했습니다.
- 회원센터 화면과 `/api/member-center` 응답 양쪽에서 복구 불가능한 자리표시자 메시지를 제외합니다.
- `김○○`처럼 실제 이름 일부를 가린 정상 표기는 유지하며, 새로 작성한 메시지의 D1 저장·상대 회원 알림 흐름은 변경하지 않았습니다.
- 기존 Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩과 스키마는 변경하지 않았습니다.
- Node 자동 테스트 **112/112** 통과.

## 2026-08-09 광고 상품 2단계 단순화

- 병원 광고 상품을 `기본 공고 59,000원/30일`과 `메인 추천 공고 149,000원/30일` 두 단계로 정리했습니다.
- 기존 `집중 채용`과 `추천 공고`는 공개 화면에서 하나의 `메인 추천 공고`로 통합했습니다. 과거 주문·환불 기록은 유지하고, 기존 `spotlight` 공고는 `featured`로 안전하게 정규화합니다.
- 후보 발굴, 전담 컨설턴트, 익명 인재풀 추천은 광고 상품에서 제거하고 별도 맞춤 헤드헌팅 서비스로 분리했습니다.
- 신규 `intensive` 주문은 서버에서 차단하며, 기존 Sites project_id와 D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.
- Node 자동 테스트 **109/109** 통과.

## 2026-08-09 공개 화면 사전 검수 표현 제거

- 메인 집중채용의 `확인된 채용정보`, 공고·헤드헌팅 상세의 확인 배지, 광고센터의 담당자 확인 후 게시 표현을 제거했습니다.
- 광고센터는 상품 선택과 결제 완료 후 바로 공개되는 실제 흐름만 안내합니다.
- 공고와 이미지 권리에 대한 책임은 등록 병원에 있고, 운영정책 위반 시 사후 숨김·삭제될 수 있다는 운영 안내만 유지합니다.

## 2026-08-09 회원 유형 자동 분류·병원 광고 즉시 게시·하단 정리

- 회원 유형은 회원가입 때 `doctor` 또는 `hospital`로 D1 `accounts.role`에 저장하며, 로그인은 서버가 저장된 역할을 조회해 자동 분류합니다. 로그인 화면에서 유형을 다시 선택하지 않습니다.
- D1 역할 제약, 가입 INSERT, 로그인 JOIN과 역할별 이동을 자동 테스트로 고정했습니다.
- 병원 광고의 관리자 사전 검수·승인·반려 단계를 제거했습니다. 주문 생성 중에는 결제 대기 `draft`, 결제 성공 즉시 `paid`와 `published`로 전환합니다.
- Sites 가상결제도 주문 생성 직후 승인 API를 호출하며, 과거 결제 완료 `draft` 광고는 공개 데이터 또는 병원 마이페이지 조회 시 자동 복구합니다.
- 관리자 콘솔의 공고 승인·반려 버튼과 공개 화면의 검수 표현을 제거했습니다.
- 홈페이지 하단의 의사·병원·안내 링크 열을 제거하고 브랜드·연락처·사업자 하단 정보만 유지했습니다.
- 기존 Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.
- Node 자동 테스트 **106/106** 통과.

## 2026-08-08 전체 기능·로직·운영 연동 전수조사

- 자동 테스트 **98/98**, 프로덕션 빌드, 생성 Worker 문법 검사와 `git diff --check`가 통과했습니다.
- 비회원 공개 화면과 공개 API, 의료인·병원·관리자 테스트 세션의 계정·회원센터·역할별 권한을 운영 Sites에서 실제 요청으로 확인했습니다.
- 의료인 관리자 접근 403, 병원 이력서 접근 403, 병원 무료 공고 생성 403, 외부 Origin 상태 변경 403으로 서버 권한 경계를 확인했습니다.
- 병원 로고 PNG를 운영 R2에 실제 저장(201)하고 다시 조회(200 `image/png`)했으며 SVG는 415로 차단했습니다.
- 인재 열람권 가상 주문·승인·권한 발급을 확인했습니다. `testMode:true`로 실제 청구는 없었습니다.
- 관리자 콘솔·상담·CRM·백업 목록과 데이터 보호 상태를 확인했습니다. 최근 백업 `2026-08-08 00:15:20`, 최근 보존 정리 `2026-08-08 00:15:14`, 보존기간 35일입니다.
- 의료인 마이페이지의 이력서 카드가 병원용 문의로 잘못 연결되던 문제를 `/resume` 수정 동선으로 고쳤습니다.
- `/resume` 수정 화면이 기존 D1 이력서를 불러오지 않아 빈 양식이 되던 문제를 고쳐 최신 저장본과 사진·상세 필드를 채웁니다. `?new=1`은 새 이력서 작성으로 유지합니다.
- `.openai/hosting.json`의 project_id와 D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-08 이미지 업로드·회원 알림

- 병원 공고의 로고·배너·시설 사진, 관리자 공고 이미지, 이력서 프로필 사진은 선택·드래그앤드롭·클립보드 붙여넣기를 지원합니다.
- 지원서 선택 영역에서 새 이력서 작성과 기존 이력서 관리를 나란히 제공합니다.
- `member_notifications` D1 테이블에 계정별 알림, 이동 경로, 읽은 시각을 보존합니다. 런타임 `ensureMemberCenterSchema`가 자동 생성하므로 수동 마이그레이션은 필요하지 않습니다.
- 병원 유료광고 직접 지원은 지원서·병원 활동·병원 알림을 D1 batch로 함께 기록합니다.
- 병원과 지원 의료인의 직접 메시지 및 헤드헌터 상담 업데이트는 상대 회원 알림함에 저장됩니다.
- 상단 종 아이콘, 마이페이지 사이드바 배지, 홈 알림 배너와 전용 알림 탭에서 읽지 않은 건을 확인할 수 있습니다.
- 기존 Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩은 그대로 유지합니다.
- 자동 테스트 97건과 프로덕션 빌드 통과.

## 2026-08-08 문의 전달 주체 분리

- 아빠(의사 헤드헌터)가 검증·게시한 맞춤 헤드헌팅 공고의 문의만 관리자 상담함과 헤드헌터 이메일·문자 알림으로 전달됩니다.
- 병원이 결제해 올린 `/jobs` 유료 채용광고 지원은 공고 등록 병원 마이페이지로만 전달되며 헤드헌터에게 전달되지 않습니다.
- 서버가 접수 출처를 `submissionChannel`로 확정하고, 맞춤 헤드헌팅 게시글 정보 또는 병원 유료광고 정보와 선택 이력서 사본을 D1에 저장합니다.
- 기존 D1 스키마·`DB` 바인딩·R2 `BACKUPS` 바인딩을 변경하거나 재생성하지 않았습니다.
- 자동 테스트 93건과 프로덕션 빌드가 통과했습니다.
- GitHub 기능 커밋 `1b7e2c4`, OpenAI Sites v246 배포 완료. 운영 상세 URL과 API 응답을 확인했습니다.

## 2026-08-08 마이페이지 위젯·맞춤 헤드헌팅 상세 페이지

- 병원·의료인 마이페이지 활동 요약 위젯을 클릭하면 공고, 문의·상담, 추천 후보, 이력서·관심 공고, 결제 상세 탭으로 이동합니다.
- 병원 추천 후보 위젯은 후보 동의가 확인된 추천 건의 진행 단계 목록으로 연결됩니다.
- 맞춤 헤드헌팅 게시판의 초빙공고는 모달 대신 `/headhunting/posts/{공고ID}` 독립 페이지로 엽니다.
- 상세 페이지는 목록 복귀, 공유 가능한 고유 URL, 전화 상담과 온라인 초빙 의뢰를 지원합니다.
- 자동 테스트 92건과 프로덕션 빌드가 통과했습니다.
- GitHub 커밋 `63e4d27`, OpenAI Sites v245 배포 완료. 기존 project_id, D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 2026-08-08 채용공고 상세 페이지

- 채용공고 상세는 모달이 아닌 `/jobs/{공고ID}` 독립 페이지로 제공합니다.
- 홈과 채용 목록의 모든 공고 카드가 동일한 상세 페이지 경로를 사용합니다.
- 목록 복귀, 공유 URL, 저장과 지원 동작을 지원합니다.

## 2026-08-08 상세 모달 기관 아이콘

- 상세 모달은 가로형 배너와 정사각형 기관 표식을 서로 다른 자산으로 취급합니다.
- 정식 로고가 없거나 배너와 같은 파일이면 메디헬퍼스 기관 심볼을 사용합니다.

## 2026-08-08 집중채용 카드 글자 크기

- 집중채용 카드의 핵심 공고 정보는 데스크톱에서 13~21px 범위로 확대 표시합니다.
- 모바일에서는 카드 너비에 맞춰 12.5~18px로 조정됩니다.

## 2026-08-08 공고 상세 모달 배너

- 배너가 있는 공고 상세 모달은 병원 정보와 배너를 분리한 반응형 2열 헤더를 사용합니다.
- 배너 안의 병원명·진료과는 실제 공고 데이터로 표시되고 작은 화면에서는 1열로 전환됩니다.

## 2026-08-08 선택형 샘플 채용 배너

- 병원 공고 등록 화면에서 직접 이미지 업로드와 6종 샘플 배너 선택을 지원합니다.
- 샘플 배너는 모두 1500×500px(3:1)이며 카드와 상세 화면에서 같은 비율로 표시됩니다.
- 선택한 샘플은 결제 주문 메타데이터의 `banner`에 저장되고 공개 공고 레코드로 전달됩니다.
- 상세 공고에서는 배너를 헤더 오른쪽에 표시하며 모바일에서는 전체 너비 반응형으로 표시합니다.
- 기존 D1·R2 바인딩 및 스키마는 변경하지 않았습니다.

기준일: 2026-08-08

## 집중채용 그리드 노출 확대

- 집중채용 한 묶음 노출 수: 데스크톱 6개 / 태블릿 4개 / 모바일 2개
- 광고 등급 우선순위와 진료과·지역 균형 정렬은 그대로 유지합니다.
- 묶음보다 공고가 많을 때 자동 순환과 이전·다음 탐색을 제공합니다.

## 병원 채용공고 / 맞춤 헤드헌팅 데이터 분리

- 병원 광고 주문으로 생성된 `doctor_job`/`medical_job`은 병원 채용 영역에서만 노출됩니다.
- 맞춤 헤드헌팅 게시판용 콘텐츠는 payload의 `publicationChannel='headhunt_board'`로 구분합니다.
- 헤드헌팅 공개 목록과 관리자 게시판 모두 전용 채널만 조회하므로 결제 채용광고가 섞이지 않습니다.
- D1 스키마·데이터 바인딩 변경 없이 payload 구분값으로 호환 처리했습니다.

## 2026-08-08 채용 배너 실규격 적용

- 프리미엄 채용 공고용 분야별 배너 7종을 1500×500px(3:1) JPEG로 제작·최적화했습니다.
- 정적 프리미엄 공고 8개 모두 `cardBanner`를 갖고 있으며, 공고 카드에서는 배너가 병원 로고보다 우선 노출됩니다.
- 배너는 카드 영역 전체를 `cover`로 채우고 병원명은 HTML 오버레이로 표시해 이미지 안 한글 왜곡을 방지합니다.
- Sites 패키지 Worker가 `/banners/recruit-*-v1.jpg`를 `image/jpeg`와 immutable 캐시로 제공합니다.
- 자동 테스트 80건과 프로덕션 빌드가 통과했습니다.
- 브라우저 검사 결과: 데스크톱 426×142, 모바일 288×96으로 3:1 유지, 3개 순환 묶음 8개 공고 이미지 로드 성공, 390px 가로 넘침 없음.
- 운영 D1 승인 공고 `124124 아무 의사나 초빙`에도 배너를 연결해 운영 노출 총 9개를 확인했고, Sites v234로 배포했습니다.

이전 기준일: 2026-08-03

## 2026-08-03 병원 광고 승인·노출 연동 안정화 (역사 기록, 2026-08-09 즉시 게시로 대체)

- 당시에는 결제 상태와 공고 게시 상태를 분리하고 관리자 게시 승인을 사용했습니다. 이 단계는 2026-08-09 제거됐으며 현재는 결제 성공 즉시 `published`로 전환합니다.
- 광고 상품을 공개 공고 데이터에 자동 연결합니다. `집중 채용`은 `spotlight`, `추천 공고`는 `featured`로 분류하며 베이직 공고는 일반 목록에 노출합니다.
- 결제 시 계산된 노출 시작일·종료일을 공고 payload에도 동기화해 프리미엄 배치와 기간 만료 필터가 같은 운영 데이터를 사용합니다.
- 기존 결제 공고도 관리자 콘솔 조회 시 등급·노출기간을 보정하되 제목·병원정보·관리자 편집값은 유지합니다.
- 운영 공고 `124124 아무 의사나 초빙`을 공개 승인하고, `집중 채용(spotlight)`·종료일 `2026-09-17`·프리미엄 첫 묶음 노출을 실제 공개 화면에서 확인했습니다.
- 병원회원 세션에서 회원센터·결제내역·관심공고 API가 HTTP 200으로 응답하고 최신 집중 채용 주문이 `paid`인 것을 확인했습니다.
- 자동 테스트 79건과 프로덕션 빌드가 통과했습니다.

## 2026-08-02 출시 전 안정화

- 계정 찾기·비밀번호 재설정 요청을 D1에 실제 저장하고 관리자 통합 모니터링에 연결했습니다. 현재 공개 화면은 내부 접수번호 대신 이메일 안내 요청 완료 여부만 표시합니다.
- 실제로 파일을 보관하지 않던 이력서 문서 첨부 UI를 제거했습니다. 입력형 이력서 데이터는 기존처럼 D1에 저장됩니다.
- 결제 완료 광고·관리자 공고·결제 트랜잭션·상담/CRM 참조를 운영 D1에서 대조해 연결 누락 0건을 확인했습니다.
- 테스트 계정 전환 시 기존 PBKDF2 해시를 불필요하게 다시 만들던 지연을 제거하고, 전환 직후 새 세션을 모든 화면이 확실히 읽도록 전체 페이지 로드로 통일했습니다.
- 회원 권한을 확인하는 동안 로그인 필요 화면이 잠깐 잘못 보이지 않도록 광고 신청·인재 열람 화면에 권한 확인 로딩 상태를 추가했습니다.
- `TEST_ACCOUNT_SWITCH_ENABLED=false`로 정식 출시 때 테스트 전환 UI와 API를 함께 닫을 수 있습니다.
- 관리자 전용 콘솔에도 자체 로그인 세션을 폐기하는 로그아웃 버튼을 추가했습니다.
- 검색엔진용 `robots.txt`, `sitemap.xml`, canonical URL, 사이트 매니페스트를 추가하고 관리자·회원·가입·API 경로는 색인에서 제외했습니다.
- 자동 테스트 78건과 프로덕션 빌드가 통과했습니다.

## 배포 현황 (배포처 2곳 — 내용이 다를 수 있음)

| 배포처 | 주소 | 기준 | 상태 |
| --- | --- | --- | --- |
| GitHub Pages (테스트 미러) | https://aijunny0604-alt.github.io/medihelpers-career/ | `main` 푸시 시 자동 배포(`.github/workflows/pages.yml`) | **최신**(2026-07-22 반영). 정식 오픈 전 테스트용이라 검색엔진 색인 차단 |
| OpenAI Sites (공개본) | https://medihelpers-career.junnyai.chatgpt.site | Codex에서 수동 배포 | **Sites v248**, 기능·문서 커밋 `ac7863d` 배포 성공 |

⚠️ 두 배포처가 **서로 다른 파이프라인**이라 한쪽만 반영되는 사고가 실제로 발생했다(히어로 영상이 Sites에는 있고 GitHub에는 없던 건). 변경 후 **양쪽 반영 여부를 각각 확인**할 것. `docs/CLOUDFLARE_MIGRATION.md`의 이전이 끝나면 배포처 단일화로 해소된다.

⚠️ GitHub Pages는 서버 API가 없는 **정적 미러**다. D1 런타임 기능은 동작하지 않고 안전한 준비 게이트가 표시된다. 기능 검증은 Sites/Cloudflare 배포본에서 해야 한다.

## 운영 중

- React·Vite SPA를 OpenAI Sites에 배포 (D1 런타임 API는 `scripts/package-sites.mjs`에 패키징)
- 홈·채용정보 공고 카드(등급별 색 통일, 제목·정보 가독성), 광고 공고는 급여 공개
- 의사 채용정보(/jobs, 병원 유료 광고 공고), 의료인 구인구직(/medical-staff, 구직 의료인 게시판+열람권), 맞춤 헤드헌팅(의사 초빙 정보란+상담)
- 일반/병원 회원가입, 역할별 마이페이지(프로필·알림·상담·결제·활동 D1 연동)
- 메디헬퍼스 자체 이메일·비밀번호 로그인(PBKDF2 해시, D1 세션, 보안 쿠키 — 일반 7일·관리자 12시간, 로그인 실패 잠금)
- **회원 전용 접근 게이트**: 의료인 구인구직·이력서를 비로그인 시 블러+로그인 유도. 옛 경로는 `ROUTE_ALIASES` 동기 정규화(`/talent`→`/medical-staff`, `/professions`→`/headhunting`, `/advertise/post`→`/advertise`). 매칭 리포트 제거됨
- **이력서**: 서버(D1) 저장 → 관리자 콘솔 '이력서 관리' 열람 → 마이페이지 완성도 지표
- **결제 라이프사이클**: 이니시스 웹표준결제 **연동 완료**(서버 서명·승인 API + 클라이언트 표준결제창 `src/inicisPay.js`, 광고·열람권 2개 지점). `INICIS_MID`/`INICIS_SIGN_KEY`/`SITE_ORIGIN` 환경변수만 넣으면 실결제 전환(코드 수정 불필요). 미설정 시 가상(테스트) 결제로 동작. ⚠️ 실결제 검증은 이니시스에 등록된 배포 도메인에서만 가능(localhost 불가),
  결제 완료→광고 등급·노출기간을 공고 payload에 동기화하고 즉시 공개, 환불/취소→노출 회수, 병원 '내 공고' 이용현황, 관리자 서비스현황
- **유료 상품은 2가지뿐 (2026-07-22)**: 병원 채용광고 + 병원 인재 열람권. **의사 대상 유료 멤버십은 폐지**됐고(`/membership`→`/jobs` 정규화), 의사·의료인은 전부 무료. 열람권은 병원 회원만 구매 가능(의사 결제 시 403 — 열람 권한이 병원에만 부여되므로)
- **인재 열람권(주 수익모델)**: 병원이 열람권 결제(단건 3,900/10명팩 29,000(건당 2,900)/30명팩 69,000원(건당 2,300), 30일. 팩은 크레딧 방식으로 새 인재를 열 때마다 1개 소모)→`talent_unlocks` 권한 기록→`GET /api/talent-detail/:id`가 해당 병원에만 이력서 연락처·상세 공개(감사 로그). 미결제 시 잠금+구매버튼
- **의료인 구직 노출(opt-in)**: 초간편 이력서(직군 자유입력·3단계) 등록 시 '구직 공개' 선택한 경우에만 익명 인재로 노출. 의사·간호·의료기사·약무 모두 의료인 구인구직(/medical-staff) 구직 게시판에 카드형으로 표시(진료과·지역 필터+검색). 병원은 열람권 결제 시에만 이름·연락처·상세 열람(결제 완료 후 `?open=코드`로 상세 자동 오픈). 자동노출·관리자승인 아님
- **병원 채용공고 = 유료 광고만**: 무료 직접게시 폐지(/advertise/post→/advertise 정규화). 병원은 광고 상품(베이직·추천·집중) 결제로만 /jobs 게시. 헤드헌터 채용의뢰(/request/hiring 상담)는 유지. 서버 `job_create`(POST /api/member-center) API도 403으로 차단 완료 — 클라 우회로 무료 공고를 만들 수 없음
- **병원 광고 신청 ↔ 결제 후 즉시 게시**: 광고 신청을 `payment_orders`와 `admin_content_records(draft)`에 함께 저장하고 결제 승인 성공 시 바로 `published`로 전환. 과거 결제 완료 후 `draft`에 남은 광고도 자동 복구
- **상단 테스트 역할 전환**: 테스트 기간에 일반회원·관리자·병원회원 원클릭 로그인 버튼을 헤더에 표시. 브라우저에 비밀번호를 넣지 않고 서버 전용 API가 예약 계정·역할·잠금·세션을 복구한다. 마이페이지/관리자 CTA는 큰 파란 버튼으로 강조하며 정식 오픈 전 테스트 전환 UI를 제거
- **다른 PC 로그인**: 회원 계정은 D1에 저장되어 같은 이메일·비밀번호로 다른 PC에서도 로그인 가능. PC별 세션은 독립적으로 유지되며 한 PC의 로그아웃은 다른 PC 세션을 종료하지 않음
- **계정 도움 요청**: 가입 이메일 확인·비밀번호 재설정 요청을 D1에 저장하고 관리자 통합 모니터링에서 확인. 2026-08-14부터 Resend 환경설정이 있으면 아이디 안내·재설정 링크를 자동 발송하며 공개 화면에는 내부 접수번호를 표시하지 않음
- **회원가입 간소화**: 휴대폰 인증(본인확인) 절차 제거 — 휴대폰은 연락용 일반 입력. 이메일+비밀번호 자체 로그인으로 본인 식별. '일반 회원' 명칭은 '의료인 회원'으로 통일(내부 role 키는 doctor 유지)
- **관심공고(찜)** 서버 저장(기기 간 동기화), **폼 자동 채움**(상담·결제·헤드헌팅에 회원정보 자동 입력)
- **급여·병원정보 유출방어**: 비인증자에게 공고 급여·식별정보를 서버에서 제거(클라 마스킹 아님)
- **인재 열람 방어**: 병원 일일 열람 상한(기본 30명)+단시간 폭주 탐지+감사 로그, 관리자 콘솔 '인재 열람 감사' 탭에서 병원별 열람량·이상 열람 확인(SECURITY_DATA_EXPOSURE_PLAN)
- 상담 접수 D1 저장, 관리자 상담함, 이메일(Resend)·문자(Solapi) 알림(키 설정 시 발송)
- 관리자 통합 콘솔, 채용 CRM(의뢰→후보→면접→입사), 병원 추천후보 마이페이지 연결

## 부분 구현 / 남은 작업

| 영역 | 현재 동작 | 운영 전 남은 작업 |
|---|---|---|
| 배포 | OpenAI Sites 공개본과 GitHub 작업 브랜치 `agent/medihelpers-ui-stability`를 전수검증 결과로 동기화. Pages는 서버 API가 없는 정적 미러 | Cloudflare 이전으로 배포처 단일화 |
| 결제 | 이니시스 연동 완료(승인·금액검증·멱등성·SSRF·소유자 인증). 팩=크레딧 방식. 키 미설정이라 테스트(가상) 결제 | **이니시스 MID/키 설정**, 웹훅 수신, Cloudflare 이전(Sites는 실결제 불가) |
| 알림 | 메일·문자 코드 완성 | **Resend/Solapi 키 환경변수 설정**(docs/NOTIFICATION_SETUP.md) |
| 회원가입 | 역할·동의 기록 | 승인된 약관 버전, 본인확인, 면허·병원 인증 |
| 공고·인재 데이터 | 관리자 D1 + 정적 data.js 병합 | 정적 샘플을 D1로 이전(단일 원장화). 실제 공고·인재 데이터 입력 |
| 이미지/파일 | **병원 배너·로고·시설 사진과 이력서 프로필 사진 R2 업로드 구현**(`/api/uploads`, 역할·소유권 검사, jpg·png·webp·gif·5MB). 프로필은 본인·관리자·유효 열람권 병원만 조회. Sites는 기존 `BACKUPS` R2를 사용하고 Cloudflare는 전용 `UPLOADS`를 우선 사용. 일반 이력서 문서 첨부는 제공하지 않음 | 업로드 악성 파일 검사. 운영 이전 시 전용 버킷 분리 여부 결정 |
| 법무 | 약관·개인정보 초안(현행이 구 사이트보다 최신) | **정식 오픈 전 법무·세무 검토** |

## 이번 최신화에서 확인·수정 (2026-07-30)

- **공고 상세 모달 통일**: 메인과 채용정보의 공고 카드를 클릭하면 다른 페이지로 이동하지 않고 현재 화면에서 상세 모달이 열립니다.
- **프리미엄 광고 자동 순환 연결**: 등급 우선순위와 진료과·지역 균형 정렬을 유지하면서 실제 `PremiumAdCarousel`이 화면에 장착됩니다. 정지·이전·다음·묶음 선택의 한글 안내와 접근성 라벨도 정상화했습니다.
- **Sites 이미지 업로드 저장소 복구**: 전용 `UPLOADS`가 없는 Sites에서는 기존 `BACKUPS` R2를 업로드 저장소로 재사용합니다. 비로그인 업로드 요청이 저장소 503이 아니라 정상 인증 401 단계로 진입하는 것을 배포본에서 확인했습니다.
- **권한·모바일 보완**: `/medical-staff/jobs/:id` 직접 접근에도 의료인 로그인 게이트를 적용하고, 모바일 메뉴 버튼의 최소 터치 영역을 44×44px로 보장했습니다.
- **검증 상태**: Node 테스트 70건, 프로덕션 빌드, Edge 실제 화면 자동검증, 공개본 HTTP 200을 통과했습니다.

## 이전 최신화에서 확인·수정 (2026-07-29)

- **다른 PC 회원가입·로그인 검증**: 신규 일반회원과 병원회원이 PC 1에서 가입한 뒤 PC 2에서 같은 계정으로 로그인되는 흐름을 확인했습니다. PC 2 로그아웃 후에도 PC 1 세션이 유지됩니다.
- **테스트 역할 전환 서버화**: 상단·로그인 화면의 3개 전환 버튼이 클라이언트 고정 비밀번호 대신 서버 전용 API를 사용합니다. 새 브라우저에서도 예약 계정을 복구하고 역할별 마이페이지·관리자 콘솔로 이동합니다.
- **검증 상태**: Node 테스트 64건과 프로덕션 빌드 통과, OpenAI Sites v221 배포본에서 역할별 전환 흐름을 실제 브라우저로 확인했습니다.

## 이전 최신화에서 확인·수정 (2026-07-26)

- **주소 검색 콘텐츠 차단 수정**: 병원 가입 주소 검색 팝업이 CSP에 막히던 것을 인라인 iframe 임베드로 변경.
- **인재 프로필 '샘플/예시' 제거**: 데모 인재를 실제 후보처럼 노출하고 '이력서 열람권 구매' 버튼을 띄움(이름은 마스킹 유지).
- **관리자 콘솔 UX**: 대시보드 위젯 클릭 시 해당 섹션 이동. `/api/admin-console` 403 시 빈 페이지 대신 안내 게이트(재시도·로그인).
- **로그인 안정화**: 관리자→콘솔·회원→마이페이지 라우팅 복구(QA 폐지 여파), 느린 PC의 로그인 직후 401 방지(세션 확인 + 재시도).
- **관리자 콘솔 전 메뉴 점검**: 15개 섹션 모두 콘텐츠 렌더 확인(빈 상태도 '데이터 없음' 메시지). 실제 빈 페이지 없음.

## 이번 최신화에서 확인·수정 (2026-07-25)

- **전수조사 버그 9건 수정**: 병원 사진 클릭 크래시, 비공개 이력서 유출(열람권으로 미공개 의사 연락처 노출), 상담 역할검사 fail-closed, 테스트모드 결제 멱등성+팩 크레딧 이중적립 방지, 진짜 병원이 광고 결제 못하던 게이트 버그, 후보 동의 전 노출 차단, 관리자 회원수정 고아행 방지, 최상위 에러 핸들러, 미등록 API의 HTML 200 오응답. (자세한 건 CHANGELOG·SECURITY)
- **열람권 가격 개편·팩 크레딧 방식**: 단건 3,900 / 10명팩 29,000 / 30명팩 69,000원. 팩은 크레딧 풀에서 새 인재 열람 시 1개 소모. 환불 시 전액 회수.
- **환불 시스템 검증**: 요청→승인→권한회수(단건·팩 크레딧 모두)→금액 정합성 확인.
- **마이페이지 배선 수정**: 관심공고 목록 제목·링크 복구, 데모 프로필 실사용자 노출 차단, 환불 요청중 상태 표시.
- **회원가입 간소화**: '병원 회원가입' 버튼 → '회원가입'. 병원 가입폼에서 기관 유형·대표 전화·우편번호 제거, **사업자등록번호 필수화**, 병원 주소는 **다음(카카오) 우편번호 검색** 연동. '계속해서 계정 인증' 버튼 → '회원가입 신청/완료'로 정리.
- **화면 미리보기(QA 프리뷰) 폐지**: 페이지 이동을 막던 원인. 헤더 링크·리본 제거, `/qa-preview`→홈. 모든 화면이 실제 세션 기준으로만 동작.
- **테스트 계정 3개**: 로그인 화면 원클릭 버튼(관리자·병원·의료인). 없으면 자동 생성. 상세는 `AUTH.md`. ⚠️ 정식 오픈 전 제거/비활성화.
- **3역할 기능 E2E 검증**(로컬): 병원 광고결제→마이페이지 표시, 열람권 10팩→크레딧 소모, 의료인 이력서 저장·구직글 버튼, 관리자 공고 게시→/jobs 노출, 콘솔 로드. 콘솔 에러 0.

## 이번 최신화에서 확인·수정 (2026-07-22)

- **의사 유료 멤버십을 폐지**하고 수익모델을 병원 광고·열람권 2가지로 단순화했습니다. 관련 페이지·결제창·상품·문구를 모두 제거하고, 옛 `/membership` 링크는 채용정보로 보냅니다.
- **열람권을 병원 전용으로 막았습니다.** 예전에는 의사도 결제할 수 있었는데 열람 권한은 병원에만 주어져 돈만 내고 못 쓰는 상태였습니다.
- **배포본 글꼴이 전부 깨져 있던 것을 고쳤습니다.** 보안 헤더(CSP)에 구글폰트가 빠져 Noto Sans KR·Manrope가 차단되고 있었습니다(로드된 웹폰트 0개 확인). ⚠️ 배포해야 반영됩니다.
- **의료인 구인구직 게시판에 '구직글 등록' 버튼**을 추가했습니다(의료인만 노출, 병원은 숨김).
- **배포본 회원가입·로그인이 실제로 되는지 직접 확인**했습니다. 가입폼 입력→검증→제출→재로그인→마이페이지까지 전 구간 정상(테스트 계정 삭제 완료).

- 대문 히어로 영상이 OS '동작 줄이기' 설정을 켠 PC에서 아예 표시되지 않던 문제를 수정했습니다(reduce-motion 시 영상을 `display:none` 처리하던 CSS 제거). 동작 줄이기·저사양 프로필(`performance-lite`)을 모두 켠 상태에서 재생되는 것을 실제 브라우저로 확인했습니다.
- 하단 '병원 채용 바로가기' 도크가 뜨기까지의 대기를 줄였습니다(첫 등장 2200→700ms, 스크롤 후 1450→400ms).
- GitHub Pages 미러를 최신 작업분으로 배포했습니다(209커밋). 되돌림 지점은 `backup-main-before-video-deploy` 태그입니다.
- 테스트 미러가 검색엔진에 색인될 수 있던 상태를 차단했습니다. 정식 도메인 배포에는 영향이 없도록 소스가 아닌 Pages 워크플로에서만 주입합니다.
- 공개 번들에 실제 개인정보가 섞이지 않았는지 점검했습니다(전화·이메일 전부 샘플·업무용 대표번호, 주민번호 패턴 0건).

## 이전 최신화에서 확인·수정

- 상담 제출 시 가입된 계정의 역할과 상담 유형이 다르면 서버가 거부합니다.
- 상담 내역의 회원 연결 기준 이메일은 사용자가 임의 입력한 값이 아니라 인증된 계정 이메일로 고정했습니다.
- 손상된 상담·관리 콘텐츠 JSON 한 건 때문에 마이페이지나 관리자 콘솔 전체가 열리지 않도록 안전 파싱을 적용했습니다.
- 공개 카테고리 API의 캐시 헤더가 실제 응답에 반영되도록 응답 헬퍼를 수정했습니다.

## 알려진 핵심 차이

관리자 화면의 콘텐츠 건수는 D1에 등록된 운영 콘텐츠만 집계합니다. 공개 채용정보 화면은 D1 운영 콘텐츠와 `src/data.js`의 시연용 정적 데이터를 합쳐 보여주므로 현재 두 화면의 건수는 같지 않습니다. 운영 전 정적 데이터를 D1로 이전한 뒤 관리자 DB를 유일한 데이터 원장으로 바꿔야 합니다.

## 출시 차단 조건

- 약관·개인정보처리방침·제3자 제공·환불정책 법률 검토
- `ADMIN_EMAILS`, `ACCOUNT_HASH_SECRET`, 가입 기능 플래그 운영값 검증
- 면허·병원 담당자 인증
- PG 계약과 승인·웹훅·취소·환불 통합 테스트
- R2 이미지 업로드 악성 파일 검사(업로드 기능과 Sites 저장소 연결은 구현됨)
- 정적 샘플의 운영 DB 이전

## 데이터 보호 현황 (2026-07-25)

- 기존 Sites D1 `DB`를 유지하고 별도 R2 `BACKUPS`에 일일 스냅샷을 저장합니다.
- 관리자 콘솔에서 즉시 백업 생성·목록·다운로드·SHA-256 무결성 상태를 확인할 수 있습니다.
- 백업은 35일, 실행 이력은 1년 동안 순환 보관합니다.
- 탈퇴 재가입 제한값 30일, 종료 상담·감사 로그 3년, 거래 기록 5년 기준의 자동 정리를 적용했습니다.
- 결제 기록이 있는 탈퇴 계정에서도 이력서·관심항목·활동·동의·인재 열람권·잔여 크레딧을 즉시 삭제합니다.
