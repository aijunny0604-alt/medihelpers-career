# 2026-08-03 배포·검증 인수인계

## 현재 기준

- 공개본: https://medihelpers-career.junnyai.chatgpt.site
- OpenAI Sites 버전: v233
- 소스 브랜치: `agent/medihelpers-ui-stability`
- 기준 커밋: `ae95268`
- 자동 테스트: 79/79 통과
- 프로덕션 빌드 통과
- `.openai/hosting.json`의 기존 프로젝트 ID, D1 `DB`, R2 `BACKUPS` 바인딩 유지

## 병원 광고 공개 흐름

1. 병원회원이 광고 상품을 신청하면 `payment_orders`와 `admin_content_records(draft)`가 함께 생성됩니다.
2. 가상 또는 실결제 승인 후 주문은 `paid`가 되고 상품별 노출기간이 결제 metadata에 기록됩니다.
3. 결제 완료만으로 공고를 공개하지 않습니다. 관리자가 콘텐츠 관리에서 공고를 승인해 `published`로 바꿔야 공개 API에 포함됩니다.
4. 관리자 콘솔 조회 시 결제 상품 등급과 노출기간이 공고 payload에 동기화됩니다.
5. 공개 `/jobs` 화면은 `spotlight`·`featured` 공고를 프리미엄 영역에, 베이직·일반 공고를 진행 중 공고 목록에 배치합니다.
6. 노출 종료일이 지나면 서버 공개 API와 클라이언트 목록에서 모두 제외됩니다.

## 상품 매핑

| 결제 상품 | 내부 상품 ID | 공개 등급 | 노출 위치 |
| --- | --- | --- | --- |
| 베이직 공고 | `basic` | 없음 | 진행 중 의사 초빙공고 |
| 추천 공고 | `featured` | `featured` | 프리미엄 광고 |
| 집중 채용 | `intensive` | `spotlight` | 프리미엄 광고 최우선 |

## 운영 검증 결과

- 최신 공고 `124124 아무 의사나 초빙` 상태: `published`
- 공개 범위: `public`
- 광고 등급: `spotlight`
- 상품명: `집중 채용`
- 노출 종료일: `2026-09-17`
- 공개 화면: 프리미엄 광고 첫 번째 묶음에서 제목·병원명·지역·진료과 한글 표시 확인
- 병원회원 `GET /api/member-center`: HTTP 200
- 병원회원 `GET /api/payment-orders`: HTTP 200, 최신 집중 채용 주문 `paid`
- 병원회원 `GET /api/saved-jobs`: HTTP 200
- 비로그인 `GET /api/account`: HTTP 200, `signedIn:false`
- 비로그인 `GET /api/resumes`: HTTP 401(정상 권한 차단)
- 병원회원 `GET /api/resumes`: HTTP 403(이력서는 의료인 회원 전용이므로 정상 권한 차단)

## 운영 주의사항

- 결제 승인 화면과 콘텐츠 게시 승인 화면은 목적이 다릅니다. 결제 `paid`와 콘텐츠 `published`를 모두 확인합니다.
- 공고 승인·수정 API에 한글 JSON을 보낼 때는 UTF-8 바이트와 `application/json; charset=utf-8`을 사용합니다.
- 테스트 역할 전환은 출시 직전 `TEST_ACCOUNT_SWITCH_ENABLED=false`로 비활성화합니다.
- OpenAI Sites는 가상 결제 검증용이며 이니시스 실결제는 Cloudflare Workers 이전 후 활성화합니다.

## 관련 문서

- `STATUS.md`
- `BILLING.md`
- `TEST.md`
- `DEPLOY.md`
- `AUTH.md`
