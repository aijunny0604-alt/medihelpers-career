# 예외·동시 요청 검수 — 2026-09-11

## 범위와 방법

기준 배포 349, 소스 c76faec88b796dc5c340f90909a95627fb4496fe에서 추가 조사했습니다. 생성된 실제 Worker에 임시 SQLite와 모의 PG를 연결하고, 동시 요청과 DB 실패를 주입했습니다. 실제 고객 정보·실제 카드·외부 승인 서버는 사용하지 않았습니다. 테스트 로그인과 관리자 접근 정책은 유지합니다.

`npm run test:adversarial`로 58개 검증 항목을 재현할 수 있습니다. D1의 batch 트랜잭션을 모사하는 SQLite 어댑터를 사용하며, 이전 낙관적 크레딧 차감 코드에는 읽기 동기화 장벽을 넣어 같은 잔여 수량을 동시에 읽는 상황을 재현했습니다. 실제 Cloudflare 부하 시험과 실제 PG 통합 시험을 대체하지 않습니다.

## 재현한 문제와 수정 결과

| 상황 | 수정 전 | 수정 후 |
|---|---|---|
| 동일 주문 승인 8개 동시 요청 | 거래기록 8개 | 상태 전환에 성공한 요청만 거래·이벤트 기록, 거래 1개 |
| 취소·환불·부분환불 주문 재승인 | paid로 되살아남 | 409, 원래 상태 보존 |
| 결제 후 열람권 저장 실패 | 성공으로 표시, 재시도해도 미지급 | 처리 지연 표시, 같은 주문 재시도로 지급 복구 |
| 결제 기록 저장 도중 DB 실패 | 트랜잭션 검증 필요 | 주문·거래 모두 롤백, 재시도 후 거래 1개 |
| 여러 인재가 같은 잔여 수량을 동시에 읽음 | 충분한 수량에도 한 요청만 성공 | 권한 발급·차감·감사 기록을 한 트랜잭션으로 처리 |
| 마지막 1크레딧에 8개 요청 | 경쟁 조건 위험 | 한 명만 열림, 음수 잔여 없음 |
| 같은 인재 8회 재열람 | 첫 열람 경쟁 조건에 영향 | 모두 상세 확인, 추가 차감 없음 |
| 하루 한도 근처에서 여러 인재 동시 열람 | 요청들이 같은 이전 집계를 읽음 | 새 크레딧과 선구매한 단건 모두 한도 내 처리 |
| 차감 SQL 실패 | 발급/차감 일관성 위험 | 발급과 차감 함께 롤백, 503 후 재시도 가능 |
| 이미 열린 인재 단건 재구매 | 새 주문 생성 | 추가 주문 거절 |
| 주문 후 구직글 삭제 | 쓸 수 없는 단건 권한 | 다른 인재에 사용할 수 있는 1크레딧 보존 |
| 같은 인재를 대상으로 주문 2개 동시 승인 | 두 번째 주문의 추가 권한 없음 | 인재 권한 1개와 잔여 1크레딧 보존 |
| JSON null·배열·숫자·문자열·깨진 JSON | 일부 500 오류 | 객체 입력만 받고 400 반환 |
| 잘못 인코딩된 인재 주소 | 500 | 400 |
| 긴 이력서·공고 내용 | JSON 문자열을 잘라 손상 저장 | 크기 초과 거절, JSON을 자르지 않고 저장 |
| 길이 헤더 없는 큰 요청 | 전체 본문을 먼저 메모리에 읽음 | JSON/이미지는 실제 스트림 바이트 수를 제한 |
| 결제창 form-urlencoded 결과 | 본문을 두 번 읽어 실패 | Content-Type에 따라 한 번만 파싱 |
| 이니시스와 비슷한 외부 호스트명 | 빌드 중 정규식 이스케이프 소실로 허용 | 정확한 호스트 경계·HTTPS·포트 검사, 외부 요청 0개 |
| 승인 응답에 주문번호가 없음 | 금액만 맞으면 승인 | 주문번호 일치 필수, 거래번호도 확인 |
| 승인 서버 리다이렉트·통신 실패 | 따라가기/무제한 대기 가능 | 리다이렉트 차단, 시간 제한, HTTP 오류 확인 |
| 실패 콜백과 성공 콜백 경쟁 | 실패 응답이 paid를 덮어쓸 수 있음 | 미결제 상태에서만 failed 변경 |
| 결제 후 오류 화면에서 재시도 | 새 주문을 만들 수 있음 | 같은 주문만 재확인하는 복구 화면 |
| 오류 상태에서 새로고침 | 화면 상태가 사라짐 | 계정·상품별 탭 저장소에 주문번호 복원 |

## 직접 화면 검수

격리된 로컬 화면에서 병원 계정으로 1명 열람권을 신청하고, 열람권 지급 SQL을 한 번 실패시켰습니다. 실패 안내→새로고침→동일 주문번호 복원→복구 버튼 연속 클릭→활성화 완료를 확인했습니다. DB에는 주문 1건·거래 1건·열람권 수량 1개만 남았습니다. 광고 등록과 열람권 구매는 같은 복구 컴포넌트를 사용합니다. 탭 저장소가 차단된 브라우저에서는 현재 화면의 재시도만 유지되며 새로고침 복원은 보장되지 않습니다.

## 최종 검증과 한계

자동 테스트 227개, 기존 실제 Worker 검수 36개, 예외 검수 58개와 프로덕션 빌드를 확인합니다. 기존 역할·로그인·관리자 조회 정책은 변경하지 않았습니다. 일반 테스트는 일부 소스 계약 검사이므로 실제 Worker 검증과 화면 검수를 함께 사용했습니다. 주문번호를 받은 이후의 복구를 검증했으며, 주문 생성 응답 자체가 유실되어 주문번호를 받지 못한 경우의 자동 복구는 아직 보장하지 않습니다.

여전히 정식 운영 완료를 의미하지 않습니다. 실제 PG 계약 환경에서 결제창 복귀·네트워크 장애 후 PG 거래 대사·카드 취소/환불·실제 다중 Worker 부하 검증이 필요합니다. 특히 로컬 거래 중복 기록 방지는 실제 PG가 동시에 도착한 승인 요청을 어떻게 처리하는지에 대한 검증을 대신하지 않습니다. 현재는 가상 결제, 공개 테스트 계정, 조회 전용 관리자, 시연 데이터가 남습니다. 이번 수정 이전에 이미 발생한 중복 거래나 잘못 차감된 과거 데이터는 임의로 변경하지 않았습니다.
