# TEST

## 병원 담당자 직책 선택 항목 검증 (2026-08-30)

- 담당자 직책이 화면에서 `선택`으로 표시되는지 검사합니다.
- 담당자 직책을 비운 병원회원 가입 초안이 검증을 통과하는지 검사합니다.
- 개인정보 수집 안내와 가입 문서의 필수·선택 구분을 같은 기준으로 유지합니다.
- `npm test`: **203/203 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 최종 운영 배포: Sites version `320`.

## 회원가입 이메일 입력 폭 회귀 검증 (2026-08-30)

- 로그인 이메일에만 데스크톱 반폭 규칙이 적용되고 주소처럼 다른 전체 폭 필드에는 영향을 주지 않는지 검사합니다.
- 780px 이하에서 이메일 입력이 전체 폭으로 복원되는지 검사합니다.
- `npm test`: **202/202 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 최종 운영 배포: Sites version `319`.

## 로그인·회원가입 외부 플랫폼 문구 제거 검증 (2026-08-30)

- 계정 화면 소스에 외부 플랫폼 계정명이 남아 있지 않고 메디헬퍼스 가입 이메일 안내만 표시하는지 검사합니다.
- `npm test`: **201/201 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 최종 운영 배포: Sites version `318`.

## 로그인·회원가입 진입 화면 크기 회귀 검증 (2026-08-30)

- 로그인 카드의 최대 폭과 입력창·로그인 버튼·회원가입 버튼의 확대 규칙을 검사합니다.
- 메인 헤더에서만 비회원 전용 회원가입 버튼을 제공하고, 작은 화면에서는 숨겨 모바일 메뉴와 겹치지 않는지 검사합니다.
- `npm test`: **200/200 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 최종 운영 배포: Sites version `317`.

## 공고 상세 지원 메뉴 스크롤 추적 회귀 검증 (2026-08-30)

- 공고 상세 사이드 메뉴에 전용 클래스가 적용되는지 검사합니다.
- 1181px 이상에서는 고정 헤더 아래에서 sticky로 따라오고, 1180px 이하에서는 일반 본문 배치로 전환되는지 검사합니다.
- `npm test`: **198/198 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 최종 운영 배포: Sites version `316`.

## 회원가입 비밀번호 배치·전문 분야 선택 항목 회귀 검증 (2026-08-30)

- 로그인 이메일이 2열 전체 폭을 차지해 다음 행의 비밀번호와 비밀번호 확인이 같은 줄에 배치되는지 검사합니다.
- 작은 화면에서 가입 입력 그리드가 한 열로 전환되는 기존 반응형 규칙을 유지하는지 확인합니다.
- 의료인 전문 분야를 비운 가입 정보가 클라이언트 검증을 통과하고 오류 항목에 포함되지 않는지 검사합니다.
- 전문 분야 입력값이 있으면 기존과 동일하게 프로필에 저장되고 이력서 기본값으로 연결되는지 기존 연동 테스트를 함께 실행합니다.
- `npm test`: **197/197 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 최종 운영 배포: Sites version `315`.

## 내 글 관리 위치·열람권 유한 수량 회귀 검증 (2026-08-30)

- 작성자 수정·삭제 버튼이 카드 하단 관리행에 있고, 모바일에서 전체 폭으로 내려가는지 정적 UI 테스트로 검사합니다.
- 상품 카탈로그가 단건 1명, 팩 10명, 대용량 팩 30명만 적립하는지 검사합니다.
- 팩 크레딧이 `used_credits < total_credits`일 때만 새 인재 최초 열람에 1건 차감되는지 검사합니다.
- 같은 병원·인재 조합의 재열람 및 동시 요청이 유니크 인덱스와 `INSERT OR IGNORE`로 중복 발급되지 않고, 후발 차감이 즉시 복구되는지 검사합니다.
- 마이페이지 API가 유효한 팩의 총수량·사용수량·잔여수량·최근 만료일을 반환하는지 검사합니다.
- `npm test`: **195/195 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 최종 운영 배포: Sites version `314`.

## 이력서·구직글 분리 및 관리자 상세 열람 회귀 검증 (2026-08-30)

- `/resume`에서 구직 공개와 연락처 공개 선택을 제공하지 않고 클라이언트와 서버가 이력서를 항상 개인 문서로 저장하는지 검사합니다.
- `/job-seeker-posts/new`가 저장된 본인 이력서를 요구하며, 이력서가 없으면 작성 후 같은 구직글 작성 화면으로 돌아오는지 검사합니다.
- 연락처 공개 여부가 구직글 원장에만 저장되고 `private` 선택을 열람권으로 우회할 수 없는지 기존 보안 테스트와 함께 확인합니다.
- 관리자 콘솔 API가 `detail_json`을 파싱하고 이력서 목록 및 헤드헌터 문의 첨부 이력서를 원시 JSON이 아닌 읽기 쉬운 상세 항목으로 표시하는지 검사합니다.
- `npm test`: **193/193 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 운영 브라우저 검증: 로그아웃 상태의 의료인 전용 권한 안내 정상, 의료인 테스트 계정 전환 후 이력서 선택·게시글 입력·연락처 공개 선택 화면 정상, 오류 로그 0건.
- 최종 운영 배포: Sites version `313`.

## 병원 즉시 가입·전화번호 비공개 표시 회귀 검증 (2026-08-30)

- 병원 가입이 사업자등록증 파일 형식·크기·서명을 검사한 뒤 승인 대기 없이 계정과 세션을 즉시 반환하는지 검사합니다.
- 과거 `unverified/pending/rejected` 병원 데이터가 `verified/approved`로 한 번만 정규화되고 정지·탈퇴 계정 차단은 유지되는지 검사합니다.
- 병원 광고 주문은 로그인한 병원 역할만 허용하며 승인 상태를 별도 게이트로 사용하지 않는지 검사합니다.
- 관리자 콘솔이 GET 읽기 전용이고 병원 승인·반려 작업 코드가 제거됐는지 검사합니다.
- 비공개 구직글이 목록·상세에 빨간 안내를 표시하며 공개 API와 열람권 응답에 전화번호·이메일 사본을 넣지 않는지 검사합니다.
- `npm test`: **184/184 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`, `npm audit --omit=dev`: 통과·취약점 0건
- 최종 운영 배포: Sites version `307`, GitHub 기준 브랜치와 동일 커밋. 비회원·의료인·병원·관리자 역할별 API 응답을 확인합니다.

## 구직글 전용 페이지·작성자 CRUD·병원 공고 삭제 차단 (2026-08-29)

- `job_seeker_posts`가 본인 이력서를 외래키로 참조하고 활성 이력서당 중복 게시를 막으며, 삭제 시 물리 삭제 대신 상태와 활동 이력을 남기는지 검사합니다.
- 구직글 API가 비회원 401, 병원·관리자 403을 반환하고 의료인도 본인 이력서·본인 게시글만 생성·수정·삭제할 수 있는지 검사합니다.
- 전용 페이지에서 복수 이력서 선택, 게시용 조건 입력, 연락처 `private/ticket` 선택을 제공하는지 검사합니다.
- 게시판과 마이페이지에 작성자 수정·삭제 동선이 있고, 병원 유료 공고 삭제 요청은 서버 403이며 편집 화면에는 수정 전용 정책이 표시되는지 검사합니다.
- `npm test`: **184/184 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 최종 운영 배포: Sites version `306`, GitHub 기준 브랜치와 동일 커밋. 비회원·의료인·병원·관리자 역할별 API 응답을 확인합니다.

## 회원가입 정보·공고·이력서 연동 회귀 검증 (2026-08-29)

- 가입 시 역할별 기본값이 D1 `member_registration_profiles`에 저장되고 로그인한 본인의 `/api/account` 응답에만 포함되는지 검사합니다.
- 병원 공고 등록이 가입·제출 정보로 병원명, 담당자, 연락처, 주소, 대표자와 사업자번호를 채우며 주문 서버도 누락 값을 보완하는지 검사합니다.
- 의료인 이력서가 가입 정보의 직군·전문분야·지역을 채우되 기존 저장본과 사용자가 이미 입력한 값을 덮어쓰지 않는지 검사합니다.
- 구직글 등록 동선이 전용 게시 원장에 저장되고 공개 목록이 `linkedResumeId`로 선택한 원본을 가리키는지 검사합니다.
- 증빙 원본·R2 키가 본인 계정 자동입력 응답에 포함되지 않는지 검사합니다.
- `npm test`: **179/179 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과

## 업로드 배너 크기·기존 데이터 보정 회귀 검증 (2026-08-29)

- 공고 등록의 한 장짜리 브랜드 파일이 `logo`가 아닌 `banner` 목적으로 업로드되고 `full-banner` 레이아웃으로 저장되는지 검사합니다.
- 동적 공고 매핑이 `brandImageLayout`을 카드와 상세 페이지까지 전달하는지 검사합니다.
- 완성형 업로드 배너는 3:1 스테이지를 가득 채우고 중복 병원명 오버레이를 렌더하지 않으며, 샘플 배너는 기존 오버레이를 유지하는지 검사합니다.
- 과거 `single-brand-image` 주문만 일회성 마이그레이션 대상이 되고, 기존 로고 필드를 제거한 뒤 배너와 레이아웃을 설정하는지 검사합니다.
- `npm test`: **175/175 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 운영 공개 API 검증: 유료 광고 3건 중 업로드형 2건이 `full-banner`; 두 건 모두 배너 URL 존재·로고 필드 미존재.
- 운영 캐시·기본 API: HTML `Cache-Control: no-store`, 비로그인 `/api/account` HTTP `200`, 최신 해시 JS·CSS 응답 확인.
- 최종 운영 배포: Sites version `303`, GitHub 기준 브랜치와 동일 커밋.
- Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 비회원 병원 공고 등록 차단 회귀 검증 (2026-08-28, 승인 상태 게이트는 2026-08-30 폐기)

- 비로그인 상태에서 `/advertise/apply?plan=basic`과 `/advertise/apply?plan=featured`를 직접 열어도 등록 폼이 렌더되지 않고 병원회원 안내만 표시되는지 확인합니다.
- 등록 폼을 연 뒤 로그아웃해도 제출 시 이미지 업로드와 `POST /api/payment-orders`가 시작되지 않는지 검사합니다.
- 비로그인 `POST /api/payment-orders`는 HTTP 401, 의료인·관리자는 HTTP 403을 반환하는지 검사합니다.
- 2026-08-30부터 병원 승인 상태는 주문 게이트가 아니며 로그인한 병원 역할이면 베이직·메인 광고 주문을 만들 수 있는지 검사합니다.
- 로컬 개발 API도 운영 서버와 같은 로그인·역할·주문 소유자 검사를 적용하는지 정적 회귀 테스트로 확인합니다.
- `npm test`: **174/174 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 운영 새 쿠키 검증: 비회원 베이직·메인 `POST /api/payment-orders` 각각 HTTP `401`; 의료인 베이직 주문 HTTP `403`; 의료인 `/api/resumes`, 병원 `/api/member-center`, 관리자 `/api/admin-console` 각각 HTTP `200`.
- 최종 운영 배포: Sites version `301`, GitHub 기준 브랜치와 동일 커밋.
- Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩은 변경하지 않았습니다.

## 공개 채용공고 비회원 열람 회귀 검증 (2026-08-27)

- 광고 등급이 공개 공고의 잠금 조건으로 사용되지 않고 `비공개` 배지만 보호 대상으로 남는지 검사합니다.
- 비회원에게 카드 급여와 상세 페이지의 보수·근무 일정·입사 조건이 그대로 표시되는지 검사합니다.
- 공개 공고의 직접 지원 동선이 로그인 권한 확인 때문에 지연되지 않는지 검사합니다.
- 비공개 헤드헌팅 포지션과 구직 인재 개인정보의 기존 보호 동선이 유지되는지 검사합니다.
- `npm test`: **170/170 통과**
- `npm run build`, `git diff --check`: 통과

## 공고 웹포스터 업로드·상세 노출 회귀 검증 (2026-08-27)

- 상세내용 아래 업로드 영역, 다중 선택, 드래그앤드롭, 붙여넣기, 미리보기와 개별 삭제를 검사합니다.
- 업로드 API의 `poster` 용도와 PNG·JPG·WEBP, 장당 5MB, 최대 3장 저장 제한을 검사합니다.
- 주문 데이터에 저장된 웹포스터와 시설 사진이 동적 공고 상세까지 전달되는지 검사합니다.
- 공개 상세 화면의 원본 비율 표시와 원본 크게 보기 링크를 검사합니다.
- `npm test`: **170/170 통과**
- `npm run build`, `git diff --check`: 통과

## 공고 등록 최종 동의 체크박스 정렬 회귀 검증 (2026-08-27)

- 결제·게시 동의 영역이 체크박스 22px와 설명 문장의 2열 구조를 사용하는지 검사합니다.
- 체크박스의 높이와 최소 높이가 입력창 공통 규칙과 분리되는지 검사합니다.
- 선택 상태의 파란 체크 표시와 키보드 포커스 스타일을 검사합니다.
- `npm test`: **167/167 통과**
- `npm run build`, `git diff --check`: 통과

## 공고 등록 연락처 자동 하이픈 회귀 검증 (2026-08-27)

- 공고 등록 연락처가 회원가입 공용 formatter를 사용하는지 검사합니다.
- 숫자 키패드, 전화번호 자동완성, 13자 제한과 `010-1234-5678` 자동 변환을 검사합니다.
- `npm test`: **166/166 통과**
- `npm run build`, `git diff --check`: 통과

## 공고 등록 버튼 가시성 검증 (2026-08-27)

- 자동 테스트: 마지막 단계 인라인 등록 버튼, 1440px 이하 고정 버튼, `IntersectionObserver` 기반 중복 숨김, 모바일 1열 배치를 검사합니다.
- 브라우저 1366×768: 스크롤 중 하단 버튼 고정, 최종 버튼 진입 시 고정 버튼 숨김 확인.
- 브라우저 390×844: 하단 버튼이 좌우 10px 안에 들어오고 문서 가로 넘침이 없음을 확인.
- 브라우저 1920×1080: 하단 버튼 미노출, 우측 결제 요약 `sticky` 유지 확인.
- 브라우저 콘솔 오류·경고 없음.
- `npm test`: **165/165 통과**
- `npm run build`: 통과

## 회원가입 연락처 필수·자동 하이픈 회귀 검증 (2026-08-27)

- 의료인·병원 회원가입의 연락처가 필수 필드 목록과 네이티브 `required` 속성에 모두 포함되는지 검사합니다.
- 숫자 입력이 `010-1234-5678` 형태로 정리되고 최대 입력 길이가 제한되는지 검사합니다.
- 테스트 API와 실제 배포 서버가 연락처 누락·잘못된 휴대폰 번호를 동일하게 거부하는지 검사합니다.
- 당시 `npm test`: **164/164 통과** (현재 전체 166개)
- `npm run build`, `node --check scripts/package-sites.mjs`, `git diff --check`: 통과

## 채용공고 카드 배너·상세내용 용어 회귀 검증 (2026-08-27)

- 메인·베이직 광고 카드의 상단선이 1px 중립 경계로 고정되고, 등급 색은 배지에만 남는지 검사합니다.
- 배너 스테이지가 얇은 경계와 은은한 그림자를 사용하며 포인터 환경에서만 호버 이동이 활성화되는지 검사합니다.
- 공고 등록 화면과 문서의 자유서식 명칭이 `상세내용`으로 표시되는지 검사합니다.
- 모바일 하단 채용 바로가기 문구가 좁은 공간에서 한 글자씩 줄바꿈되지 않는지 검사합니다.
- `npm test`: **163/163 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과

## 구직 인재 독립 상세·인증 제거 회귀 검증 (2026-08-21)

- 인재 카드가 `/medical-staff/talents/{id}` 독립 경로로 이동하고 상세 페이지와 목록 복귀 링크를 제공하는지 검사합니다.
- 기존 `TalentDetailModal` 구현이 제거되고 페이지 전용 반응형 스타일을 사용하는지 검사합니다.
- 화면·스타일·샘플 데이터에 `헤드헌터 인증` 문구, 배지 스타일, 인증 플래그가 다시 포함되지 않는지 검사합니다.
- 인재 열람권과 의료인 연락처 공개 동의 정책은 기존 회귀 테스트로 함께 확인합니다.

## 병원 가입·의료인 연락처 보호 회귀 검증 (2026-08-15, 승인 흐름은 2026-08-30 폐기)

- 의료인의 연락처 공개 선택 기본값이 `private`이고, 잘못된 값도 서버에서 `private`로 정규화되는지 검사합니다.
- 이력서 상세 JSON에 전화번호·이메일·실명 사본이 남지 않는지, 병원이 열람권을 보유해도 비공개 연락처를 받지 못하는지 검사합니다.
- 병원 가입이 사업자등록증 파일을 요구하고, 허용 형식·크기·파일 서명을 서버에서 검사하는지 확인합니다.
- 병원 계정은 제출 완료 즉시 세션을 발급하고, 과거 대기·반려 데이터도 즉시 가입 정책으로 정규화되는지 검사합니다.
- 병원 인증 원본이 공개 URL이 아니라 관리자 전용 API와 기존 비공개 R2 바인딩을 통해서만 제공되는지 검사합니다.
- 관리자 콘솔은 병원 가입을 포함해 모든 기록이 읽기 전용이고 비-GET 요청은 405인지 검사합니다.
- 당시 `154/154`였으며 최신 전체 회귀 테스트는 **184/184**입니다.
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- Sites `project_id`, 기존 D1 `DB` 및 R2 바인딩은 변경하지 않았습니다.

## 공고 등록 노트북 반응형·필수 라벨 회귀 검증 (2026-08-15)

- 1440px 이하에서 입력 본문과 결제 요약이 한 열로 전환되고, 1180px 이하에서 브랜드 미리보기와 업로드가 한 열로 전환되는지 검사합니다.
- 입력 본문과 섹션이 `min-width: 0`을 유지해 긴 업로드 안내 문구가 옆 영역을 침범하지 않는지 검사합니다.
- 병원명·기관 유형·담당자명·연락처·이메일·병원 위치·초빙 분야가 별표 없이 빨간 `필수` 문구를 사용하는지 검사합니다.
- 로컬 1280px 브라우저: 가로 넘침 `false`, 입력 본문·결제 요약 겹침 `false`, 결제 레이아웃 `1열`, 필수 라벨 `7개` 확인.
- `npm test`: **150/150 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- Sites `project_id`, D1 `DB`, R2 `BACKUPS` 및 D1 스키마는 변경하지 않았습니다.

## 병원 채용 상단 바 위치·크기 회귀 검증 (2026-08-15)

- `채용정보` 제목과 `공고 등록` 버튼이 26px 간격의 동일한 왼쪽 정렬 그룹으로 표시되는지 검사합니다.
- 등록 버튼이 56px 이상 높이, 16px 글자, 파란색 그라데이션과 추가·이동 아이콘을 유지하는지 검사합니다.
- 780px 이하에서는 제목과 버튼을 양쪽에 배치하고, 430px 이하에서도 최소 50px 터치 높이를 유지하는지 검사합니다.
- 로컬 `/jobs`: `채용정보` 1개, `공고 등록` 링크 1개, `헤드헌팅 상담` 0개를 확인했습니다.
- `npm test`: **148/148 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- Sites `project_id`, D1 `DB`, R2 `BACKUPS` 및 D1 스키마는 변경하지 않았습니다.

## 공개 페이지 상담 CTA 제거 회귀 검증 (2026-08-15)

- 홈은 `의사 초빙정보 보기`, 광고센터는 `광고 상품 선택`, 의료인회원의 구인구직 화면은 `의료인 이력서·구직 글 등록`을 유지하는지 검사합니다.
- 홈 `헤드헌터 상담`, 병원 채용 `헤드헌팅 상담`·하단 상담 배너, 의료인 구인구직의 역할별 상담 버튼, 광고센터 `헤드헌터 채용 상담`·`별도 견적 상담`이 소스와 실제 화면에 없는지 검사합니다.
- 전용 맞춤 헤드헌팅 페이지의 `의사 헤드헌터에게 상담하기`와 `병원 · 채용 의뢰하기`는 실제 접수 기능으로 유지되는지 별도 검사합니다.
- 로컬 브라우저 `/`, `/medical-staff`, `/jobs`, `/advertise`: 제거 대상 문구 모두 `false`, 유지 대상 주 기능 모두 `true` 확인.
- `npm test`: **147/147 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과
- 보존 원칙: Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩과 D1 스키마는 변경하지 않았습니다.

## 마이페이지 세션 충돌·일시 오류 회귀 검증 (2026-08-15)

- 서버가 HttpOnly 쿠키와 탭 보조 bearer 토큰을 모두 수집하고 쿠키를 우선 검증한 뒤, 유효하지 않을 때 다음 토큰을 확인하는지 검사합니다.
- 로그아웃은 요청에 포함된 쿠키·탭 세션을 모두 D1에서 폐기하는지 검사합니다.
- 공용 `/api/account` 조회가 일시 실패해도 확정된 `member`를 `guest`로 낮추지 않고 3회 재시도와 자동 재연결을 수행하는지 검사합니다.
- 회원센터 응답 전에는 빈 지표를 그리지 않고 로딩 상태를 유지하며, 최종 연결 실패 시 공용 회원 권한과 역할을 보존하는지 검사합니다.
- 로컬 실제 클릭: 일반회원 `홈·알림·이력서·상담·결제·회원정보`, 병원회원 `홈·알림·내 공고·문의·결제·회원정보`, 관리자 전환에서 로그인 안내 `0개`, 임시 오류 `0개`를 확인했습니다.
- `npm test`: **145/145 통과**
- `npm run build`: 통과
- 보존 원칙: Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩과 D1 스키마는 변경하지 않았습니다.

## 전체 기능·권한·운영 화면 재검증 (2026-08-15)

- 운영 HTTP: 공개·계정·상담·상세·법무 화면 19개, `robots.txt`·`sitemap.xml`·웹 앱 매니페스트 3개, 1500×500 샘플 배너 6개가 모두 HTTP `200`과 올바른 콘텐츠 형식을 반환했습니다.
- 비회원 API: `/api/account` `200`, `/api/resumes` `401`, `/api/member-center` `401`, 미등록 API `404` JSON.
- 의료인 API: 테스트 전환 `200`, `/api/account` `200` `role=doctor`, `/api/resumes` `200`, `/api/member-center` `200`, `/api/admin-console` `403`.
- 병원 API: 테스트 전환 `200`, `/api/account` `200` `role=hospital`, `/api/member-center` `200`, `/api/resumes` `403`, `/api/admin-console` `403`.
- 관리자 API: 테스트 전환 `200`, `/api/account` `200` `isAdmin=true`, `/api/admin-console` `200`.
- 브라우저 역할 점검: 의료인 10개 경로와 병원 6개 경로에서 인증 로딩 잔여 요소 `0개`, 깨진 이미지 `0개`, 가로 넘침 `0개`, `???`·유니코드 대체문자 `0개`를 확인했습니다. 병원회원의 `/resume`·`/request/job-seeker`에는 재로그인·재가입 버튼이 나타나지 않습니다.
- 독립 상세: 채용공고는 `/jobs/{id}`에서 모집개요·상세조건·지원 동선, 맞춤 헤드헌팅은 `/headhunting/posts/{id}`에서 상세정보·상담·목록 복귀 동선을 확인했습니다. 일반 화면에는 내부 접수 코드가 노출되지 않았습니다.
- 가입·로그인: 병원 가입 주소 검색은 카카오 검색 UI를 실제로 표시했고, 로그인 화면의 이메일·비밀번호·계정 찾기 동선과 잘못된 `회원가입 완료` 문구 부재를 확인했습니다.
- 접근성 기본 검사: 공개 주요 9개 화면에서 이름 없는 버튼·링크·폼 필드와 `alt` 없는 이미지가 모두 `0개`, 화면별 `h1` 1개를 확인했습니다.
- 의존성 보안: `npm audit --omit=dev` **0건**. 취약한 전이 버전을 제거하고 `vinext 1.0.0-beta.6`, `vite 8.2.1`, `postcss 8.5.26`, `nanoid 3.3.18`을 잠금 파일에 반영했습니다.
- 자동 검증: `npm test` **144/144**, `npm run build`, `node --check dist/server/index.js`, `git diff --check` 통과. 빌드의 단일 청크 크기 경고는 후속 성능 개선 항목입니다.
- 보존 원칙: 과거 D1 테스트 원문의 `??` 레거시 데이터, Sites `project_id`, D1 `DB`, R2 `BACKUPS` 바인딩과 스키마는 변경하지 않았습니다.

## 상단 테스트 계정 전환 회귀 검증 (2026-08-15)

- 전환 응답의 `doctor`, `hospital`, 관리자 권한을 `medihelpers:auth-changed` 이벤트에 포함해 공용 인증 상태가 즉시 반영되는지 검사합니다.
- 전환 뒤 `window.location.assign`으로 현재 페이지를 다시 읽지 않고, 동일 URL에서 선택 역할만 갱신하는지 검사합니다.
- 이전 계정 조회와 새 계정 조회가 겹쳐도 요청 순번이 지난 응답을 무시하는지 검사합니다.
- `/mypage`에서 병원회원 → 일반회원 전환 시 `HOSPITAL DASHBOARD`가 제거되고 `DOCTOR DASHBOARD`가 표시되는지, 관리자 전환 시 상단 계정 링크가 `/admin/console`을 가리키는지 확인했습니다.
- 브라우저 실제 클릭: 병원회원 → 관리자 → 일반회원 모두 `aria-pressed=true`, URL 변화 없음, `.auth-action-pending` `0개` 확인.
- `?slow-auth=1`: 클릭 직후와 900ms 후 병원회원 선택 유지, `.auth-action-pending` `0개` 확인.
- `npm test`: **144/144 통과**

## 운영 사이트 직접 검증 (2026-08-15)

- 대상: `https://medihelpers-career.junnyai.chatgpt.site` / Sites version `279` / commit `868ca62`.
- 실제 일반회원 브라우저 세션으로 `/resume`, `/medical-staff`, `/jobs`, `/advertise`, `/mypage`, `/request/hiring`을 열고 핵심 콘텐츠, 역할 불일치 안내 부재, 인증 완료 후 `.auth-action-pending` `0개`를 확인했습니다.
- 별도 쿠키 세션에서 `POST /api/auth/test-switch`를 의료인·병원·관리자 순서로 실행해 모두 HTTP `200`을 확인하고, 각 세션의 `GET /api/account`가 D1 역할과 관리자 권한을 정확히 반환하는지 확인했습니다.
- 의료인 세션 `GET /api/resumes`: HTTP `200` 및 등록 이력서 JSON 확인.
- 병원 세션 `GET /api/member-center`: HTTP `200` 및 공고·결제·알림 JSON 확인.
- 관리자 세션 `GET /api/admin-console`: HTTP `200` 및 읽기 전용 운영 기록 JSON 확인.
- 비회원 `GET /api/account`: HTTP `200` JSON, 비회원 `GET /api/resumes`: 의도된 HTTP `401` JSON 확인.
- 과거 2026-08-08 테스트 지원·알림의 일부 `??` 문자열은 D1에 이미 저장된 레거시 원문으로 확인했습니다. 이번 검증에서는 데이터 보존 원칙에 따라 수정·삭제하지 않았습니다.
- `npm test`: **142/142 통과**
- `npm run build`, `node --check dist/server/index.js`, `git diff --check`: 통과

## 전 화면 인증 UI 첫 렌더 회귀 검증 (2026-08-15)

- 공고 상세·의료인 채용 상세·이력서·헤드헌팅 접수·상담·결제·계정 화면이 App의 공용 계정 상태를 재사용하고 자체 `/api/account` 요청을 만들지 않는지 검사합니다.
- 헤더, 병원 채용 추천/기본 공고 등록, 광고 상품, 하단 등록 도크가 권한 확인 전 실제 CTA 대신 같은 크기의 중립 자리를 유지하는지 검사합니다.
- 관리자 콘솔이 DB 응답 전 데모 데이터와 관리 컨트롤을 렌더링하지 않는지 검사합니다.
- 로컬 `?slow-auth=1` 500ms 지연에서 응답 전후 헤더·공고 버튼 폭 차이 `0px`, 응답 후 `.auth-action-pending` `0개`를 브라우저로 확인했습니다.
- 비회원 `/`, `/resume`, `/medical-staff`, `/login`, `/jobs`, `/advertise`; 의료인 `/resume`, `/request/job-seeker`, `/request/hiring`, `/jobs`, `/medical-staff`, `/advertise`, `/mypage`; 병원 `/resume`, `/request/job-seeker`, `/advertise`, `/jobs`, `/medical-staff`; 관리자 `/admin/console`을 확인했습니다.
- `npm test`: **142/142 통과**

## 회원 역할 버튼 깜빡임 회귀 검증 (2026-08-15)

- 인증이 필요한 하위 페이지가 앱의 공용 계정 상태를 재사용하고 `/api/account`를 중복 호출하지 않는지 검사합니다.
- 역할 확인 전에는 `의료인 회원으로 등록`, `회원가입`, `로그인` 같은 비회원 CTA를 추측해 노출하지 않는지 검사합니다.
- 병원회원의 구직 의료인 게시판은 작성·상담 버튼 자리를 안정적으로 비우고, 의료인회원에게만 구직글 등록을 표시하는지 검사합니다.
- 데스크톱 헤더와 모바일 계정·하단 버튼이 같은 인증 로딩 기준을 사용하는지 검사합니다.
- `npm test`: **140/140 통과**

## 회원 역할 불일치 게이트 회귀 검증 (2026-08-15)

- 로그인한 병원회원이 의료인 전용 이력서 화면에서 재로그인·재가입 버튼을 보지 않는지 검사합니다.
- 역할 불일치 화면에서는 보호된 이력서 폼을 렌더링하지 않고 독립 안내 페이지만 표시하는지 검사합니다.
- 안내 페이지의 뒤로 가기와 직접 진입 시 안전한 공개 목록 대체 경로를 검사합니다.
- 비회원의 로그인 경로와 의료인회원의 정상 이력서 접근은 그대로 유지되는지 검사합니다.
- `npm test`: **137/137 통과**

## 채용공고 등록 화면 크기 회귀 검증 (2026-08-15)

- 데스크톱 등록 화면의 섹션 여백·제목·라벨·입력값·샘플 배너 글자가 확대 규격을 유지하는지 검사합니다.
- 입력창이 최소 60px 높이이고 17px 글자를 사용하는지 검사합니다.
- 781~980px 구간의 브랜드 이미지 한 열 전환 규칙이 유지되는지 검사합니다.
- `npm test`: **135/135 통과**

## 병원 채용 중간 메뉴 간소화 회귀 검증 (2026-08-15)

- `/jobs` 히어로 아래 도구 모음에 `공고 등록`만 남는지 검사합니다.
- `전체 채용`, `의료인 구인구직`, `맞춤 초빙`, `내 활동` 링크가 다시 추가되지 않는지 검사합니다.
- 비로그인 공고 등록은 병원회원 가입 경로를 유지하는지 검사합니다.
- `npm test`: **134/134 통과**

## 공개 화면 검수 표현 회귀 검증 (2026-08-09)

- 메인, 공고 상세, 헤드헌팅 상세, 광고센터에 사전 검수·확인 후 게시를 암시하는 문구가 없는지 검사합니다.
- 광고센터의 즉시 게시 안내가 유지되는지 검사합니다.

## 회원 유형·광고 즉시 게시·하단 정리 회귀 검증 (2026-08-09)

- 회원가입 역할이 D1 `accounts.role`에 저장되고 로그인 API가 DB 역할을 조회하는지 검사합니다.
- 로그인 화면에 회원 유형 재선택 입력이 없는지 검사합니다.
- 병원 광고 주문 생성 후 가상·실결제 승인 경로가 공고를 자동 `published`로 전환하는지 검사합니다.
- 과거 결제 완료 `draft` 광고 자동 복구와 관리자 승인·반려 UI 제거를 검사합니다.
- 홈페이지 하단 의사·병원·안내 링크 열이 제거됐는지 검사합니다.
- `npm test`: **106/106 통과**

## 회원정보 로딩 성능 회귀 검증 (2026-08-08)

- 반복 스키마 준비 결과를 Worker 인스턴스에서 재사용하는지 검사합니다.
- 프로필·상담·결제·메시지 조회가 하나의 D1 `batch` 요청으로 실행되는지 검사합니다.
- 로그인·권한 검사 이후에만 회원 데이터가 조회되는 기존 순서를 유지합니다.

## 알림 메시지 상세 연결 회귀 검증 (2026-08-08)

- 문의 답장을 알림뿐 아니라 대화 원장에도 저장하는지 검사합니다.
- 기존 알림 본문을 문의번호로 연결해 상세 페이지에서 복원하는지 검사합니다.
- 상세 페이지가 송·수신 메시지 본문과 시간을 대화형 UI로 표시하는지 검사합니다.

## 지원자 실제 회원 이름 회귀 검증 (2026-08-08)

- 신규 의료인 지원 시 회원 프로필 `display_name`을 접수 이름으로 저장하는지 검사합니다.
- 기존 접수 건은 이메일로 연결된 계정의 회원 프로필 이름을 우선 표시하는지 검사합니다.
- 회원 프로필이 없을 때 이력서 제출본 이름과 기존 접수 이름으로 안전하게 대체되는지 검사합니다.

## 구직글 작성자 무료 열람 회귀 검증 (2026-08-08)

- 로그인 계정 ID와 구직 이력서 `account_id`가 일치할 때만 작성자 무료 열람을 허용하는지 검사합니다.
- 작성자 열람 시 병원 열람권과 팩 크레딧을 사용하지 않는 분기 순서를 검사합니다.
- 목록의 `내 글 · 무료`, 상세의 `작성자 무료 열람`과 수정 버튼을 검사합니다.
- 타인 글은 기존 병원 열람권 정책을 유지하는지 검사합니다.

## 문의 상세 독립 페이지 회귀 검증 (2026-08-08)

- `/mypage/inquiries/{문의ID}` 라우팅 및 목록 이동 경로를 자동 테스트합니다.
- 기존 문의 모달 상태 코드가 제거됐는지 검사합니다.
- 과거 테스트 데이터의 깨진 물음표 문자열을 안전한 안내문으로 대체하는지 검사합니다.
- 상세 답변·상태 변경 API는 기존 인증과 권한 검사를 그대로 사용합니다.

## 현재 권위 있는 전수검증 결과 (2026-08-08)

- `npm test`: **104/104 통과**
- `npm run build`: 통과
- 생성 Worker `node --check dist/server/index.js`: 통과
- 비회원 공개 화면 7개: 모두 HTTP 200 HTML
- 공개 API: `/api/account`, `/api/categories`, `/api/site-operations` HTTP 200 JSON
- 보호 API: 비회원 `/api/resumes` 401, `/api/member-center` 401, 미등록 API 404 JSON
- 역할별 테스트 세션: 의료인·병원·관리자 전환 및 `/api/account`, `/api/member-center` HTTP 200
- 권한 경계: 의료인의 관리자 콘솔 403, 병원의 이력서 API 403, 병원의 무료 공고 생성 403 `FREE_POSTING_DISABLED`
- 관리자: 콘솔·상담·CRM·백업 목록 HTTP 200, 데이터 보호 상태 `configured:true`
- CSRF: 외부 Origin의 상태 변경 요청 HTTP 403
- R2 실객체: 병원 PNG 업로드 201 → 조회 200 `image/png`; SVG 업로드 415
- 가상 결제: 열람권 주문 201 → 승인 200, `testMode:true`, 실제 청구 없음, 열람권 응답 `unlocked:true`
- 이력서 회귀: 마이페이지 의료인 카드가 `/resume`으로 이동하고, 수정 화면이 `GET /api/resumes`의 최신 D1 데이터를 채우는지 자동 검사

아래 날짜별 숫자는 당시 회귀 테스트 이력입니다. 현재 판정에는 위 98건 결과를 사용합니다.

## 2026-08-08 이미지 입력·회원 알림 검증

- 공통 이미지 업로드, 병원 광고 결제 이미지, 이력서 프로필 사진에 드래그앤드롭과 클립보드 붙여넣기 경로가 있는지 검사합니다.
- 이력서 작성·관리 버튼이 같은 액션 그룹에 배치되는지 검사합니다.
- `member_notifications` 런타임 스키마와 Sites 마이그레이션 포함 여부를 검사합니다.
- 병원 유료광고 직접 지원이 상담·활동·알림을 같은 D1 batch로 저장하는지 검사합니다.
- 병원·지원 의료인 메시지, 읽음 처리, 전체 읽음, 상단 배지와 마이페이지 알림 UI 연결을 검사합니다.
- `npm test`: 97/97 통과
- `npm run build`: 통과

## 2026-08-08 헤드헌터 문의·병원 직접 지원 분리 검증

- 맞춤 헤드헌팅 상세 CTA가 `headhuntPost` 원문 정보를 보존한 의사 헤드헌터 문의 경로로 이동하는지 검사합니다.
- 병원 유료 채용광고 CTA가 `job` ID를 보존한 병원 직접 지원 경로로 이동하는지 검사합니다.
- 서버가 `submissionChannel`을 직접 판정하고 유료광고 지원에는 헤드헌터 이메일·문자 알림을 실행하지 않는지 검사합니다.
- 헤드헌터 관리자 상담함에서 유료광고 직접 지원이 제외되고, 공고 등록 병원 마이페이지에는 해당 지원이 표시되는 SQL 계약을 검사합니다.
- 두 원문 유형을 동시에 제출하는 요청이 거부되고, 선택한 이력서 스냅샷이 기존 보안 규칙대로 저장되는지 검사합니다.
- `npm test`: 93/93 통과
- `npm run build`: 통과

## 2026-08-08 마이페이지·맞춤 헤드헌팅 상세 이동 검증

- 병원·의료인 활동 요약 4개 위젯이 버튼과 접근 가능한 상세보기 이름을 제공하는지 검사합니다.
- 관심 공고와 추천 후보 위젯이 해당 상세 목록 앵커로 이동하는지 검사합니다.
- 맞춤 헤드헌팅 목록 행이 `/headhunting/posts/{공고ID}`로 이동하고 이전 모달 컴포넌트가 제거됐는지 검사합니다.
- 상세 페이지의 목록 복귀, 모바일 반응형 레이아웃과 상담 CTA 연결을 검사합니다.
- `npm test`: 92/92 통과
- `npm run build`: 통과
- 운영 상세 URL: HTTP 200, 배포 번들에서 독립 상세 페이지·목록 복귀 경로 확인

## 2026-08-08 채용공고 상세 페이지 전환

- 홈과 채용 목록의 카드가 공고 ID를 URL 인코딩한 `/jobs/{공고ID}` 경로로 이동하는지 검사합니다.
- 상세 경로가 `JobDetailRoute`를 렌더링하고 기존 모달 상태 호출이 제거됐는지 검사합니다.

## 2026-08-08 상세 모달 기관 아이콘

- 배너 파일이 정사각형 기관 로고로 중복 렌더링되지 않는지 검사합니다.
- 배너와 구분되는 정식 로고는 기존 로고 표시 경로를 유지하는지 검사합니다.

## 2026-08-08 집중채용 카드 가독성

- 확대된 제목과 메타 정보가 2줄 제한과 카드 높이 안에서 유지되도록 프로덕션 빌드를 검증합니다.

## 2026-08-08 공고 상세 모달 배너

- 배너가 있는 상세 화면이 전용 헤더 클래스와 배너 컴포넌트를 사용하는지 자동 검사합니다.
- 데스크톱 2열과 모바일 1열에 필요한 반응형 스타일은 프로덕션 빌드에 포함됩니다.

## 2026-08-08 샘플 채용 배너 검증

- 샘플 6종이 모두 실제 JPEG 파일이고 정확히 1500×500px인지 검사합니다.
- 각 파일이 업로드 제한 8MB보다 작은지 검사합니다.
- 샘플 선택값이 결제 주문의 `banner`로 저장되고 서버 공개 공고 데이터에 전달되는지 소스 계약을 검사합니다.
- 상세 화면 배너 렌더링과 OpenAI Sites 정적 이미지 응답 경로 6종을 검사합니다.
- `npm test`: 85/85 통과
- `npm run build`: 통과

## 2026-08-08 집중채용 그리드 확대 검증

- 반응형 노출 기준이 모바일 2개, 태블릿 4개, 데스크톱 6개로 고정되어 있는지 검사합니다.
- 기존 프리미엄 순환 컴포넌트 연결과 광고 정렬 테스트를 함께 유지합니다.

## 2026-08-08 채용공고 채널 분리 검증

- 일반 병원 채용광고는 `operationalDoctorJobs`에 남고 헤드헌팅 콘텐츠로 판정되지 않는지 검사합니다.
- `publicationChannel='headhunt_board'`인 의사·의료인 콘텐츠는 헤드헌팅 전용으로 판정되고 일반 채용 목록에서는 제외되는지 검사합니다.
- `npm test`: 81/81 통과
- `npm run build`: 통과

## 2026-08-08 채용 배너 검사

- `src/recruitmentBanners.test.js`가 프리미엄 fixture 전체의 `cardBanner` 존재 여부를 확인합니다.
- 각 JPEG의 실제 SOF 크기를 읽어 1500×500px(3:1)인지 검사하고 파일당 8MB 제한을 검증합니다.
- 브라우저 1600×1000 검사: 3개 순환 묶음의 8개 공고가 모두 로드되고, 표시 영역과 이미지가 같은 3:1 비율이며 `object-fit: cover`가 적용됨을 확인했습니다.
- 브라우저 390×844 검사: 노출 카드가 288×96으로 유지되고 문서 가로 넘침이 없음을 확인했습니다.
- 당시 Node 자동 테스트는 80건이었으며 `npm test`, `npm run build`가 통과했습니다.

## 자동 검증

```bash
npm test
npm run build
```

현재 165건의 Node 테스트가 다음 순수 로직과 스키마 계약을 검증합니다.

- 브라우저 저장소 실패·손상 데이터 복구
- 회원가입 입력 검증과 개인정보 초기화
- QA 역할·권한 상태
- 인재정보 공개 범위
- 공고 등급·진료과·지역 균형 노출
- 관리자 콘텐츠·결제·회원 운영 스키마
- 사이트 운영 카테고리 변환
- 병원 카드 색상·로고 대체 규칙
- 서버 테스트 역할 전환 API 배선과 클라이언트 비밀번호 미포함
- Sites R2 업로드 저장소 폴백(`UPLOADS` → `BACKUPS`)
- 홈·채용정보의 공유 가능한 독립 상세 페이지 연결과 프리미엄 광고 순환 컴포넌트 장착
- 구형 D1 결제 유형 제약과 열람권 주문 호환 저장
- 가상 승인 실패 시 성공 화면 진입 차단과 무청구 안내
- 유료 광고 상품 등급과 결제 노출기간이 공개 공고 레코드에 동기화되는지
- 계정 도움 요청이 D1 API 저장 결과와 접수번호를 확인한 뒤에만 성공 화면으로 전환되는지
- 실제 저장 기능이 없는 이력서 문서 첨부 UI가 다시 노출되지 않는지

## 로컬 가상 API 목 (개발용)

OpenAI Sites 서버(`scripts/package-sites.mjs`)는 로컬에 없어 `npm run dev`에선 `/api/*`가 404다. 그래서 결제·열람권·이력서 저장 등 서버 흐름을 로컬에서 확인할 수 없다. 이를 위해 **개발 모드 전용 가상 목**(`src/devApiMock.js`)을 둔다.

- `npm run dev`(vite)에서만 자동 설치(`import.meta.env.DEV` 가드). **배포 빌드에는 포함되지 않는다**(빌드 후 `client-build/`에 `devApiMock` 문자열 없음으로 확인).
- 동작: `window.fetch`를 감싸 `/api/*`만 가로채 localStorage로 흉내낸다.
  - `POST /api/payment-orders` → 주문 생성 / `POST /api/payment-approve` → 가상 승인 + 열람권(talentId) 부여
  - `GET /api/talent-detail/:id` → 부여된 열람권이 있으면 상세 공개, 없으면 잠금
  - `POST /api/resumes` → 이력서 저장 / `GET /api/account` → 병원 목 계정
- 목 초기화: 브라우저 콘솔에서 `localStorage.removeItem('devmock_talent_unlocks')` 등으로 재설정.
- 주의: 목은 **화면 흐름 확인용**이며 실제 서버 검증이 아니다. 실동작은 배포 후 Sites에서 확인한다.

## 배포 전 수동 시나리오

- 홈과 채용정보의 동일 공고가 `/jobs/{공고ID}` 독립 상세 페이지로 열림
- 진료과·지역·초빙유형·근무조건·키워드 조합과 초기화
- 관심공고 저장, 더보기, 추천순·최신순
- 일반 회원·병원 회원 가입 화면과 역할별 진입
- PC 1에서 가입한 일반 회원·병원 회원이 PC 2에서 같은 이메일·비밀번호로 로그인
- PC 2에서 로그아웃한 뒤 PC 1의 기존 세션 유지
- 로컬 저장 데이터가 없는 새 브라우저에서 일반회원·관리자·병원회원 테스트 전환 버튼과 역할별 도착 경로 확인
- 비로그인 상담 차단, 로그인 상담 제출, 잘못된 역할 제출 거부
- 마이페이지 상담·주문 상세 확인
- 병원 광고 결제 `paid`와 공고 게시 `published`를 각각 확인하고, 집중·추천 상품이 프리미엄 영역에 배치되는지 확인
- 관리자 상담함·CRM·통합 모니터링·콘텐츠 상세
- 관리자 카테고리 변경이 공개 필터에 반영
- 존재하지 않는 경로와 의료인 채용 경로
- 모바일 메뉴·모달·사진 갤러리·ESC·방향키
- 360·768·1366·1920px에서 가로 넘침과 문서 스크롤
- `prefers-reduced-motion` 환경에서 장식 모션 비활성화

## 운영 연결 후 필수 통합 테스트

- 가입 승인 약관 버전과 본인확인
- 회원 정지·탈퇴·결제 기록 보존
- PG 성공·실패·취소·부분환불·중복 웹훅·금액 변조
- R2 업로드 권한·파일 형식·용량·실제 객체 쓰기·읽기·삭제
- 후보 동의 부여·철회·만료와 열람 감사
- 정적 fixture를 제거한 뒤 관리자 DB 건수와 공개 목록 건수 일치
